{
  "name": "foxworks-dispatch",
  "version": "2.0.1",
  "private": true,
  "description": "Conductor monorepo: dispatch-core (shared lib), dispatch-cli (fd CLI), dispatch-daemon (Conductor daemon).",
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "dispatch-cli": "workspace:*",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  }
}
