# Followup index

Consolidated index of post-MVP work filed across Phase Y + Phase Z by both
Session A (daemon + CLI) and Session B (web UI + menubar). Each entry
references its origin cluster/ticket; commit bodies have richer context.

Three priority bands: **MVP-required** (must ship for v2.0 — should be
empty if Phase Z closed cleanly), **Post-MVP / v2.1** (planned next
iteration), **Deferred / nice-to-have** (no current commitment).

Z-7 harvest snapshot: 18 distinct followups across both sessions. MVP-
required band empty as expected (Z-1 + Z-4 closed real bugs as part of
their own tickets; no MVP-blocker carried into Z-7).

---

## MVP-required

_(empty)_

If anything surfaces here at re-harvest, it represents work that should
have resolved before Phase Z cluster close.

---

## Post-MVP / v2.1

### Session A — daemon + CLI

| ID | Scope | Origin |
|---|---|---|
| `DAEMON-F-installer-cross-platform` | Linux / Windows installer paths (S04 documented darwin-only for v2.0) | D-6 / T18 |
| `DAEMON-F-installer-prebuilt-binary` | Bundle daemon as single-file executable for end users without Node.js installed | D-6 / T18 |
| `DAEMON-F-installer-code-signing` | Apple notarization for distribution outside this repo (S04 followup #2) | D-6 / T18 |
| `DAEMON-F-runtime-deps-hygiene` | `node-notifier`, `@fastify/websocket`, `ws` currently in devDependencies; move to dependencies pre-distribution | D-5 / T16 |
| `DAEMON-F-version-source` | `/v2/health` version is hardcoded `'0.0.0'`; read package.json dynamically at startup | D-5 / T16 |
| `DAEMON-F-watcher-registry-mutex` | Composite: `last_commit_sha` (T14) + `last_status_json_at` (T15) + possibly `last_handoff_pulled_at` written by manager via single mutex coordinated with route writes | D-4+D-5 |
| `DAEMON-F-last-commit-sha-update` | Subsumed by the composite mutex above | D-4 / T14 |
| `CLI-F-ci-setup` | CI matrix: daemon-up + daemon-down configs; runs the existing test suite under both per X2 line 361 | CLI-T / T05 |
| `CLI-F-probe-timeout-env` | `/v2/health` probe timeout (currently 500ms hardcoded) configurable via env var | CLI-T / T04 |

### Session B — web + menubar

| ID | Scope | Origin |
|---|---|---|
| `WEB-F-focused-card-highlight` | Visual refinement on focused-via-keyboard SessionCard | W-2 / WEB-T07 |
| `WEB-F-send-modal-error-differentiation` | Modal currently surfaces a single error region; differentiate validation vs network vs daemon-422 paths | W-3 / WEB-T11 |
| `UI-F-banner-copy` | Banner text refinement (Session B authoring decision) | W-1 |
| `UI-F-real-browser-modal` | Real-browser smoke test for SendPromptModal interactions (deferred per Session B's no-Playwright scope) | W-3 |
| `MB-F-pnpm-electron-packaging` | When COARCH-T03 reinstalls `@anthropic-ai/sdk`, re-spike packager against actual workspace dep tree. Decide between `node-linker=hoisted` (workspace-wide), `public-hoist-pattern` (local), or `electron-builder`. MB-S04 ADR's K6 claim (263 MB / 8.4 s) was sterile-spike evidence, not workspace-dep evidence. | MB-T01 green |
| `MB-F-mb-s04-spike-amendment` | Amend MB-S04 ADR §K6 to reflect spike-vs-reality gap discovered at MB-T01 green: sterile package.json doesn't surface pnpm symlink + `@electron/packager` prune walker incompatibility. Future packaging spikes must use actual workspace dep tree. | MB-T01 green |
| `MB-F-MB-T03-DOCK-BADGE` | Dock badge implementation deferred per RESOLUTION-2 (operator 2026-04-30). V3_TICKETS.md L120 "if relevant" language stripped from MB-T03 frozen surface (contract §4.6, OPEN-Q-C-2). Implement in a future ticket: `app.setBadgeCount(n)` for unread-action-count badge on the macOS dock icon. Requires daemon polling or SSE event to drive count updates. Non-blocking for v3.0 ship. | MB-T03 green |
| `MB-F-MB-T02-PRODUCTION-LOADING` | v3.0 ships dev-only `loadURL('http://localhost:7878')` per contract §3.4 (IMPORTANT-5 operator resolution 2026-04-30). Production-mode loading (static file fallback or packaged-app URL resolution) is out of scope for MB-T02. Implement in MB-T08 or a dedicated follow-on ticket. | MB-T02 green |
| `MB-F-MB-T02-DOM-ASSERTION` | Round 2 webview-loader test (webview-loader-callable.test.ts) verifies callability + non-crash; full DOM assertion of kanban columns (AWAITING REVIEW / STALE / RUNNING / IDLE headers, session cards) deferred until dispatch-web server runnable in test env (MB-T07 territory). | MB-T02 green |
| `MB-F-COARCH-T02-REAL-DAEMON-WIRING` | **CLOSED by COARCH-T03 7f15c78.** HttpDaemonClient wired in coarchitect-ipc.ts; mount.ts bridge adapter uses `window.coarchitectBridge`; AnthropicChatClient streams via Sonnet 4.6. Daemon persistence gracefully fails when daemon unreachable (synthetic local-only message fallback per HttpDaemonClient design). OPEN-Q-D-2 remains open for typed import resolution in a future ticket. | COARCH-T02 green → CLOSED COARCH-T03 |
| `MB-F-ZIPPER-2-ESM-PRELOAD` | Electron 41 sandboxed preloads cannot load ESM modules — `import` statements in `.mjs` files cause `SyntaxError: Cannot use import statement outside a module` in the sandbox context, regardless of `package.json` `"type":"module"`. Fixed in Zipper-2 by compiling `src/main/preload.mts` to `dist/main/preload.cjs` via esbuild `format:'cjs'`. All future preload scripts in this package must use CJS output. If Electron ever ships first-class ESM preload support, revisit and remove the `build-preload.mjs` script. | Zipper-2 green 039721f |
| `MB-F-ZIPPER-2-COARCH-T03-IPC` | **CLOSED by COARCH-T03 7f15c78.** coarchitect-ipc.ts now uses HttpDaemonClient (real /v3/* calls, graceful failure); sendAndStream one-way IPC handler added; preload exposes streaming bridge; mount.ts uses bridge adapter from `window.coarchitectBridge`. Integration test for real message persistence (daemon must be running) deferred to COARCH-T04+ scope. | Zipper-2 green 039721f → CLOSED COARCH-T03 |
| `MB-F-MB-T04-PROJECT-LIST` | MB-T04 ships file-dialog fallback only — V3_TICKETS.md L130 names a "configured project list" picker that depends on the settings UI (V3_TICKETS.md MB-T11 / W-T19). When MB-T11 lands, the spawn modal should grow a dropdown above the path input populated from `electron-store` (or equivalent) with the operator-configured project paths; current text input + Browse… stay as the manual entry / fallback. | MB-T04 green |
| `MB-F-MB-T04-INITIAL-PROMPT` | WORKSTATION_CONTRACT.md §3.3 spawn-new-session row lists "optional initial prompt" in the payload. MB-T04 modal does NOT surface this field; IPC payload shape committed as `{repoPath, sessionName}`. If MB-T05 needs it, extend modal with a multi-line textarea + extend payload to `{repoPath, sessionName, initialPrompt?: string}` and add a v3-schema entry for the IPC channel under §2.1. Tier-2 because behavior under absence is well-defined (claude starts with no prompt; operator types one in the spawned pane). | MB-T04 green |
| `MB-F-MB-T04-PAYLOAD-VALIDATION` | spawn-ipc.ts main-process listener accepts payload as `unknown` and only validates shape implicitly via JSON.stringify (test-hook only). MB-T05 should add a Zod schema (likely `WorkstationSpawnRequestedSchema` in dispatch-core/src/v3/schema.ts §2.1 IPC section) and validate at the main-process boundary, surfacing `WorkstationError` on shape mismatch per WORKSTATION_CONTRACT.md §6.5. Until then, a malformed renderer could pass non-string repoPath/sessionName. | MB-T04 green |
| `MB-F-MB-T04-EMPTY-INPUT-UX` | Spawn confirm button is a silent no-op when either input is empty (after `.trim()`). Acceptable for MB-T04 ship per ticket scope, but a future UX pass should add inline validation (red border + helper text) or button-disable-when-empty similar to MB-T06's at-cap disable pattern. Tier 3 — non-blocking. | MB-T04 green |
| `MB-F-CONSOLE-T01-60S-SOAK` | CONDUCTOR_API_CONTRACT.md §4.7.7 explicitly defers a 60-second sustained backpressure soak test to CONSOLE-T01 (then continues to defer it). Vision §10.10 ship-gate names "60 s sustained streaming" as a criterion. MB-S06 §4 KNOWN backpressure at 2-second burst; converting MODELED→KNOWN at 60-second scale requires a real-tmux soak harness. Land before v3.0 ship-gate sign-off. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-STDIN-SEQ-PERSIST` | §4.7.1 says "Counters are session-scoped and persist across daemon restarts via the cc_console_buffer table". cc_console_buffer stores stdout rows only; CONSOLE-T01 ships stdin_seq as in-memory only (resets at daemon restart). Add a `cc_console_state` table (or extend cc_console_buffer with a direction column) to persist stdin_seq across restarts. Operator-impact is minor (stdin_seq is for write-ack correlation; clients don't typically rely on its monotonicity across restart) but the contract literal wording does require this. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-BUFFER-ENABLED-PERSIST` | vision §10.5 lists buffer_enabled as operator-configurable. CONSOLE-T01 hardcodes default = true, no toggle endpoint. Settings UI ships in MB-T11/W-T19; the daemon-side persistence (per-session buffer_enabled column in cc_console_state) lands alongside it. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-PIPE-PANE-HIGH-WATER-PAUSE` | §4.7.3 says PTY reader "pauses if no subscribers remain AND ring buffer hits its high-water mark". CONSOLE-T01 ships always-keep-reading even with zero subscribers (simpler upper bound; memory bounded by cap-and-prune loop). Optimization: pause pipe-pane when zero subscribers AND buffer ≥ 0.95 × cap, resume on subscribe. Saves CPU under always-on daemon scenarios. Tier 3. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-AUDIT-LOG` | vision §10.11 Q5 + §4.7.7 defer audit-log integration for operator-typed prompts to v3.x (separate `console_prompts` table). Each POST /v3/sessions/:name/console/stdin should log to the audit table per §7.8 audit discipline. Pending v3.x amendment to §4.7. Tier 3. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-TMUX-HISTORY-LIMIT-CAP` | MB-S06 §4 finding: tmux's pane history-limit (default 2000 lines) duplicates retention with the daemon ring buffer. CONSOLE-T01 should call `tmux set-option -t <target> history-limit N` (small N, e.g. 100) per session at attach time so tmux does not duplicate-store the same lines the daemon has cached. Saves a few tens of MB per session in steady state. Tier 3. | CONSOLE-T01 green |
| `MB-F-MB-T05-PAYLOAD-VALIDATION` | spawn-ipc.ts `workstation:spawn-requested` listener still accepts `payload as SpawnSessionRequest` without runtime shape validation (cast trust). The MB-F-MB-T04-PAYLOAD-VALIDATION followup tracked this for MB-T05; MB-T05 GREEN preserved the cast pattern rather than introducing a Zod dependency. Add `WorkstationSpawnRequestedSchema` to `dispatch-core/src/v3/schema.ts` §2.1 IPC-section and validate at the main-process boundary, surfacing typed `WorkstationError` on shape mismatch per §6.5. Tier 2 — non-blocking but a malformed renderer could still pass a non-string repoPath/sessionName today. | MB-T05 green |
| `MB-F-MB-T05-INITIAL-PROMPT` | WORKSTATION_CONTRACT.md §3.3 lists "optional initial prompt" in the spawn-new-session payload. MB-T05 ships without it (matches MB-T04 modal which omits the field per MB-F-MB-T04-INITIAL-PROMPT followup). When operator wants initial-prompt support: extend SpawnSessionRequest with `initialPrompt?: string`, pass it to claude via tmux `new-session ... claude --prompt <text>` (or paste-buffer it after a brief delay if claude lacks a flag). Tier 2 — defer until ratified UX. | MB-T05 green |
| `MB-F-MB-T05-API-KEY-SETTINGS-UI` | **PARTIALLY CLOSED by MB-T08 d39f35d** (initial-entry path): MB-T08's onboarding flow now invokes `saveApiKey` from `src/onboarding/api-key-storage.ts` which calls `safeStorage.encryptString(plaintext)` and writes the buffer to `<userData>/anthropic-api-key.enc`; spawn-ipc.ts:defaultSpawnHandlerDeps reads it via `loadEncryptedApiKey`. Replacement-key UI for an already-onboarded operator (change key from settings panel without re-running onboarding) remains MB-T11/W-T19 scope. Tier 3 (downgraded from Tier 2 after MB-T08 closed the operator-blocker). | MB-T05 green → MB-T08 partial close |
| `MB-F-MB-T05-CLAUDE-FLAG-SUPPORT` | spawn-handler.ts assumes `tmux new-session ... claude` — i.e. claude with no flags. Real-world usage may want `claude --resume <id>` (continue conversation), `--model <id>`, or `--mcp-config <path>`. Extend SpawnSessionRequest with `claudeArgs?: string[]` and append to the argv. Tier 3 — defer until operator dogfood surfaces a need. | MB-T05 green |
| `MB-F-MB-T05-DAEMON-409-VERBATIM` | defaultRegisterSession surfaces 409 as SessionAlreadyRegistered with a generic message; the daemon's actual 409 body (per CONDUCTOR_API_CONTRACT.md §4.3 + Blocker 3 verbatim "Session name in use (killed record exists). Pick a new name.") is more useful for renderer UX. Forward the daemon body's `error` field through to the typed error envelope so the renderer can surface "Pick a new name" verbatim. Tier 3. | MB-T05 green |
| `MB-F-MB-T05-PRE-EXISTING-VALIDATOR-IMPORT` | Pre-existing failure observed during MB-T05 baseline: `test/unit/coarch-t04/build-doc-validator.spec.ts` fails to import `dispatch-core/src/v3/schema.js` in this worktree (build artifact missing — `dispatch-core` isn't compiled into `dist/` here). NOT MB-T05 territory; flagged for COARCH-T04 owner / build-system owner to investigate whether `dispatch-core` needs `pnpm -r build` before workstation tests, or whether the import path should resolve to `.ts` source. Tier 3. | MB-T05 baseline observation |
| `MB-F-MB-T08-ONBOARDING-RENDERER-MOUNT` | OnboardingModal React component lives at `src/onboarding/onboarding-modal.tsx` and is unit-tested in isolation (cluster 2 — 19 specs). The main-process IPC layer is wired (`workstation:onboarding-save-api-key` + `workstation:onboarding-complete` in main.ts d39f35d). What's missing: bundling the component into the production renderer + mounting it conditionally on first launch. Mirrors the `build-coarchitect.mjs` / `build-console-panel.mjs` esbuild pattern; needs a new `build-onboarding.mjs`, an `onboarding.html` page, and a `mount.tsx` entry. Without this, vision §8.1 ship-gate "First-launch flow walks operator through API key entry" is not satisfied for real operators (the persistence layer + smoke harness driver work, but the user-visible modal does not render in production). **Tier 1 — vision §8.1 ship-gate blocker.** | MB-T08 green |
| `MB-F-MB-T08-VISION-PROJECT-LIST-CONFIG` | Vision §8.1 (RATIFIED) onboarding requirement names two components: API key entry **AND** project list config. MB-T08 ships the API-key path; project-list config is not in MB-T08 scope per the ticket prompt's OOS line ("Project-list management UI" → MB-F-MB-T04-PROJECT-LIST + MB-T11/W-T19 settings UI). The acceptance gap: vision §8.1 ratified text expects both in the first-launch flow. Two operator paths: (a) accept the gap as a v3.0.x followup and ship MB-T08 satisfying API-key only; (b) extend MB-T08 onboarding with a project-picker step that captures the operator's first repo path before completion. Path (a) is the default unless operator arbitrates otherwise. **Tier 1 — vision §8.1 ship-gate decision.** | MB-T08 green |
| `MB-F-MB-T08-SPAWN-RESULT-SENTINEL` | The smoke harness's `spawnSession()` waits for `SPAWN_MODAL_OPENED` only — it cannot wait for spawn completion because no existing renderer-side subscription to `workstation:spawn-result` emits a console sentinel that the main.ts MB_TEST_HOOKS forwarder allowlist could surface. Wiring this requires (1) renderer-side subscription to `workstation:spawn-result` in `workstation-shell.html` (or a new preload bridge), (2) a `console.log('SPAWN_RESULT_OK')` on success / `SPAWN_RESULT_ERROR <type>` on failure, (3) extension of main.ts:54-72 forwarder allowlist. Crosses MB-T05 spawn-* territory which was OOS for MB-T08; defer to a small follow-on ticket so the smoke harness can validate the full spawn round-trip. Tier 2. | MB-T08 green → ENABLER RESOLVED Fix-B batch fix-batch-1 at `07fae77` (cairn finding #83). Items (1)–(3) now ship: `workstationBridge.onSpawnResult` subscribes via the new `spawn-result-listener.ts` helper; `workstation-shell.html` emits `SPAWN_RESULT_OK <sessionName>` / `SPAWN_RESULT_ERROR <error_type> <message>` console sentinels; main.ts SPAWN_RESULT_SUBSCRIPTION sentinel region forwards them under `MB_TEST_HOOKS=1`. The trivial follow-on (extending `smoke-harness.ts:spawnSession()` to `await this.ctrl.waitForSentinel('SPAWN_RESULT_OK …')`) is a single-line change against the now-shipped enabler — deferred per Fix-B scope-adjustment Option A (smoke-harness.ts is read-only in Fix-B file-ownership scaffold §1; not bundled in same patch). Live integration drive observed `SPAWN_RESULT_ERROR DaemonUnreachable …` end-to-end during Fix-B verification. Closing the smoke-harness consumer side is a sub-1-LOC next ticket. |
| `MB-F-MB-T08-SMOKE-CI` | Smoke harness ships at `src/main/run-smoke.ts` + `pnpm smoke` script. CI integration is operator territory and out of scope per the MB-T08 ticket prompt OOS line. Future v3.x work: GitHub Action that runs the smoke harness against a built `.app` on macOS runners with a placeholder API key + ephemeral daemon. Documented dependencies: a built `dist/`, a running daemon (or a daemon-less smoke variant that asserts onboarding-only). Tier 3. | MB-T08 green |
| `MB-F-MB-T08-MODAL-NOT-DISMISSIBLE-MID-FLOW` | OnboardingModal is intentionally not dismissible mid-flow (see test_onboarding_modal_dismissible_on_complete.spec.tsx rationale). Operators on a first-launch attempt who realize they don't have an API key cannot back out cleanly — they must close the entire app via Cmd+Q and relaunch later (which will re-trigger onboarding because config wasn't written). UX refinement: add a "Quit" / "Skip for now (limited functionality)" affordance on the welcome step. Tier 3 — defer until operator dogfood surfaces friction. | MB-T08 green |
| `MB-F-MB-T07-CARD-BRIDGE-PRELOAD-WIRING` | MB-T07 GREEN ships the dispatch-web side (OrchestratorCard component, card-ipc-bridge.ts emitters, useOrchestratorCards hook) and the workstation card-ipc.ts handler factory + pure audit-row builders. The Electron contextBridge wiring that exposes `window.cardBridge` to the dispatch-web webview is NOT yet landed: the webview needs its own preload script (the wrapper's preload.cjs runs in workstation-shell.html context, NOT in the nested `<webview>` for dispatch-web at WEB_UI_URL). Land a small `card-bridge.ts` factory (pattern after console-bridge.ts) plus a webview-preload script set via `<webview preload="...">` attribute in workstation-shell.html. Until then, dispatch-web's `window.cardBridge` is undefined and the bridge no-ops gracefully. Tier 1 — must ship before operator sees orchestrator-driven cards. | MB-T07 green → RESOLVED batch-6 Session B at 7217e66 (live integration dogfood-validated post-merge) |
| `MB-F-MB-T07-MAIN-IPC-WIRING` | `registerCardIpcHandlers` in src/main/card-ipc.ts takes an injected `ipcOn` for testability; production needs to register against `ipcMain.on` in main.ts alongside `registerIpcHandlers()` / `registerSpawnIpcHandlers()` / `registerConsoleIpcHandlers()`. Trivial wire-up. Tier 1 — bundles with the preload wiring above. | MB-T07 green → RESOLVED batch-6 Session B at 28f55c5 (extracted to card-wiring.ts; main.ts edit is the sentinel-region call) |
| `MB-F-MB-T07-DAEMON-AUDIT-CLIENT` | `card-ipc.ts` depends on a `DaemonAuditClient { postAudit(req) }` interface; production needs `HttpDaemonClient.postAudit(req: OrchestratorAuditWriteRequest)` that POSTs to `/v3/orchestrator/audit` (the daemon endpoint shipped by COARCH-T01). Mirror the existing `postMessage` pattern in HttpDaemonClient (graceful fallback on daemon-unreachable). Tier 1 — bundles with the wiring above. | MB-T07 green → RESOLVED batch-6 Session B at fc1d57c (pure-helper postAuditViaFetch + class wrapper; 5/5 unit tests cover success/401/422/ECONNREFUSED/no-token) |
| `MB-F-MB-T07-CARD-CONTEXT-CACHE` | `CardContextLookup` in card-ipc.ts is dependency-injected; production needs an actual cache populated when the shell emits `orchestrator-card-rendered` to the webview. The cache stores per-card context (trigger_event, build_doc_id, build_doc_commit_sha, output_payload, superseded_card_ids) so the audit-write path can reconstruct a complete `OrchestratorAuditWriteRequest` on Approve/Decline/multi-choice. Lands alongside MB-F-MB-T07-ORCHESTRATOR-CARD-EMITTER below. Tier 1. | MB-T07 green → RESOLVED batch-6 Session B at e933498 (CardContextCache class + cardContextCache singleton; get returns null per coord §4.4 not undefined as brief said) |
| `MB-F-MB-T07-ORCHESTRATOR-CARD-EMITTER` | The shell-side code that parses structured `CardOutput` / `MultiChoiceCardOutput` from the orchestrator's stream (`coarchitect-ipc.ts` `coarchitect:sendAndStream` path) and emits `orchestrator-card-rendered` IPC messages to the webview is NOT in MB-T07 scope. Currently the orchestrator's structured output surfaces as text in the chat panel only. A follow-on ticket (likely COARCH-T0x territory) parses `OrchestratorOutputSchema` discriminated union out of the model response, validates via Zod, and routes `card` / `multi-choice-card` outputs to the cardBridge while keeping `escape-block` in the chat panel and `action` (read-only `read-file`) firing-without-card per cairn-Sonnet §2.7. Tier 1 — without this MB-T07's webview surface has no input. | MB-T07 green → RESOLVED batch-6 Session B at f8c57f7 (orchestrator-output-router.ts pure variant routing + coarchitect-ipc.ts integration; emits orchestrator-card-rendered to webContents on card/multi-choice-card variants) |
| `MB-F-MB-T07-KANBAN-COLUMN-INTEGRATION` | `OrchestratorCardsLane` ships as the existing-column placement helper per WC §5.5 but is NOT yet wired into `KanbanColumn.tsx`. Wire it so `awaiting_review` column hosts the `awaiting` lane below its session cards and `stale` column hosts the `stale` lane below its session cards. Per WC §5.5: orchestrator cards plug into existing columns; per WC §5.2: subtle blue tint visual distinction (already in OrchestratorCard). Tier 2 — depends on the cardBridge being wired (above) so awaiting/stale buckets actually populate. | MB-T07 green |
| `MB-F-MB-T07-AUDIT-SUPERSESSION-SEMANTICS` | When a card is auto-superseded WITHOUT operator response (orchestrator-card-superseded arrives before any operator click), no audit row is written today. The `/v3/orchestrator/audit` endpoint is POST-only (no UPDATE) and `staleness_status` is set at insert time. Operator arbitration needed: should the shell write an audit row at supersession time with `operator_response='pending'` + `staleness_status='superseded'` so the audit log captures the supersession event? Or is no row by design (only operator-acknowledged supersessions are audited)? Surface for ratification — the v3.0 contract is silent on this case. Tier 2 — non-blocking for MB-T07 unit-test scope; needed before audit-log retention queries are operator-facing. | MB-T07 green |
| `MB-F-MB-T07-E2E-INTEGRATION` | V3_TICKETS.md MB-T07 acceptance lists "end-to-end test where operator uploads build doc, orchestrator generates action proposal as kanban card, operator approves, action fires, daemon-side state changes, audit row written". MB-T07 GREEN ships unit-test coverage of the seams (component, bridge, audit-row builders, IPC handlers — 35 web tests + 28 workstation tests, 226/226 web suite + 28/28 workstation MB-T07 suite). End-to-end with live orchestrator + live daemon belongs to Phase 3 dogfood validation per the redraft prompt's note. Land an integration-test harness in test/integration/ that spawns daemon + workstation + mocks Anthropic Sonnet 4.6 to drive the full flow. Tier 2 — Phase 3 ship-gate. | MB-T07 green |
| `MB-F-MB-T07-MULTI-CHOICE-OPTION-LABELS` | `OperatorResponseEnum` lists `multi-choice-A`..`multi-choice-D` but the v3 schema's `MultiChoiceCardOutputSchema.options` is `z.array(z.string()).min(2).max(4)` (just strings). Today `OrchestratorCardsLane` renders option text as button labels; the audit row records `multi-choice-A` etc. for routing. If the orchestrator wants to disambiguate "operator picked the option labeled X" vs "operator picked the option at index 0", a future schema amendment may add structured option records `{label: string, id: string}`. Out of scope for v3.0. Tier 3. | MB-T07 green |
| `MB-F-MB-T07-DISMISSED-CARD-RETENTION` | After Decline + audit-write confirmation, `is_dismissed` shows the card faded with a "Declined" badge. Parent state owns garbage-collection. Today there's no GC: dismissed cards live forever in the `dismissed` bucket of the card-state reducer. Add a TTL (e.g. clear after 30s) or operator-controlled clear ("Hide declined") so dismissed cards don't accumulate over a long session. Tier 3. | MB-T07 green |

| ID | Scope | Origin |
|---|---|---|
| `DAEMON-F-backpressure-integration` | Real slow-client browser smoke for T12's WS backpressure (force-disconnect at 1009); current coverage is unit-only | D-4 / T12 |
| `DAEMON-F-git-recursive-branches` | Support slash-bearing branch names (`feature/foo`); requires recursive `fs.watch` (S02 flagged macOS-fragile) | D-4 / T14 |
| `DAEMON-F-watcher-fsevents-smoke` | Real `fs.watch` integration smoke test (T13/T14/T15 currently mock-factory only) | D-4 |
| `DAEMON-F-watcher-real-git-smoke` | Real `git init` + commit roundtrip integration test for T14 git watcher | D-4 / T14 |
| `DAEMON-F-watcher-status-real-fs-smoke` | Real `STATUS.json` write + watch integration test for T15 | D-4 / T15 |
| `DAEMON-F-status-json-configurable-path` | Per-session configurable STATUS.json location (currently `<cwd>/STATUS.json`) | D-4 / T15 |
| `DAEMON-F-test-contention` | Investigated only if recurs: T14 P2 git-watcher debounce flaked once under cross-package pnpm recursive contention | D-4 / T14 |
| `DAEMON-F-health-response-schema-consumption` | Session B's UI consumption of `notifications_available` flag from `/v2/health` (Session B territory) | cross-session |
| `CLI-F-real-daemon-state-isolation` | Z-4 spawnRealDaemon writes to `~/.foxworks-dispatch/*` (operator state); post-MVP support test-isolated paths via env-var override or daemon CLI args | Z-4 |
| `CLI-F-send-pull-real-env-smoke` | Z-4 deferred fd send + fd pull from smoke coverage (need tmux pane + HANDOFF.md fixture) | Z-4 |
| `UI-F-color-audit` | Cross-component color usage audit (Session B authoring decision) | W-* |

---

## Numbering convention

Followups carry a domain prefix matching their package owner:

- `DAEMON-F-*` — `packages/dispatch-daemon/`
- `CLI-F-*` — `packages/dispatch-cli/`
- `WEB-F-*` — `packages/dispatch-web/`
- `UI-F-*` — Session B's UI surface broadly (web + menubar)
- `MB-F-*` — `packages/dispatch-workstation/` (v3.0 Workstation surface)

Origin column references the cluster ID (`D-1` through `D-6`, `W-1`
through `W-5`, `CLI-T`, `Z-*`) plus the originating ticket where
unambiguous. Commit bodies in the originating commits carry full
context; this index is a scan target, not a substitute.
| `MB-F-COARCH-T04-BUILD-DOC-SETTINGS-UI` | Build-doc config (repoRoot / relativePath / allowedScopes) is persisted via `setBuildDocConfig` IPC but there is no renderer UI to set it. Operator must call the bridge directly from DevTools or a future settings panel. Wire into MB-T11 (broader settings page) when that ticket lands — expose a "Build doc path" section with repo picker + relative path input + allowed-scopes textarea. | COARCH-T04 green |
| `MB-F-COARCH-T04-DAEMON-STATE-TIER3` | Context builder Tier 3 (daemon state — active RUNNING/IDLE sessions) is passed as `null` in coarchitect-ipc.ts pending a daemon sessions list endpoint. When the daemon exposes `/v3/sessions` or equivalent, fetch active sessions and populate `daemonState` in the `buildContext()` call. Contract: `DaemonStatePayload = { sessions: Array<{id, name?, status?}> }`. | COARCH-T04 green |
| `MB-F-COARCH-T04-CONTEXT-HALT` | **ARBITRATED 2026-05-02 by operator: full-doc injection per build-doc-schema-spec.md.** Original ambiguity: build-doc-schema-spec.md says full-doc; V3_TICKETS.md Tier C wording said "relevant ticket section only." Operator chose full-doc — context-builder.ts no longer has open question; comment in source should be updated by next coarchitect-touching ticket.  → CLOSED | COARCH-T04 green |
| `MB-F-COARCH-T02-STYLING` | Chat panel renders functional but unstyled — D shipped UI scaffold only per COARCH-T02 contract scope; needs production-quality CSS matching modern LLM chat aesthetic (Claude/GPT/Gemini-style message bubbles, role-based layout, input affordances). Operator-arbitrated new ticket required (visual reference + frozen exports + acceptance criteria with operator manual gate).  → PARTIALLY CLOSED at MB-T20 WB4 (Q-MBT20-6=b): tab-host shell chrome (chat-shell-root + chat-shell-tab-strip + chat-shell-tab-chat + chat-shell-tab-content slot) lands as the outer panel container. Inner ChatPanel content (message bubbles + input affordances) DEFERRED to MB-T21 (Chat tab proper). v3.1 polish followup `MB-F-T20-CHAT-TAB-CONTENT-STYLING` filed at WB5 to track inner styling specifically. | Round 2 Zipper-2 manual gate |
| `MB-F-DISPATCH-WEB-AUTH-PERSISTENCE` | Workstation Electron BrowserWindow session shows dispatch-web "Conductor authentication" screen on every fresh launch; Electron session has no relationship to user's regular browser auth state. Architectural decision needed: (a) IPC token-injection at workstation launch from `~/.foxworks-dispatch/token`, (b) auth-bypass for workstation context, (c) accept as workstation onboarding step. Link to MB-T08 (production-mode webview) territory. | Round 2 Zipper-2 manual gate |
| `MB-F-COARCH-T02-DEFAULT-LAYOUT` | Default `chat-region` height of 280px in workstation-shell.html may not be optimal default; revisit when styling lands and visual proportions become design-decision-able.  → DEFERRED at MB-T20 WB4 (Q-MBT20-7=a): tab strip adds ~35px chrome above ChatPanel content; 280px default kept unchanged pending visual review with real Chat tab styling (MB-T21 territory). v3.1 polish followup `MB-F-T20-CHAT-REGION-DEFAULT-HEIGHT` filed at WB5 for the specific revised number. | Round 2 Zipper-2 manual gate |
| `MB-F-T20-CHAT-TAB-CONTENT-STYLING` | Inner ChatPanel content (history list message bubbles, deliberating/thinking indicators, input form affordance, error alert styling) renders functional but unstyled inside MB-T20's tab-host shell. MB-T20 only ships outer panel chrome (chat-shell-root + tab-strip + tab-content slot). Closes by replacing/refactoring `coarchitect/chat-panel.tsx` styling at MB-T21 (Chat tab proper) per Family-B sequence. Tier 2 — paired with MB-T21 charter. Discoverability: `docs/coordination/mb-t20-findings-2026-05-07.md` §VII.  → **CLOSED at MB-T21 WB5** (commits `536d413` WB2 ChatBubble + `fd6b9c5` WB3 QuickPickButtons + `f2b7ccd` WB4 SpawnedList): chat-panel.tsx refactored in place per Q-MBT21-1=a; sub-components ChatBubble + QuickPickButtons + SpawnedList + chat-content-markers.ts extracted into coarchitect/. Letter-glyph avatars O/C/S (Q-MBT21-9=a), role-aligned layout (user right, conductor left), tail-anchor scroll-pin (Q-MBT21-8=a), inline styles (Q-MBT21-12=a). chat-input + send-button testids preserved (probe-03 5/5 green every WB). Quick-pick + spawned: rendered inline via sentinel-marker parse (Q-MBT21-2=b + Q-MBT21-3=b — renderer-only; schema upgrade to first-class fields tracked at MB-F-T21-QUICK-PICK-SCHEMA-FIRST-CLASS + MB-F-T21-SPAWNED-LIST-SCHEMA-FIRST-CLASS). Quick-pick clicks roundtrip via existing sendAndStream (Q-MBT21-4=a; preload.mts unchanged per Q-MBT20-5). Discoverability: `docs/coordination/mb-t21-findings-2026-05-07.md` + `docs/coordination/mb-t21-decisions-2026-05-07.md`. | MB-T20 WB5 |
| `MB-F-T20-CHAT-REGION-DEFAULT-HEIGHT` | Default `chat-region` height of 280px in workstation-shell.html may now be visually tight given MB-T20 added ~35px tab-strip chrome above ChatPanel. Specific revised number deferred to operator-arbitrated visual review post-MB-T21 styling. Closes by adjusting workstation-shell.html line 251 inline-style `height: 280px` after visual review. Tier 3 — cosmetic; current 280px still functional. Discoverability: previously tracked at `MB-F-COARCH-T02-DEFAULT-LAYOUT` (deferred-closed at MB-T20 WB4). | MB-T20 WB5 |
| `MB-F-T20-FAMILY-B-ADDITIONAL-TABS` | **CLOSED by MB-T22 WB2.** MB-T20 ships single Chat tab with aria-selected hard-coded "true". MB-T22 (Commits tab), MB-T23 (Tasks tab), MB-T24..T27 (toggles + meters) add additional tabs to the Family-B shell. ChatShell needs multi-tab state machine (active-tab tracking + tab-switch handler + per-tab content slots) before MB-T22 can wire its tab body. Closes by extending `src/chat-shell/chat-shell.tsx` to accept `tabs: TabConfig[]` + `activeTabId` + `onTabChange` props. Tier 2 — Family-B prerequisite for MB-T22+. Discoverability: `docs/coordination/mb-t20-findings-2026-05-07.md` §VII. → **CLOSED** at MB-T22 WB2 (Q-MBT22-3=a, decisions doc 2026-05-07): `chat-shell.tsx` extended to multi-tab API (`tabs: readonly TabConfig[]` + `activeTabId?: string` + `onTabChange?: (id: string) => void`); hybrid controlled/uncontrolled state via internal `useState(() => tabs[0]?.id)`; `mount.ts` migrated to `tabs` resolution (explicit `tabs` override → bridge wrap → `defaultChatTabStub` fallback); existing MB-T20 probe-01 + probe-02 migrated to new API (data-testid contract preserved verbatim — `chat-shell-root`, `chat-shell-tab-strip`, `chat-shell-tab-{id}`, `chat-shell-tab-content`); probe-04-multi-tab-api (RED at MB-T22 WB1) flipped GREEN (9/9 pass). A's MB-T21 quick-pick integration test continues to pass without modification (`mountChatShell({rootElementId,bridge})` signature preserved). Sentinel zone `=== BEGIN: MB-T22 header-bar extension point (MB-T26/MB-T27 territory) ===` reserved ABOVE the tab strip per cross-session coordination 2026-05-07 — C (MB-T26 cost meter) + D (MB-T27 model mix) additively land their own header-bar slot props inside their own sentinel zones at their respective WBs. **PARTIAL-SUPERSEDE at MB-T26 WB4 (Q-MBT26-9=a, 2026-05-07):** the original body line *"MB-T24..T27 (toggles + meters) add additional **tabs**"* is partially incorrect. **MB-T22-T23 are tabs** (multi-tab state machine landed at MB-T22 WB2, this followup's primary closure). **MB-T24..T27 are header-bar slots ABOVE the tab-strip**, NOT tabs (Q-MBT26-1=a header-bar slot model operator-confirmed 2026-05-07; Terminal C's MB-T26 lands `chat-shell-header-bar` element + `renderCostMeter?` slot prop inside Terminal B's reserved MB-T22 extension-point zone at `09b38ce`; Terminal D's MB-T27 `renderModelMix?` slot follows in the same header-bar element via own non-overlapping sentinel zone). | MB-T20 WB5 → CLOSED MB-T22 WB2 → PARTIAL-SUPERSEDE MB-T26 WB4 |
| `MB-F-T20-CHAT-SHELL-MOUNTED-SENTINEL-FORWARDING` | `CHAT_SHELL_MOUNTED` sentinel emitted from `src/chat-shell/mount.ts:96` is not currently forwarded by main.ts test-hooks console-message handler (~line 151-154 allowlist). RENDER_OK serves the load-bearing smoke purpose so this is cosmetic; v3.1 polish if MB-T20-specific smoke wants to assert chat-shell-mounted-before-ChatPanel-mounts ordering. Closes by appending `msg === 'CHAT_SHELL_MOUNTED'` to the existing allowlist in main.ts at the next main.ts-touching ticket. Tier 3 — cosmetic. Discoverability: `docs/coordination/mb-t20-findings-2026-05-07.md` §V. | MB-T20 WB5 |
| `MB-F-T20-LOCAL-HEAD-DIVERGENCE-MECHANISM` | During MB-T20 WB3→WB4 transition in shared-working-tree parallel-cairn context, local HEAD diverged from origin (origin had `a68cd0b`, local had `ecdd0e4`) without an obvious mechanism. Recovered via stash + rebase + manual mount.ts conflict resolution; root cause unidentified. If this recurs in future parallel-cairn sessions, file specific reproduction steps (env state, prior commands, hook interactions) to make diagnose-able. Tier 3 — methodology hygiene. Discoverability: `docs/coordination/mb-t20-findings-2026-05-07.md` §IV. | MB-T20 WB5 |
| `MB-F-COARCH-T04-COST-REVALIDATION` | Operator arbitration on MB-F-COARCH-T04-CONTEXT-HALT (2026-05-02) selected full-doc injection over ticket-section-only. MB-S01 ADR cost projections were measured against ticket-section-sized contexts; full-doc context is materially larger per call. Re-validate cost projections under full-doc conditions before v3.0 ship-gate. Block: take a representative build doc (e.g., V3_TICKETS.md), run through the actual context-builder pipeline, measure input token count per Sonnet call, re-project at 100/200/500 calls/day. Compare against MB-S01 ADR baseline. If material delta, surface to operator for v3.x cost mitigation decision. | COARCH-T04 a2424f8 |
| `MB-F-CONSOLE-T02-RECONNECT-BACKOFF` | Cluster 2 GREEN ships a fixed 1-second reconnect delay with infinite retries on non-terminal close codes. Production-grade behavior should be exponential backoff (e.g., 1s/2s/4s/8s capped at 30s) with a max-attempts ceiling that surfaces a terminal `console:error` after N failed reconnects. The fixed-1s loop is fine in dev; under daemon outages it would generate a steady connection storm. Tier 2 — land before v3.0 ship-gate so operator-experience under daemon-down scenarios is acceptable. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-PANEL-CAP-SETTINGS` | Vision §10.11 Q3 ratifies cap=4 in v3.0 and notes "configurable in settings". CONSOLE-T02 ships DEFAULT_PANEL_CAP=4 with `panelCap` constructor option as the test seam; settings UI integration lands with MB-T11/W-T19. When that ticket lands, expose a "Max simultaneous CC consoles" numeric input that persists to a shared settings store and threads through to ConsoleIpcController construction. Tier 2. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-V3-SCHEMA-INTEGRATION` | WORKSTATION_CONTRACT.md §7 Schema enforcement says IPC message types are part of the v3 Zod schema in `dispatch-core/src/v3/schema.ts`. CONSOLE-T02 defines the seven console:* payload shapes locally in console-bridge.ts (ConsoleBridge interface) and console-ipc.ts (controller methods). Promote these into dispatch-core/src/v3/schema.ts so message-bus boundaries can validate per WORKSTATION_CONTRACT §7.3. Tier 2 — land before v3.0 ship-gate so the IPC layer matches the schema-enforcement invariant. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-INTEGRATION-TEST` | All CONSOLE-T02 tests are unit-only: pure DI, no Electron spawn. Add a spawn-electron integration test pattern (per coarch-t03 streaming-renders.spec.ts) that exercises the full window.consoleBridge → ipcMain → mock-daemon round-trip. Mock daemon can be a vitest setup that runs a small ws + http server. Lands as part of CONSOLE-T03 (which adds the renderer-side panel mount that the integration test needs to drive) OR as a dedicated post-CONSOLE-T03 spec file. Tier 2 — needed before v3.0 ship-gate so the integration boundary is validated at runtime, not just at the unit-test mock boundary. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T03-MENU-SUBSCRIPTION` | **PARTIALLY CLOSED by Fix-C (cairn finding #82 PARTIAL-RESOLVED at 320f707).** Subscribe wiring shipped: `subscribeConsoleMenuToDaemon` in `console-mount.ts` ships the operator-arbitrated hybrid pattern (bootstrap GET /v2/sessions + WS /v2/events/stream as "something changed" trigger + 150ms debounce). All 6 helper unit tests + live-daemon integration GREEN (`refreshMenu(["newTest1"])` correctly invoked). Visual closure NOT achieved — finding #89 (`MB-F-WORKSTATION-MENU-REBUILD-NO-OP`) discovered during Fix-C integration verification: `Menu.setApplicationMenu` post-ready does not propagate to the macOS menu bar regardless of subsequent `rebuildApplicationMenu` calls. Full closure of this followup gated on resolution of #89. Original triage retained for context: The CC Console native menu is rebuilt on demand via `refreshConsoleMenu(sessions)` in main.ts but the daemon-session-list subscription (so the menu auto-refreshes when /v2/sessions changes) is unwired. Initial app start hands `[]` so the menu shows the disabled "No sessions registered" placeholder until something calls `refreshConsoleMenu(['s1', 's2'])`. Wiring options: (a) periodic GET /v2/sessions poll (simple, latency-bounded), (b) /v2/events/stream subscription (push-based, lower-latency). Recommend (b) since the daemon already streams events; this matches the kanban refresh pattern. Tier 2 — land before v3.0 ship-gate so the operator-driven menu open path actually surfaces sessions. | CONSOLE-T03 green → PARTIALLY CLOSED Fix-C `320f707` (gated on cairn #89) |
| `MB-F-CONSOLE-T03-MULTI-PANEL` | **CLOSED by MB-T12 WB4 (2026-05-07).** CONSOLE-T02 ConsoleIpcController tracks up to `panelCap` panels (default 4 per vision §10.11 Q3) and forwards events independently per session. The CONSOLE-T03 React surface was originally single-panel: state held one `sessionName`, opening a second session rebound (overwrote) the first in the visible UI. Closure approach (per Q-MBT12-6=a operator-arbitrated): `ConsolePanel` now takes a required `targetSessionName: string` prop set by its tile-grid parent (one ConsolePanel per tile). Bridge listeners filter on `p.sessionName === targetSessionName` for all 5 channels (open/close/stdout-chunk/gap/error). State shape changed from `{sessionName, gap, error}` to `{open, gap, error}` where `open: boolean` tracks bound/unbound for THIS targetSessionName. The single-panel auto-mount in `console-panel/mount.ts` was deprecated (the tile-grid React tree from MB-T12 WB6 calls `mountConsolePanel` programmatically per tile). 7 new multi-mount tests (probe-01-three-panels-no-crosstalk.spec.tsx) verify: idle states, single-bind, multi-bind without unbind, stdout fan-out filter, gap fan-out filter, listener count = 5×N, close-unbinds-only-target. Vision §10.10 ship-gate ("Two simultaneous CC-console panels bound to different sessions both render correctly with no cross-talk") — VERIFIED. The original "tab strip / Map<sessionName, PerPanelState>" approach was rejected in favor of the simpler "one ConsolePanel React instance per tile" model that aligns with §3.3 spec. Tier 2 (CLOSED). | MB-T12 WB4 (2026-05-07) |
| `MB-F-CONSOLE-T03-SHELL-INTEGRATION` | dist/console-panel/ produces a standalone renderer (console-panel.html + renderer.js + xterm.css). The running Workstation shell (workstation-shell.html) does NOT yet load this renderer — clicking "CC Console > [session]" in the native menu calls `controller.openConsolePanel(name)` which fires the shell→webview `console:open` event, but the shell HTML has no `#console-root` and no `<script src=…/console-panel/renderer.js>`. The panel therefore renders nowhere visible in v3.0 ship without this last-mile wiring. UX placement is an operator decision: (a) embed alongside chat panel as a third region in the splitter, (b) replace chat panel via tab strip, (c) separate Electron BrowserWindow opened on demand. Recommend (a) for v3.0 simplicity. Tier 1 — blocks the v3.0 §10.10 ship-gate ("operator opens CC-console panel for a registered session"). | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-XTERM-BUNDLE-SIZE` | dist/console-panel/renderer.js is 1.5 mb because esbuild bundles @xterm/xterm + react + react-dom + console-bridge types inline. Acceptable for v3.0 (Workstation is desktop-installed, not over-the-network), but a future optimization could (a) externalize react/react-dom and serve from a shared chunk, (b) tree-shake xterm to drop unused addons, (c) use xterm's plain-text mode if ANSI fidelity is not required. Tier 3 — non-blocking; revisit during a packaging pass. | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-OPEN-TRIGGER-CARD-BUTTON` | Vision §10 implies the operator may want to open a console panel from a kanban session card too (not just the native menu). v3.0 ships menu-only. Adding a per-card "Open console" button in dispatch-web requires (a) dispatch-web UI changes, (b) a webview→shell IPC channel for "request console open" since dispatch-web runs in the embedded webview not the shell renderer. Tier 3 — operator can already open via the native menu; the per-card button is a UX nicety. | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-DAEMON-RESTART-RECONNECT` | Vision §10.10 ship-gate: "Daemon restart mid-stream cleanly reconnects WebSocket and backfills missed STDOUT from daemon ring buffer with no operator-visible gap beyond reconnection latency." CONSOLE-T02 cluster 2 ships fixed-1s reconnect on non-terminal close codes; the renderer panel does NOT explicitly visualize the reconnect-pending state. Add a transient "Reconnecting…" banner (similar to the gap-warning banner) that surfaces when the bridge fires a console:error and clears when the next console:stdout-chunk arrives. Tier 2 — needed for §10.10 ship-gate compliance. | CONSOLE-T03 green |
| `MB-F-MB-T06-CAP-DEFAULT-CONFIRM` | MB-T06 ships DEFAULT_SESSION_CAP=5 in `packages/dispatch-workstation/src/main/session-cap.ts` per ticket-prompt fallback. Vision §10.11 Q3 ratifies a 4-5 sustained-active range; MB-T06 selected 5 without operator arbitration. Confirm the 5 default (or override to 4) before v3.0 ship. Tier 3 — both 4 and 5 are within the ratified range; this is a default-policy confirmation, not a behavior bug. | MB-T06 green |
| `MB-F-MB-T06-CAP-SETTINGS` | Concurrent-session cap is hardcoded to DEFAULT_SESSION_CAP=5 with `sessionCap` constructor option as the test seam. Settings-UI integration lands with MB-T11/W-T19. When that ticket lands, expose a "Max concurrent sessions" numeric input that persists to a shared settings store (electron-store per WORKSTATION_CONTRACT.md §8.1) and threads through into `defaultSpawnHandlerDeps` construction in spawn-ipc.ts. Tier 2 — operator-configurability is part of the §8.1 settings surface. | MB-T06 green |
| `MB-F-MB-T06-CAP-STATE-FILTER-PRECISION` | `isActiveSession` in session-cap.ts filters out `state === 'archived'` and `state === 'killed'` only. V3_TICKETS.md MB-T06 specifies "RUNNING + IDLE count toward cap; AWAITING REVIEW, STALE, KILLED, archived do not" — strictly, AWAITING_REVIEW + STALE should also be filtered. The current first-pass filter therefore over-counts (counts AWAITING_REVIEW and STALE sessions). Refinement requires the daemon's `state` ↔ `computed_status` mapping to be exposed cleanly via `/v2/sessions`; current response carries both fields. Land before v3.0 ship-gate so the cap behavior matches V3_TICKETS.md spec. Tier 2. | MB-T06 green |
| `MB-F-MB-T06-OVERRIDE-FLOW` | V3_TICKETS.md MB-T06 also specifies "override flow with confirmation per vision §3.6" so the operator can spawn past cap. MB-T06 ships HARD-CAP only (SessionCapExceededError surfaces via IPC envelope to the renderer; no override path). Renderer-side modal that includes "[Spawn anyway]" button + confirmation dialog is workstation-UI work, scope-fenced from this ticket. Add an override path: either (a) operator clicks override in the modal → renderer emits a separate `workstation:spawn-requested-override` IPC with a flag; or (b) the cap-exceeded reply includes an "override token" the renderer surfaces back. Tier 2 — without override, at-cap operators cannot spawn even when intentional. | MB-T06 green |
| `MB-F-MB-T06-RENDERER-MODAL-UX` | MB-T06 wires the cap-error envelope (error_type=SessionCapExceeded, activeCount, cap, sessionName) but does NOT ship renderer-side display. Spawn modal (MB-T04 surface) currently treats all error replies generically. Add a SessionCapExceeded-specific branch that renders "Session cap (N) reached. M sessions active. Close one before spawning another." with a list of active session names + close-one affordance. Tier 2 — needed for V3_TICKETS.md MB-T06 acceptance ("at-cap state behaves per spec"). | MB-T06 green |
| `MB-F-MB-T06-HEADER-COUNT-DISPLAY` | V3_TICKETS.md MB-T06 also says "Header surfaces current session count, spawn button disables at cap with hover text." MB-T06 ships server-side cap enforcement; the dashboard header counter + spawn-button disable-at-cap state is workstation-shell UI work, not in this ticket's scope. Wire a periodic GET /v2/sessions poll (or /v2/events/stream subscription per CONSOLE-T03 menu pattern) to drive a header count display + button-enable predicate. Tier 2. | MB-T06 green |
| `MB-F-MB-T05-PATH-ALLOWLIST-CLAUDE-RESOLUTION` | **CLOSED by batch-6 Session A (cairn finding #72).** Dogfooded 2026-05-03: spawn-from-workstation appeared to succeed (modal closed, no error toast) but tmux session never started; daemon record orphaned. Root cause: `spawn-env.ts` `ALLOWLIST_PATH` excludes `~/.local/bin` (Anthropic official-installer location); tmux exited 0 then claude failed to exec. Permanent fix lands `packages/dispatch-workstation/src/main/binary-resolver.ts` with `resolveClaudeBin()`; `defaultSpawnHandlerDeps` resolves the absolute claude path at workstation startup; tmux argv now passes the absolute path verbatim, bypassing PATH lookup. Unresolved-bin guard in `spawnSession` surfaces SpawnFailed up-front when resolution fails. Tier 1 ship-gate blocker; KNOWN-validated for operator's specific environment, MODELED for other install paths (path-agnostic fix). | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-MB-T05-POST-SPAWN-LIVENESS-CHECK` | **CLOSED by batch-6 Session A (cairn finding #73).** Defense-in-depth companion to #72. Architectural assumption being repaired: tmux's `new-session -d` exit-0 means session-created, NOT program-running. Without a post-spawn check, any future failure mode where claude dies on exec (corrupt binary, license refused, immediate crash) would leave an orphaned daemon record. Permanent fix: between `runTmuxNewSession` and daemon `registerSession`, sleep `livenessCheckDelayMs` (default 500ms in production, 0ms in unit tests) then run `tmux has-session -t <name>`; on rejection throw SpawnFailed and skip daemon registration. New `runTmuxHasSession` dep + `livenessCheckDelayMs` knob threaded through `SpawnHandlerDeps`. Tier 1 defense-in-depth; 500ms heuristic flagged for measurement-driven tuning in a future ticket. | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-MB-T05-SPIKE-ENVIRONMENT-VALIDATION` | **CLOSED by batch-6 Session A (cairn finding #74).** Methodology amendment only; no code. MB-S02 spike (basis for `spawn-env.ts` ALLOWLIST_PATH) was run on a Homebrew-only system and baked the assumption "claude lives at /opt/homebrew/bin" into production code without enumerating the install-method variation space. Lesson: spike evidence about operator-installed binaries needs validation across documented install methods, not the spike-runner's environment alone. Binding decision (filed at `docs/adr/MB-S02-spike-environment-validation-amendment.md`): future spikes touching operator-environment-dependent assumptions MUST enumerate the install/configuration variation space and explicitly mark un-validated variations as SPECULATIVE in the ADR. Binds future spikes; does NOT retroactively re-open MB-S02. Tier 2 methodology. | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-PARALLEL-CAIRN-SCHEMA-FILE-MERGE-CONFLICT` | When two parallel-cairn branches both add schemas to the same file but cut from different parents (one branch cuts from the common ancestor before the other has merged), the second-merging branch hits a content conflict. The original parallel-cairn coordination prevented sess-mbt10 WB1 from committing schema before sess-mbt09 WB1 (RULE 1), but did not specify rebase-before-merge for the second-merging branch. Today's MB-T10 merge to main hit this conflict; resolved via rebase-then-merge with content-additive resolution. Add to parallel-cairn coordination prompt template: "Second-merging schema-file branch must rebase onto post-first-merge main BEFORE attempting its own merge to main." Discoverability: parallel-cairn coordination doc. Tier 1. | sess-mbt10 merge |
| `MB-F-CC-STREAM-IDLE-TIMEOUT-MID-COMMIT` | Long CC sessions can timeout mid-execution between file-write and commit. Per-commit-push discipline is the load-bearing safety net — because each WB ends with commit+push, a stream timeout interrupting between WB4 close and WB5 commit leaves zero ambiguity (file on disk, branch HEAD = WB4, no half-state). Today's MB-T10 WB5 was recovered cleanly via verify-existing-file path (read the orphan file, validate against probe expectations, integrate, commit). Recovery prompt template should default to verify-existing rather than regenerate when a recovered uncommitted artifact exists. Discoverability: recovery prompt template + chat-layer SOP. Tier 1. | sess-mbt10 WB5 recovery |
| `MB-F-INTEGRATION-TEST-ELECTRON-PROCESS-LEAK` | Workstation integration tests spawn Electron processes and don't clean up reliably. Multiple incidents this session (87 zombies, 30+ zombies, 10 zombies in separate clusters). Tests need afterEach/afterAll cleanup that explicitly kills spawned Electron processes. Currently mitigated via manual pkill -9 -f "[Ee]lectron" between phases. Discoverability: test runner ergonomics; future Workstation test infra ticket. Tier 1. | sess-mbt09+10 test-infra |
| `MB-F-WORKSTATION-KANBAN-VIEWPORT-BLANK` | Workstation main viewport renders only event-log strip + filter controls; kanban board panel below event strip is blank black area despite scrollbar thumb-to-track ratio (~1/4) suggesting content exists. Captured 2026-05-06 12:07pm during MB-T10 development. Possibly caused by stale dispatch-workstation dist (running May 5 build against post-MB-T07-T08 source) — should fix with rebuild after MB-T09 + MB-T10 ship. If kanban still blank after Phase 2 dist rebuild + Workstation relaunch, this becomes a real rendering regression worth triaging before v3.0 ship-gate. Affects v3.0 §8 ship-gate criterion. MB-T12 (auto-mount + tiling) supersedes the kanban panel rendering if it lands first. Discoverability: post-Tier-E ship-gate triage. Tier 1. → **RE-DIAGNOSED 2026-05-06 post-Phase-2 rebuild + Option-A fix attempt:** original "stale dist" premise was a partial truth (dist WAS stale, AND the worktree it was originally launched from had been removed from disk leaving the running Electron unable to lazy-load any resources), but a fresh rebuild of main HEAD revealed a more severe latent bug — Workstation could not launch at all due to MB-F-MB-T09-WORKSTATION-RUNTIME-IMPORT-BUG-SHIPPED (ERR_MODULE_NOT_FOUND on `dispatch-core/src/v3/schema.js`; helpers spawned then orphaned; 0 renderer processes; no window opened). Fix landed at 01dfd54; Workstation now launches cleanly (WINDOW_READY sentinel + 5-process tree with 2 renderer helpers). Status: **RESOLVED** — startup-crash path closed (01dfd54); kanban panel renders correctly (operator visual verification 2026-05-06 against post-fix dist). | sess-mbt10 dev observation 2026-05-06 → RESOLVED 01dfd54 (visual verified) |
| `MB-F-WORKTREE-FRESH-MISSING-DIST-CRASH` | Fresh worktrees created via git worktree add lack dist/ artifacts. Workstation tests that rely on dispatch-core/dist/v3/schema.js (resolved through pnpm workspace symlinks) fail at import-time on fresh worktrees until dispatch-core is built locally. Currently mitigated by spawning CC in worktrees that have dispatch-core dist/ pre-built. Future fix: pnpm pre-test hook that runs dispatch-core build if dist/ is absent or older than src/. Discoverability: worktree-spawn ergonomics; dev-onboarding doc. Tier 1. | sess-mbt09+10 worktree ergonomics |
| `MB-F-PRE-WB6-COST-ESTIMATE-VS-SCHEMA-MAXIMA-DIVERGENCE` | CONDUCTOR_V3_RESCOPE.md §3.5 line 136 quoted "~24KB max" before WB6 measurement. The 24KB number was a typical-content estimate that did NOT account for schema-permitted maxima (4KB recent_handoff per Q-MBT10-6=a, schema-uncapped recent_console_tail, populated pending_intents). At schema maxima the measured payload was 54.40 KB at N=8 sessions — over 2x the original estimate. Lesson: rescope-doc cost estimates that touch tiers capped at schema maxima must back-calculate from schema field caps + arithmetic, not from typical-content modeling. Future cost-validation sections should cite "≈ N × max-field-bytes" alongside typical estimates. Resolved in MB-T10 WB6 via HALT 2 Option A: rescope ceiling raised from 30KB to 60KB to match measured reality. Discoverability: rescope authoring SOP; build-doc review checklist. Tier 1. | sess-mbt10 WB6 |
| `MB-F-PNPM-INSTALL-FROZEN-LOCKFILE-DISCIPLINE` | CC needs to use pnpm install --frozen-lockfile in CI/build contexts to prevent silent lockfile mutation. Today's session had at least one incident where a CC ran pnpm install (without flag) which silently updated the lockfile. Future CC prompts should specify --frozen-lockfile when build-time install is required. Discoverability: CC prompt template + CI script ergonomics. Tier 2. | sess-mbt09+10 chat-layer |
| `MB-F-INTEGRATION-TEST-REGISTRY-LEAK` | Test fixtures (probe-mb-t05-card-*, probe-94-03-ask-*, probe-83-04-*, etc.) leave entries in the daemon registry after test runs complete. Visible 3+ hours after test execution as state_changed events in the Workstation event log. Same root-cause class as MB-F-INTEGRATION-TEST-ELECTRON-PROCESS-LEAK. Tests need afterEach/afterAll cleanup that removes the registry entries they create. Tier 2 — does not block ship but causes operator-facing UI noise. Discoverability: test runner ergonomics; test infra ticket. | sess-mbt09+10 test-infra |
| `MB-F-T10-COARCHITECT-IPC-WIRE-TIER4` | **CLOSED by sess-mbt11 WB7.** packages/dispatch-workstation/src/main/coarchitect-ipc.ts:137 originally passed spawnedSessions: null. Wiring closure shipped per Q-MBT11-7=a + Q-MBT11-8=a: (a) NEW packages/dispatch-workstation/src/main/tier4-fan-out.ts hosts the daemon HTTP fan-out helper buildTier4Payload that calls GET /v3/sessions/:name/context-snapshot per registered non-killed session AND merges workstation autopilot state (pending_intents + last_action_fired_at) into each SessionContextSnapshot per Q-MBT11-8=a (daemon endpoint stays returning hardcoded []/null per context-snapshot.ts:167; workstation overwrites authoritatively); (b) session-name source = HttpSessionListClient.listSessions filtered by !archived && !killed; (c) Promise.all fan-out via existing tier4-builder.assembleTier4Payload; (d) coarchitect-ipc.ts:141 replaced `spawnedSessions: null` with `await buildTier4Payload({ sessionListClient, autopilot, fetchImpl, daemonUrl, daemonToken }).catch(() => null)`. Originally Tier 2; closure validated via WB7 unit tests + runtime-relaunch smoke. | sess-mbt10 WB5 → CLOSED sess-mbt11 WB7 |
| `MB-F-MB-T09-WORKSTATION-RUNTIME-IMPORT-BUG-SHIPPED` | **CLOSED by 01dfd54.** MB-T09 (sess-mbt09) shipped 3 runtime-blocking value imports + 2 type-only same-pattern imports in workstation main process using `dispatch-core/src/...js` paths. TypeScript path-mapping resolved through workspace src-resolution; Node ESM at runtime requires actual on-disk `dispatch-core/dist/...js` artifact (only `.ts` source exists at `src/`). Bug passed pre-merge typecheck + unit tests + integration tests; surfaced only at workstation runtime relaunch from fresh main HEAD during 2026-05-06 post-cleanup verification. Crash mode: `ERR_MODULE_NOT_FOUND` on first lazy-resolved import; Electron spawned helper processes (GPU + network) then crashed before opening any renderer process; no window. Affected files: `session-send-prompt-ipc.ts` (3 value + 1 type-only), `card-ipc.ts` (1 type-only multi-import block), `http-daemon-client.ts` (1 type-only). Fix at 01dfd54: all 5 imports patched to `dispatch-core/dist/...js`; rebuild + relaunch confirmed `WINDOW_READY` sentinel + 5-process tree (main + GPU + network + 2 renderers). Was a v3.0 ship-gate blocker — workstation would not start at all from main HEAD between sess-mbt09 merge (7a9e564) and the fix (01dfd54). Tier 1. Discoverability: `git grep "dispatch-core/src/" packages/dispatch-workstation/src/`. | sess-mbt10 cleanup relaunch → CLOSED 01dfd54 |
| `MB-F-WORKSTATION-RUNTIME-RELAUNCH-AS-MERGE-GATE` | MB-T09 ran a full WB ladder (typecheck + unit tests + integration tests + spike validation + finding doc) and merged cleanly to main; the runtime ESM-import bug above was invisible to all of those gates because TypeScript accepts `dispatch-core/src/...js` via workspace path-mapping. Lesson: workstation merges that touch `src/main/*.ts` must include a runtime-launch smoke step (`pnpm --filter dispatch-workstation exec electron dist/main/main.js` → observe `WINDOW_READY` sentinel within N seconds) as part of the merge gate, not a post-merge dogfood. Future cairn-coord prompt template should add: "Workstation merges that touch main-process src files MUST include a runtime-launch smoke verification step before merge to main; ERR_MODULE_NOT_FOUND class bugs are invisible to typecheck + unit + integration suites." Tier 1 — methodology fix prevents recurrence of the MB-T09-class import-pattern bug + similar runtime-only failure modes. Discoverability: parallel-cairn coordination doc + chat-layer SOP. | sess-mbt10 cleanup relaunch |
| `MB-F-DISPATCH-CORE-DUAL-IMPORT-PATTERN-DRIFT` | Codebase had two import patterns coexisting against dispatch-core: (a) `dispatch-core/dist/v3/schema.js` (correct — points at the actual built artifact, runtime-resolvable via pnpm workspace symlink) used by sess-mbt10 territory + most pre-existing code; (b) `dispatch-core/src/v3/schema.js` (broken at runtime — TypeScript accepts via path-mapping, Node ESM rejects since no `.js` exists at `src/`) used by sess-mbt09 territory. After 01dfd54 all 5 known broken-pattern imports are patched and the codebase is consistent (all dispatch-core imports use `dist/`). Future hardening: add an ESLint rule (or a tsconfig path restriction) that bans `from 'dispatch-core/src/...'` patterns in workstation source to prevent future drift. Tier 2 — immediate bug closed by 01dfd54; this entry tracks the optional tooling-side hardening to prevent future regression. Discoverability: ESLint config + tsconfig.json review pass. | sess-mbt10 cleanup relaunch |
| `MB-F-T11-WB7-ORCHESTRATOR-SPAWN-DEFERRED` | MB-T11 WB7 wired the orchestrator-action-handler dispatcher's `fireSpawn` dep with a placeholder that throws "orchestrator-fired spawn-new-session not wired in v3.0". Operator-driven spawn (MB-T05) flows through the renderer → `workstation:spawn-requested` IPC path unchanged. v3.0 acceptance test (WB8) spawns sessions via the smoke harness, not via orchestrator-fired action, so this gap is non-blocking for v3.0 ship. Future closure: wire orchestrator-fired spawn through the existing SpawnIpcController.handleSpawnRequest, threading `defaultSpawnHandlerDeps()` (which is async — needs the same controllerPromise pattern as `registerSpawnIpcHandlers` in spawn-ipc.ts:266). Tier 2 — orchestrator-driven spawn becomes useful once the swarm-conductor flow regularly spawns sessions mid-plan; v3.0 dogfood will reveal whether this surfaces. Discoverability: coarchitect-ipc.ts inline comment in fireSpawn dep + this row. | sess-mbt11 WB7 |
| `MB-F-T11-T13-RESOLVER-STUB` | **CLOSED 2026-05-07.** sess-mbt11 originally shipped `approval-policy-resolver-stub.ts` per Q-MBT11-6=a (always `{approvalRequired:true}` while sess-mbt13's real resolver was in flight). Closure verified the stub vs real-resolver contract surfaces DIVERGED (stub: `{actionType, sessionName} → ResolverResult`; real: `{policy, actionType, predicates?} → ApprovalResolverResult`) — NOT a one-liner import swap as originally anticipated. Operator selected Path C (thin shim wrapper): new `approval-policy-resolver-shim.ts` preserves the (actionType, sessionName) call shape, fetches per-session policy via daemon HTTP `GET /v3/sessions/:name/approval-policy`, and delegates to the real `resolveApproval`. Stub source + 2-probe test directory deleted. Handler call site signature changed sync→async (8 mock-injection sites updated mechanically). Eventual call-site rewrite that drops the shim entirely + reconciles the action-type cast tracked at `MB-F-T11-T13-RESOLVER-CALL-SITE-REWRITE`. Tier 1. | sess-mbt11 WB4 → CLOSED Phase 3 2026-05-07 |
| `MB-F-T13-TILE-HEADER-PICKER-INTEGRATION` | **CLOSED by MB-T16 WB4 (2026-05-07).** Q-MBT13-10=b deferral: tile UI did NOT exist at sess-mbt13 ship time (MB-T12 unbuilt). MB-T13 shipped data layer + resolver + audit + IPC only; the per-session tile-header dropdown that lets operators flip approval policy via the tile UI was charted as a follow-on after MB-T12 shipped. Closure path landed verbatim per the original implementation sketch: `tile-approval-picker.tsx` (renderer) reads policy via workstationBridge.getSessionApprovalPolicy → IPC → `GET /v3/sessions/:name/approval-policy` (sess-mbt13 WB5 route); native `<select>` with 3 options tight/medium/loose; onChange fires workstationBridge.putSessionApprovalPolicy → IPC → `PUT /v3/sessions/:name/approval-policy` (sess-mbt13 WB5 route). No daemon-side work — pure UI integration with already-shipped routes. WB4 wires `ApprovalPolicyIpcController` in main.ts via NEW sentinel block + extends `WorkstationBridgeShape` with optional `getSessionApprovalPolicy?` + `putSessionApprovalPolicy?` methods + adds `renderPickerSlot?` render-prop on Tile/TileGrid + TileGridApp constructs the bridge adapter (null when methods missing → picker renders 'unavailable' per Q-MBT16-2=a). Picker mounts INSIDE the existing `<div data-slot="picker" data-testid="tile-picker-slot-{name}">` wrapper preserving the MB-T12 WB5 contract. Optimistic-UI on change with silent rollback + tooltip on PUT error per Q-MBT16-3=a. 44 MB-T16 tests verify the surface end-to-end. Tier 2 (CLOSED). | sess-mbt13 WB11 → MB-T16 WB4 (2026-05-07) |
| `MB-F-T13-SETTINGS-DEFAULT-POLICY-INTEGRATION` | Q-MBT13-11=a deferral: MB-T11-old (settings UI from MB-F-MB-T05-API-KEY-SETTINGS-UI followup) is unbuilt; ticket body referenced "Default policy configured via workstation settings (existing MB-T11 settings UI from prior scope, now MB-T11-old/MB-T13-new — needs renumber, see §6)". sess-mbt13 ships SQL DEFAULT 'medium' as the structural default (per Q-MBT13-4=c defense-in-depth: SQL DEFAULT + workstation-side resolver fallback on no-row, both medium). When MB-T11-old / MB-T14 settings UI lands, surface a "Default approval policy" dropdown in workstation settings; the workstation reads the configured default at session-spawn time and PUTs the policy for the new session via the already-shipped `PUT /v3/sessions/:name/approval-policy` route. No daemon-side work needed; pure settings-UI integration. Tier 2 — sess-mbt13 ship-gate satisfied without it. Discoverability: this followup + CONDUCTOR_V3_RESCOPE.md §6 / §7 Q2. | sess-mbt13 WB11 |
| `MB-F-T11-T13-ACTION-TYPE-ENUM-DEDUP` | Q-MBT13-12=a parallel-definition: sess-mbt13's `ApprovalActionTypeEnum` (in dispatch-core/src/v3/schema.ts §13) and the resolver's local `ApprovalActionType` type (in dispatch-workstation/src/main/approval-policy-resolver.ts) are structurally parallel to sess-mbt11's §12 ActionType (which sess-mbt11 will ship as part of orchestrator action tools). Both branches define the same 5-value enum independently to avoid cross-territory writes pre-merge. Post-cross-merge dedup: choose ONE canonical source (likely sess-mbt11's §12 enum since it owns OrchestratorOutputSchema discriminated union extensions), re-export from §13 (or alias), update resolver to import the canonical type. No semantic change (the enums match by construction); pure schema-side cleanup. Tier 2 — minor schema duplication does NOT block ship. Discoverability: schema.ts §13 doc-comment on ApprovalActionTypeEnum. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-MODAL-FILTERS` | Q-MBT13-9=a v3.1 enrichment: WB8 audit-modal renders LIMIT 100 ORDER BY ts DESC (single page). Operator may want filter axes (session_name, action_type, date range, approval_status) for forensics on large audit logs. Implementation: extend `OrchestratorSwarmAuditQuerySchema` (§13) with optional filter fields; route handler builds dynamic WHERE clause; modal UI adds filter controls (dropdowns / date pickers / text inputs); paginated load-more button replaces single-page render. Tier 3 — single-page LIMIT 100 is sufficient for v3.0; only matters if audit logs grow beyond a few hundred rows. Discoverability: this followup + WB8 commit body + Q-MBT13-9=a arbitration record. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-WRITE-RETRY-QUEUE` | Q-MBT13-7=b risk R5: audit-write is async-after-fire, best-effort with single-shot retry. A workstation crash during the (action-fire → audit-write) window can lose audit rows. Acceptable per "forensics, not gating" framing (per CONDUCTOR_V3_RESCOPE.md §3.8: "useful for forensics when autopilot did something unexpected"). Future enhancement: persist failed audit-writes locally (e.g., to electron-store at ~/.foxworks-dispatch/pending-audit.json) and replay on next workstation startup. Tier 2 — defer to v3.1 unless operator dogfood surfaces lost-rows scenarios. Discoverability: Phase 1 diagnose risk R5 + this followup. | sess-mbt13 WB11 |
| `MB-F-T13-SESSION-POLICY-CLEANUP-ON-KILL` | Q-MBT13-1=a side-effect: sessions persist in `sessions.json` (RegistryV2, frozen v2 schema); `session_policies` is a separate SQLite table per coexistence-without-sync rule. When a session is killed (transitioned to `state=killed` in sessions.json), the corresponding `session_policies` row is NOT auto-cleaned. Stale rows accumulate over time — minor concern (rows are tiny, indexed on session_name, no GET-path cost), but a tidy session re-spawn under the same name could pick up the stale policy unexpectedly. Future enhancement: wire DELETE FROM session_policies WHERE session_name=? at the daemon's PATCH /v2/sessions/:name/state handler when the new state is 'killed'. Tier 2 — non-blocking; the stale-row scenario is operationally rare and the policy-pickup behavior is operator-acceptable (any policy mismatch is observable via the tile-header picker once MB-T12 + MB-F-T13-TILE-HEADER-PICKER-INTEGRATION ship). Discoverability: this followup + WB5 route comment + Q-MBT13-1=a doc. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-SCHEMA-CONVERGENCE` | Risk R6 from Phase 1 diagnose: dispatch-core/src/v3/schema.ts has TWO audit schemas: §5 `OrchestratorAuditRowSchema` (operator-facing approve/decline of orchestrator chat output, freeze-anchor surface from MB-S03) and §13 `OrchestratorSwarmAuditRowSchema` (orchestrator swarm-action firings, sess-mbt13 surface). They are structurally similar but semantically orthogonal per §3.8 (different surfaces, separate forensics). v3.1 may want to converge them into a single discriminated-union audit table if convergence becomes desirable for unified forensics tooling. Tier 3 — orthogonal v3.0 surfaces are correct as-is; this followup tracks the optional convergence direction for v3.1+ if cross-surface forensics becomes a need. Discoverability: schema.ts §5 + §13 + this followup. | sess-mbt13 WB11 |
| `MB-F-DISPATCH-CORE-POST-PULL-REBUILD-DISCIPLINE` | After any merge to main that adds new exports to dispatch-core/src/v3/schema.ts (or any dispatch-core source), the workstation typecheck WILL fail until `pnpm --filter dispatch-core build` is run to refresh dist/ artifacts. Workstation imports use `dispatch-core/dist/v3/schema.js` paths (per MB-F-DISPATCH-CORE-DUAL-IMPORT-PATTERN-DRIFT lesson) and TypeScript reads exported member declarations from the dist .d.ts files, NOT from src. Manifested 2026-05-06 post-sess-mbt13-merge: workstation typecheck failed with TS2724 errors on the new §13 OrchestratorSwarmAudit* exports until dist was rebuilt. Recommend: (a) postinstall hook running `pnpm -r build` for dispatch-core, OR (b) merge-gate adding `pnpm --filter dispatch-core build` between merge-commit and post-merge typecheck verification, OR (c) explicit operator-discipline note in merge runbook. Tier 1 — every dispatch-core schema-additive merge will reproduce this until addressed. Discoverability: this followup + 2026-05-06 main-merge incident. | sess-mbt13 merge 2026-05-06 |
| `MB-F-WORKSTATION-KANBAN-EMPTY-STATE-UX` | Operator screenshot 2026-05-06 evening showed blank kanban region in workstation. Triangulated via `sessions.json` inspection: all sessions in `killed` state, no live tmux server, dispatch-web kanban renders correctly but operator perceives blank rectangle as a bug. Original `MB-F-WORKSTATION-KANBAN-VIEWPORT-BLANK` (Tier 1) was correctly RESOLVED at `01dfd54` (renderer crash fix); this entry tracks the distinct empty-state UX gap. Recommended fix: kanban-region renders an explicit empty-state placeholder ("No active sessions — click + Spawn Session to start") when zero non-killed sessions exist in `sessions.json`. Pairs with MB-T12 tile-grid empty-state (Q-MBT12-9 from diagnose) — both surfaces share the same operator-perception failure mode (blank rectangle interpreted as broken UI rather than zero-state). Tier 1 — operator-visible perceived-broken-UI ship-gate concern. Discoverability: this followup + MB-T12 Phase 1 diagnose §IV Q-MBT12-9. | 2026-05-06 evening operator triage |
| `MB-F-T11-T13-RESOLVER-CALL-SITE-REWRITE` | Phase 3 closure of `MB-F-T11-T13-RESOLVER-STUB` landed `approval-policy-resolver-shim.ts` as a thin async wrapper that fetches the per-session `ApprovalPolicy` via daemon HTTP and delegates to the real `resolveApproval` (sess-mbt13). The shim is intentionally short-lived: the orchestrator-action-handler call site still passes `{ actionType, sessionName }` and forwards no predicates. v3.1 closure: rewrite the call site to (a) fetch the per-session policy ONCE per `dispatchAction` invocation (keep async at the handler level, not at the resolver dep), (b) compute the §3.2 medium predicates (`willCommit`, `willTouchContract`, `isMultiStep`) from the action payload + workstation state and pass them into the real resolver directly, (c) drop the shim file entirely + revert the resolver dep to a sync `(input: ApprovalResolverInput) => ApprovalResolverResult` signature, (d) drop the `MBT11ActionType → ApprovalActionType` cast (paired with `MB-F-T11-T13-ACTION-TYPE-ENUM-DEDUP` closure). Net effect: handler does I/O explicitly + resolver returns to its pure-function contract. Tier 2 — shim is correct under medium baseline (graceful-degraded predicates yield the most-permissive correct answer per resolver R3); operator-visible behavior is acceptable for v3.0 dogfood. Discoverability: `approval-policy-resolver-shim.ts` file-header + this row. | Phase 3 2026-05-07 |
| `MB-F-COARCHITECT-IPC-LINE-485-ROUTEORCHESTRATOR-DETERMINISTIC-FAIL` | `test/unit/coarchitect-ipc/test_register_ipc_handlers.spec.ts` test "routeOrchestratorOutput card-or-multi-choice decision broadcasts to all webContents and caches context" (line 485) fails DETERMINISTICALLY when run in isolation: `waitForSend('coarchitect:streamDone', 1000ms)` times out after seeing 17 streamChunk events + 1 streamError. Pattern: the routeOrchestratorOutput card-or-multi-choice branch fires streamError instead of streamDone after streaming chunks. Surfaced during MB-T12 WB2 diagnostic (3-evidence convergence: full-suite set-drift, isolation-reproducibility, import-graph exclusion of MB-T12 territory). Reproduces against pre-WB2 file state — pre-existing bug or stale-mock pattern unrelated to MB-T12. Triage: read coarchitect-ipc.ts routeOrchestratorOutput card-or-multi-choice path + the test's mock setup; identify whether the streamError is logic-correct (and the test assertion is wrong) or vice versa. Likely candidate: the test was written before sess-mbt11 WB7's tier4-fan-out edits to coarchitect-ipc.ts and the mock chain hasn't been updated. Tier 2 — deterministic single-test fail; doesn't block MB-T12 ship but does block clean `pnpm test` for workstation. Discoverability: this row + MB-T12 WB2 commit body diagnostic. | MB-T12 WB2 diagnostic 2026-05-07 |
| `MB-F-WORKSTATION-INTEGRATION-TEST-FLAKE-SUITE` | 5 integration tests fail under standard `pnpm --filter dispatch-workstation test` because they require infrastructure absent in the default test environment: (a) `fix-83-verification/probe-04-spawn-result-ok-live-daemon` — daemon-gated, no live daemon; (b) `fix-84-verification/probe-06-defect-b-card-emission-manual` — MANUAL test (operator-only); (c) `fix-89-menu-rebuild/probe-01-menu-rebuild-propagates` — Electron menu API; (d) `mb-t05-kanban-card/probe-01-spawn-card-renders` — webview/DOM dependent; (e) `mb-t04/spawn-modal-emits-intent` — DOM IPC dependent. Failure set surfaced during MB-T12 WB2 diagnostic and verified UNCHANGED through WB13 — none in MB-T12 import graph. Recommended fix: add tier separation via vitest projects (one project per tier: unit / integration-headless / integration-electron / MANUAL) OR use `MB_TEST_INTEGRATION=1` env gating to skip-by-default. Tier 3 — flake-resistant tests already exist in the suite; this is a methodology / discoverability concern, not a logic bug. Discoverability: this row + MB-T12 WB2 commit body. | MB-T12 WB2 diagnostic 2026-05-07 |
| `MB-F-T12-BUNDLE-SHARED-CHUNK-DEDUP` | `dist/tile-grid/renderer.js` is 1.5MB (matches `dist/console-panel/renderer.js` pre-existing 1.5MB and `dist/audit-modal/renderer.js` 1.1MB). Total workstation bundle weight: ~4.5MB across the 3 bundles, mostly duplicated React + react-dom + react-jsx-runtime. The post-WB12 shell loads BOTH console-panel/renderer.js (for the WB11 detached-window standalone path) AND tile-grid/renderer.js — both bundles include their own copies of React. Mitigation options: (a) externalize react/react-dom + serve via a shared chunk, (b) tree-shake xterm to drop unused addons (console-panel-specific), (c) extract a shared "react-runtime.js" that all bundles import, (d) consolidate the renderer bundles into one with multiple mount entry points. Tier 2 — non-blocking for v3.0 (workstation is desktop-installed, not over-the-network), but a clear performance win for cold-launch + Electron's renderer-process memory. Discoverability: this row + scripts/build-tile-grid.mjs inline comment + R-MBT12-1 in MB-T12 Phase 1 diagnose. | MB-T12 WB1 2026-05-07 |
| `MB-F-T12-AUTOPILOT-TILE-TOGGLE-INTEGRATION` | **CLOSED at MB-T17 WB4 (2026-05-07)** — per-session autopilot toggle now ships end-to-end via the MB-T17 ladder. Implementation: `<TileAutopilotToggle>` React component (native `<input type="checkbox" role="switch">` per Q-MBT17-1=a) renders inside the preserved `<div data-slot="autopilot" data-testid="tile-autopilot-slot-{name}">` wrapper via `Tile.renderAutopilotSlot?` render-prop (Q-MBT17-4=a). Bridge: `workstationBridge.getSessionAutopilotEnabled` + `setSessionAutopilotEnabled` (preload.mts) → IPC channels `workstation:autopilot-{get,put}` → `AutopilotIpcController` → `AutopilotLoop.setEnabled/.isEnabled` → autopilot-state.json (workstation-side; NO daemon route per Phase 1 diagnose §I-E). State machine: `loading | ready | unavailable` with disabled+tooltip on bridge-missing or fetch-error (Q-MBT17-2=a) + optimistic UI with silent rollback on PUT error (Q-MBT17-3=a). Closing commit: `green(MB-T17): WB4 — slot integration + main.ts wire + closes MB-F-T12-AUTOPILOT-TILE-TOGGLE-INTEGRATION`. Original-row context preserved: per Q-MBT12-7=a, MB-T12 WB5 had shipped `<Tile>` with placeholder slot divs that sess-mbt11 deferred. | MB-T12 WB5 2026-05-07 |
| `MB-F-T12-RENDERER-PERSISTENCE-WIRING` | TileGridApp (WB9) accepts `onPersistSessions` + `onPersistGridOverride` callback props that the parent (mount.ts) is meant to wire to `tile-grid-state.ts` writers (`writeTileLayoutState` + `writeAllTileLayoutStates` + `writeGridOverride`). WB12's mount.ts ships the renderer in-memory only — the callbacks are not provided, so tile state (collapsed/detached/orderIndex) and grid override (drag-resize sizes) reset on workstation restart. Closure path: (a) add IPC channels `tile-grid-state:read-all` / `tile-grid-state:write-tile-state` / `tile-grid-state:write-grid-override` to a new `src/main/tile-grid-state-ipc.ts` module, (b) expose them on workstationBridge in preload.mts, (c) update mount.ts to seed `initialSessions` + `initialGridOverride` from IPC read on startup + wire the persistence callbacks to IPC writes. Tier 2 — operator default kept at this tier post-WB13 HALT 1 review. Reasoning: tile-state-loss-on-restart is a UX nice-to-have for v3.0 dogfood; operators can reorganize on each launch. Tier 1 only if operator views layout-loss-on-restart as a blocker. Discoverability: this row + WB12 mount.ts file-header + MB-T12 WB13 verification doc §VIII. | MB-T12 WB12 2026-05-07 |
| `MB-F-T12-TAB-STRIP-OVERFLOW` | Per Q-MBT12-10=a, MB-T12 WB6 + WB7 ship scroll-only overflow at N≥9 (the explicit grid stays 2×4; tiles 9+ flow into implicit rows via CSS grid auto-flow + `overflow-y:auto`). §3.3 line 84 of CONDUCTOR_V3_RESCOPE.md mentions a tab-strip at the top for accessing overflow panes — that UX is deferred to v3.1. Implementation likely: render a horizontal tab-strip above the grid when sessions.length > rows*cols; clicking a tab scrolls that tile into view (or, alternative: the tab-strip becomes the source of truth and the grid renders only N=8 tiles at a time, with the tab-strip toggling which session occupies which tile). Tier 3 — v3.0 acceptance test only covers N=4 per spec; scroll-only is functional for N=9+. Discoverability: this row + MB-T12 decisions doc Q-MBT12-10. | MB-T12 WB6 2026-05-07 |
| `MB-F-T12-DETACH-BACK-INTO-MAIN` | Per CONDUCTOR_V3_RESCOPE.md spec line 219: "Detached-tile inter-process state sync is fire-and-forget (operator can't drag it back into the main window in v3.0; closing the detached window re-tiles in main)". WB11a + WB11b honor this scope: closing the detached window restores the tile in the main grid, but there's no UI affordance to drag a detached window's content back without closing the window. v3.1 closure: add an "attach back" button to the detached window's chrome (or a window-position-snap UX where dragging the BrowserWindow over the main grid attaches). Tier 3 — v3.0 close-and-reopen flow is functional. Discoverability: this row + MB-T12 decisions doc + spec line 219. | MB-T12 WB11 2026-05-07 |
| `MB-F-T12-COLLAPSE-ROW-COMPRESS` | WB10 ships per-tile collapse where the collapsed tile renders header-only (40px) at the TOP of its grid cell via `align-self: start`. The grid template-rows stay `1fr` for both rows even when one row's tiles are all collapsed — visually: collapsed row shows 40px headers + ~60% empty space below. UX polish for v3.1: when ALL tiles in a row are collapsed, set that row's grid-template-rows to a fixed 40px (or `auto`) so the OTHER rows of expanded tiles get more vertical space. Implementation likely: in TileGrid, derive `rowCollapsedFlags: boolean[]` from sessions+layout; set gridTemplateRows[r] = '40px' if all tiles in row r are collapsed. Tier 3 — v3.0 collapse is functional (header is visible, body is hidden); the compress is visual polish. Discoverability: this row + MB-T12 WB10 commit body coordination context. | MB-T12 WB10 2026-05-07 |
| `MB-F-T12-CROSS-LAYER-FLOW-DIAGRAM-DOC` | **CLOSED by MB-T12 WB14 (2026-05-07).** Operator post-WB13 ask: move the WB11b (`59becfa`) commit-body cross-layer flow diagram to a permanent doc so it's discoverable outside `git log`. Closure: `docs/coordination/mb-t12-architecture-flow.md` authored at WB14 with §III being the WB11b detach flow + §I composition diagram + §II spawn auto-mount + §IV drag-resize + §V drag-swap + §VI multi-target routing — net 6 cross-layer flows now grep-discoverable. Tier 3 (CLOSED). | MB-T12 WB14 2026-05-07 |
| `MB-F-LADDER-PER-COMMIT-PUSH-VS-HALT-2-TENSION` | At WB14 of MB-T12, the per-commit-push primitive (operative since WB1) and HALT 2 (gated push to main) conflicted. CC defaulted to per-commit-push and pushed WB14 immediately, despite Q9 self-check correctly affirming the HALT 2 policy. Surfaced transparently with rollback offered. Closure path: future ladder briefs that include a HALT-N gate on push must explicitly suspend per-commit-push for that WB, or specify that the gated push is the only push for that WB. Operator-side instructions that include HALT-N gates should name the gated action explicitly ("WB-N commits ONLY; do NOT push until operator acks"). Tier 2 — methodology fix prevents recurrence; the gap is in primitive-vs-gate composition, not in either primitive in isolation. Discoverability: this row + MB-T12 WB14 commit body Q9 + the post-WB14 surface message disclosure. | MB-T12 HALT 2 2026-05-07 |
| `MB-F-T15-MODEL-CHIP-HEX-COLORS-PLACEHOLDER` | MB-T15 WB2 + WB3 ship placeholder hex color codes for the model chip palette (S4.6 → `#4a9eff` blue/cyan, O4.6 → `#a36cff` light purple, O4.7·1M → `#7c3eed` deeper purple, H → `#facc15` amber/yellow) + status-dot palette (gray/green/yellow/red) + token-meter tints (normal blue / warn amber / danger red). Operator-side inventory `§3 MB-T15 entry` (path `/mnt/user-data/outputs/wireframe-tickets-inventory.md`, not committed) likely has the canonical hex specifications. Closure: operator pastes the canonical palette → single-file edit to `packages/dispatch-workstation/src/tile-grid/color-helpers.ts` `modelChipColor` switch + `packages/dispatch-workstation/src/tile-grid/tile-header.tsx` `STATUS_DOT_HEX` + `TOKEN_TINT_HEX` const tables. No test changes required (existing assertions check hex format `/^#[0-9a-fA-F]{6}$/` + distinct-colors invariant; specific hex values not asserted). Tier 2 — operator-visible UX fidelity; v3.0 ships with placeholders that look defensible against the dark theme. Discoverability: this row + MB-T15 decisions doc §I + WB2 color-helpers.ts file-header. | MB-T15 WB5 2026-05-07 |
| `MB-F-T15-AUTOPILOT-TOKEN-INTEGRATION` | MB-T15 WB3 + WB4 ship the token meter UI surface with stubbed data (Q-MBT15-2=a: `tokensUsed=0`, `tokenBudget=200_000` defaults). Real data source lands with autopilot loop telemetry (sess-mbt11 WB6 + future MB-T17 successor): per-session token tracking via NEW field on the autopilot-state-store OR a separate per-session-cost-info module. The TileGridSessionEntry already accepts `tokensUsed?` + `tokenBudget?` plumb-through; closure path is wiring the autopilot telemetry → TileGridApp `useEffect` subscription → `setSessions` updates per-session token fields. Optionally: per-model `tokenBudget` lookup (Sonnet 4.6 = 200k, Opus 4.7·1M = 1M, Haiku 4.5 = 200k) in `color-helpers.ts` so the meter scales correctly when model varies. Tier 2 — meter visual ships correctly; data is fake until autopilot wires it. Discoverability: this row + MB-T15 WB3 tile-header.tsx file-header + decisions doc §I-stub-defaults table. | MB-T15 WB5 2026-05-07 |
| `MB-F-T15-SESSION-DAEMON-FIELDS-WIRING` | MB-T15 WB3 + WB4 ship the tile-header chrome with stubbed defaults for branch (`'main'`), repo (basename of cwd; needs parent plumb-through), and model (`'claude-sonnet-4-6'`). Real data sources per Q-MBT15-2=a stub elimination plan: (a) **branch** via subscribing `/v2/events/stream` `commit_landed` events (already emit `data.branch` per CommitLandedEvent at packages/dispatch-core/src/v2/schema.ts:229) + caching per session in TileGridApp state; (b) **repo** via existing `cwd` field on SessionV2 → basename — plumb-able today via TileGridApp seed without daemon work; (c) **model** via spawn-handler captures (operator-only `model` field amendment to SessionSchemaV2 OR renderer-side cache mapped by sessionName, populated when operator spawns with explicit model selection at MB-T16/T17 picker). All paths preserve existing TileGridSessionEntry shape (optional fields). Tier 2 — chrome reads correctly; v3.0 dogfood operates on stub defaults. Discoverability: this row + MB-T15 decisions doc §I-stub-defaults table + Phase 1 diagnose §I-D session schema analysis. | MB-T15 WB5 2026-05-07 |
| `MB-F-T15-COMPACT-MODE-AT-NARROW-WIDTH` | MB-T15 WB3 ships ellipsis truncation + `flex-shrink: 1` on text spans + `flex-shrink: 0` on dot/chip/meter, sufficient for the brief's 240px min-width acceptance. UX polish for v3.1: at sub-240px widths (or by operator preference for compact display), the header could switch to a "compact mode" hiding repo name first, then abbreviating session name to the first segment, then dropping the model chip text (showing only the chip color block). Implementation likely: a CSS @container query (or `useResizeObserver` hook) toggling a `data-compact="true"` attribute on tile-header-content + scoped CSS overrides. Tier 3 — v3.0 truncation is functional; this is visual-density polish. Discoverability: this row + Phase 1 diagnose Q-MBT15-7=a tentative recommendation. | MB-T15 WB5 2026-05-07 |
| `MB-F-WRITE-TOOL-READ-TRACKING-CROSS-TURN` | The Write tool's "must Read before Write" tracking did NOT persist between conversation turns during MB-T15 WB1, WB2, WB3. Pattern: file written successfully via Write at WB1 → continued conversation in WB2 → Write to same file errored "File has not been read yet". Workaround that worked: explicit Read call (even 1-line) immediately before the Write re-establishes tracking. Affected files: `color-helpers.ts` (WB2), `tile-header.tsx` (WB3 + WB4). Surfaced as a planned MB-T15 WB1 surprise (per MB-T15 WB1 commit body); recurred WB2 + WB3. Closure path: not actionable from CC side — this is a harness behavior. Operator may want to surface to harness maintainers OR document as a CC operating discipline ("Read before Write of same file across turn boundaries"). Tier 3 — methodology lesson; recurrence rate ~1 per WB across MB-T15. Discoverability: this row + MB-T15 WB1 + WB2 + WB3 commit bodies disclosing the pattern. | MB-T15 WB5 2026-05-07 |
| `MB-F-T16-PICKER-CACHE-TTL` | MB-T16 WB3 ships per-tile fetch on mount with NO cache (Q-MBT16-7=a). With ≤8 tiles this means 8 small GETs to `/v3/sessions/:name/approval-policy` on workstation startup + every component re-mount. Acceptable for v3.0; for v3.1 polish, add a per-session in-memory cache shared across all picker instances + invalidate on PUT mutation. Closure path likely: a small `approval-policy-cache.ts` module exporting `get(sessionName)` (memoized Promise) + `invalidate(sessionName)` (cache-bust on PUT or on TTL expiry). Open design questions: (a) cache invalidation on TTL expiry vs PUT-only — if TTL, what's the right value (5min? 30min?); (b) cross-window sync — currently each detached BrowserWindow has its own React state + cache; v3.1 may want a single source-of-truth via main-process cache + IPC events. Tier 3 — v3.0 picker is functionally correct without cache. Discoverability: this row + MB-T16 decisions doc Q-MBT16-7 + findings doc §VI + WB3 picker.tsx file-header. | MB-T16 WB5 2026-05-07 |
| `MB-F-T16-PICKER-RETRY-AFFORDANCE` | MB-T16 WB3 ships disabled `<select>` + `title=` tooltip surfacing the error message when GET `/v3/sessions/:name/approval-policy` fails on initial fetch (Q-MBT16-2=a graceful-degrade-to-unavailable). When the daemon is unreachable on workstation startup, the picker stays in 'unavailable' state with NO retry path — operator must reload the renderer (Cmd+R) or restart the workstation to retry. v3.1 polish: add an explicit retry affordance — e.g., a small "↻" icon button beside the disabled select that re-runs the initial fetch + transitions to 'ready' on success. Closure: extend the picker state machine to include `retryRequested` flag → `useEffect` re-fires fetch when flag flips. Tier 3 — v3.0 ships functional; manual workaround (renderer reload) covers the rare transient-error case. Discoverability: this row + MB-T16 decisions doc Q-MBT16-2 + findings doc §VI + WB3 picker.tsx file-header. | MB-T16 WB5 2026-05-07 |
| `MB-F-T16-OPTIMISTIC-ROLLBACK-OBSERVABILITY` | MB-T16 WB3 ships optimistic UI on PUT `/v3/sessions/:name/approval-policy` (Q-MBT16-3=a): on PUT failure, the select silently rolls back to the prior value AND surfaces the error via `title=` tooltip on the select element. Risk per R-MBT16-8: the tooltip-only surfacing is easy to miss — if operator moves on to other tiles after triggering the change, they may not realize the policy update did not commit. v3.1 polish: add a more prominent error indicator — e.g., a transient toast/banner at the workstation footer OR a colored ring around the select element for ~3s after a rollback OR a status icon adjacent to the select that persists until next successful PUT. Closure path: extend picker state with `lastErrorAt: number` timestamp + render a styled badge/banner conditional on recent error. Tier 3 — v3.0 ships with tooltip-surfacing; in practice, daemon writes are reliable when the daemon is reachable. Discoverability: this row + MB-T16 decisions doc Q-MBT16-3 + R-MBT16-8 + findings doc §VI + WB3 picker.tsx file-header. | MB-T16 WB5 2026-05-07 |
| `MB-F-T17-AUTOPILOT-CACHE-TTL` | MB-T17 WB3 ships per-tile fetch on mount with NO cache (Q-MBT17-7=a). With ≤8 tiles this means N small IPC round-trips to `workstation:autopilot-get` on workstation startup + every component re-mount. Acceptable for v3.0 (sync fs reads are cheap; no daemon round-trip); v3.1 polish: add a per-session in-memory cache shared across all toggle instances + invalidate on PUT mutation OR on autopilot-state-store mutations from non-IPC paths (orchestrator-action-handler `recordAction`). Closure path likely: a small `autopilot-state-cache.ts` module exporting `get(sessionName)` (memoized Promise) + `invalidate(sessionName)` (cache-bust on PUT or on TTL expiry). Open design questions: (a) cache invalidation on TTL vs PUT-only — orchestrator's recordAction mutations don't go through IPC, so TTL needed for parity; (b) cross-window sync — currently each detached BrowserWindow has its own React state + cache; v3.1 may want a single source-of-truth via main-process cache + IPC events. Tier 3 — v3.0 toggle is functionally correct without cache. Discoverability: this row + MB-T17 decisions doc Q-MBT17-7 + findings doc §VIII + WB3 toggle file-header. | MB-T17 WB5 2026-05-07 |
| `MB-F-T17-AUTOPILOT-RETRY-AFFORDANCE` | MB-T17 WB3 ships disabled `<input type="checkbox" role="switch">` + `title=` tooltip surfacing the error message when the IPC bridge is unavailable OR `workstation:autopilot-get` rejects on initial fetch (Q-MBT17-2=a graceful-degrade-to-unavailable). When the bridge methods are missing in test fixtures or non-Electron envs the toggle stays in 'unavailable' state with NO retry path. v3.1 polish: add an explicit retry affordance — e.g., a small "↻" icon button beside the disabled checkbox that re-runs the initial fetch + transitions to 'ready' on success. Closure: extend the toggle state machine to include `retryRequested` flag → `useEffect` re-fires fetch when flag flips. Tier 3 — v3.0 ships functional; manual workaround (renderer reload) covers transient-error case; bridge-missing is fixture-only and not user-visible. Discoverability: this row + MB-T17 decisions doc Q-MBT17-2 + findings doc §VIII + WB3 toggle file-header. | MB-T17 WB5 2026-05-07 |
| `MB-F-T17-OPTIMISTIC-ROLLBACK-OBSERVABILITY` | MB-T17 WB3 ships optimistic UI on `setSessionAutopilotEnabled` (Q-MBT17-3=a): on PUT failure, the checkbox silently rolls back to the prior value AND surfaces the error via `title=` tooltip on the checkbox element. Risk per R-MBT17-8: the tooltip-only surfacing is easy to miss — if operator moves on to other tiles after triggering the change, they may not realize the autopilot toggle did not commit (note: AutopilotLoop best-effort fs writes swallow disk-full / permission errors per autopilot-state-store.ts file header, so the IPC layer's PUT will rarely "fail" visibly; v3.1 may want explicit fs-write failure surfacing too). v3.1 polish: add a more prominent error indicator — e.g., a transient toast/banner at the workstation footer OR a colored ring around the checkbox for ~3s after a rollback OR a status icon adjacent to the toggle that persists until next successful PUT. Closure path: extend toggle state with `lastErrorAt: number` timestamp + render a styled badge/banner conditional on recent error. Tier 3 — v3.0 ships with tooltip-surfacing; in practice, sync fs writes are reliable when the userData dir is writable. Discoverability: this row + MB-T17 decisions doc Q-MBT17-3 + R-MBT17-8 + findings doc §VIII + WB3 toggle file-header. | MB-T17 WB5 2026-05-07 |
| `MB-F-PARALLEL-CAIRN-INDEX-RACE-ATOMIC-COMMIT` | **Tier 1 methodology**. In shared-working-tree parallel-cairn (multiple sessions on the same checkout), the git index is process-shared, NOT session-private. Per-path `git add <path>` (CLAUDE.md §2.7) protects against THIS session's accidental sweeps but does NOT protect against another session running `git add` between this session's pre-commit `git status --short` snapshot and this session's `git commit`. Evidence: MB-T18 WB1 commit `ecdd0e4` was authored as a 4-path MB-T18 scaffold; this session's pre-stage `git --no-pager diff --cached --stat` confirmed exactly 4 paths staged AND pre-commit `git status --short` confirmed the same 4 staged + 7 unstaged from Terminal A's MB-T17 WB4; between that snapshot and the `git commit` invocation, Terminal A staged their 7 WB4 paths from a parallel shell, and the resulting commit captured all 11 paths under MB-T18's subject (see `docs/coordination/mb-t18-wb1-index-race-clarification-2026-05-07.md`). **Forward fix:** every cairn-grammar commit chains `git add → diff --cached --name-only verify → commit` in a SINGLE shell invocation so no other session can mutate the index between verify and commit. Pattern: `git add <paths> && git diff --cached --name-only \| sort > /tmp/intended-staged.txt && printf '<paths>\n' \| sort > /tmp/intended-list.txt && diff /tmp/intended-staged.txt /tmp/intended-list.txt && git commit -m "..." && git --no-pager log -1 --name-only && git push origin main`. The `diff` aborts the chain if the index has more or fewer paths than expected. Q7 self-check protocol revised to verify against `git log -1 --name-only` POST-commit (not pre-commit `git status` snapshot). **Ceiling consideration:** 4 concurrent sessions in a shared working tree create N choose 2 = 6 pairwise race opportunities at every commit; the atomic-chain pattern eliminates the race INSIDE a session but does not address index-write contention between simultaneously-running atomic chains. If 4-session shared-tree parallel-cairn becomes load-bearing, prefer `git worktree` split (CLAUDE.md §4.3 — "each session works in its own worktree") for structural isolation. Cross-reference: `MB-F-T20-LOCAL-HEAD-DIVERGENCE-MECHANISM` (line 130, Tier 3) is Terminal C's downstream observation of this same incident filed under "root cause unidentified"; this followup IDENTIFIES the root cause. Discoverability: this row + clarification doc + ecdd0e4 commit body Q7 + this session's incident-recovery audit thread.  → **Second occurrence at MB-T21 Phase 1 (`e02aa52`, 2026-05-07)**: Terminal A MB-T21 atomic chain ran `git diff --cached --name-only` confirming exactly 1 path staged (`mb-t21-diagnose`); between the diff verify and `git commit` (the `&&` boundary), Terminal C's parallel atomic chain `git add`-ed `mb-t26-diagnose-2026-05-07.md`; my commit + push captured both paths under MB-T21's subject + Q1-Q9 self-check (Q7 "Touched files another parallel session might modify? No" now factually wrong on origin/main — same shape as ecdd0e4). Operator disposition: **accept-and-document** (no force-push, no revert; both files are correct content, only commit attribution misleading). **Forward mitigation per operator (parallel-cairn coordination protocol update 2026-05-07):** at every subsequent atomic-chain, halt ~30 seconds before `git pull --ff-only` to allow other terminals' chains to complete (probabilistic, not guaranteed). Per-path discipline + atomic-chain + courtesy-delay all upheld at MB-T21 WB1-4 (`5fff03a`, `536d413`, `fd6b9c5`, `f2b7ccd`); Q7 verified TRUE on post-commit `git log -1 --name-only` for every WB after Phase 1. **Third occurrence triggers all-sessions halt + structural pivot to `git worktree`** per CLAUDE.md §4.3. The courtesy-delay protocol successfully prevented further incidents in MB-T21 across 4 cairn-grammar commits with 3 concurrent terminals. If shared-tree parallel-cairn continues beyond this run, evaluate worktree-split as the durable forward fix. | MB-T18 WB1 recovery 2026-05-07 |
| `MB-F-T18-FOOTER-DAEMON-AUTHORITATIVE-SPAWN-TIME` | MB-T18 WB2 ships uptime as a renderer-side mount-time snapshot (Q-MBT18-3=a): TileFooter captures `Date.now()` at first useEffect render + ticks every 5s. Risk per R-MBT18-5: semantics are "time since this tile was last mounted in this window" — NOT "time since session spawn." Renderer reload (Cmd+R / window refresh) or detach-close re-mount resets uptime to 0s; the displayed value diverges from the operator's mental model of session uptime. Acceptable for v3.0 single-user single-window workflow; v3.1 polish: surface daemon-authoritative spawn time. Closure paths: (a) extend daemon GET /v2/sessions/:name response body to include `created_at` (registry already has it; route excludes per §2.10 frozen surface — operator-arbitrated contract amendment required); (b) extend SpawnSessionResult with `spawnedAt: string` ISO datetime + persist via tile-grid-state.ts so the value survives renderer reload (workstation-internal; no contract change); (c) hybrid — mount-time snapshot for first-mount sessions + tile-grid-state lookup for re-mounted sessions. Tier 3 — v3.0 ships functional with honest "since-mount" semantics. Discoverability: this row + MB-T18 decisions doc R-MBT18-5 + findings doc §VII + WB2 tile-footer file-header. | MB-T18 WB4 2026-05-07 |
| `MB-F-T18-FOOTER-LAST-ACTIVITY-INSTRUMENTATION` | MB-T18 ships uptime as the time chrome (Q-MBT18-1=e cwd + uptime). Operator brief mentioned "last-activity-time" as an alternative footer signal; Phase 1 diagnose §I-E observed that `last_action_fired_at` from SessionContextSnapshot could surrogate but is NOISY (only fires on orchestrator-driven actions, NOT operator-typed prompts; sits at null for idle / operator-driven sessions). v3.1 polish: add a true last-activity timestamp by instrumenting console-output events (renderer-side subscription to terminal-adapter.write events OR daemon-side cc_console_buffer row insertions). Closure path: (a) renderer-side — TileGridApp subscribes to per-tile terminal output, updates lastActivityAt: number per session entry, footer renders `Math.max(uptime, age-since-last-activity)` or swaps display modes; (b) daemon-side — extend SessionContextSnapshot with `last_console_output_at` (frozen surface §2.10 — operator-arbitrated contract amendment); (c) hybrid where renderer uptime is the fallback when no activity is observed. Tier 3 — v3.0 ships with uptime-only footer per Q-MBT18-1=e tentative; "uptime" + "last-activity" are mutually informative and the operator may prefer one or both at v3.1. Discoverability: this row + MB-T18 decisions doc Q-MBT18-1 + Phase 1 diagnose §I-E + findings doc §VII + operator brief 2026-05-07. | MB-T18 WB4 2026-05-07 |
| `MB-F-T19-SQUAD-STRIP-SHRINK-TO-FIT` | MB-T19 WB3 ships horizontal scroll on the squad strip when (n-1) tiles underflow the available width (Q-MBT19-10 operator decision: "horizontal scroll for v3.0 — preserves tile minimum width"). v3.1 polish alternative: shrink-to-fit, where squad tiles compress below their typical-min-width to all stay visible without scroll. Closure path: tile-grid.tsx already wires `gridStyle.overflowX = 'auto'` in hero mode; v3.1 toggle would either (a) remove `overflowX = auto` + add `gridTemplateColumns: minmax(MIN_PX, 1fr)` so squad cols compress proportionally; (b) operator-preference setting (workstation-level) selecting between scroll/shrink at runtime; (c) automatic switch based on (n-1) threshold (e.g., shrink when (n-1) <= 5, scroll when > 5). The HeroSquadLayout descriptor's `overflow: boolean` field already discriminates `n > 8` overflow case; v3.1 just needs the renderer-side CSS toggle. Tier 3 — v3.0 ships functional with operator-confirmed horizontal scroll. Discoverability: this row + MB-T19 decisions doc Q-MBT19-10 + findings doc §VIII + tile-hero-squad-layout.ts file-header. | MB-T19 WB5 2026-05-07 |
| `MB-F-T19-MUTATION-UI` | MB-T19 WB4 ships heroSessionName storage + API surface (`tile-grid-state.ts` field + reader/writer + TileGridApp passthrough prop) but NO UI affordance to MUTATE the hero designation. v3.0 path: operator-edit-then-restart of `<userData>/tile-grid-state.json` (write `heroSessionName` field manually + workstation restart). v3.1 polish: add a UI mutation flow. Q-MBT19-2 surveyed options at Phase 1: (a) per-tile "promote to hero" button (parallel to MB-T16 picker / MB-T17 toggle — adds a tile-header chrome affordance), (b) auto-derived from activity (most-recently-active session = hero), (c) global keyboard shortcut + tile-focus model (e.g., Cmd-Shift-H to promote the focused tile). Operator chose Q-MBT19-2=d (out-of-band heroSessionName field); the trigger UI was deferred. Closure path: extend TileGridApp with onPromoteHero callback wired to writeHeroSessionName() + add the UI affordance per chosen option (a/b/c) + tile-grid.tsx visual indicator on the current hero (badge / border / chrome differentiation). Also resolves Q-MBT19-6 hero-collapse-or-detach interaction (auto-demote vs invariant) — pending operator preference. Tier 2 — operator-visible feature parity gap (hero/squad layout is functionally testable but not operator-flippable in v3.0). Discoverability: this row + MB-T19 decisions doc Q-MBT19-2 + findings doc §VIII + WB4 tile-grid-state.ts readHeroSessionName docstring "operator-edit-then-restart only" note. | MB-T19 WB5 2026-05-07 |
| `MB-F-T19-WITHIN-STRIP-RESIZE` | MB-T19 WB3 ships hero/squad boundary drag-resize (horizontal handle between row 0 hero and row 1 squad strip — Q-MBT19-5=b operator decision). Within-squad-strip resize (vertical handles between squad cols) is SUPPRESSED in hero mode for v3.0 — only the horizontal handle renders. v3.1 polish: restore vertical handles inside the squad strip. Closure path: tile-grid.tsx already has the conditional `if (!isHeroMode)` guard around `verticalHandles.push(i)`; v3.1 just removes the guard + adds drag math for variable squad-col sizes (existing computeNewSizesAfterDrag is band-count-agnostic; should work as-is). Open design question: should within-strip resize affect the hero region's width? In hero mode, hero spans all cols (gridArea: t<heroIndex> repeated), so col resizing within the squad strip changes the hero's cell width too — operator may want hero locked to full-width with squad-only drag (requires gridTemplateAreas refactor where hero spans a SINGLE col taking 100% via gridTemplateColumns + squad in a separate row using its own col grid). Tier 3 — v3.0 ships functional with row boundary drag + uniform squad cols. Discoverability: this row + MB-T19 decisions doc Q-MBT19-5 + findings doc §VIII + WB3 tile-grid.tsx verticalHandles guard. | MB-T19 WB5 2026-05-07 |
| `MB-F-T19-ENHANCED-HERO-CHROME` | MB-T19 WB3 ships hero tile rendering with the SAME chrome as squad tiles per Q-MBT19-8=a (TileHeader + TileFooter + picker + autopilot toggle all unchanged in hero mode). v3.1 polish: enhanced hero chrome leveraging the larger hero region. Options surveyed (none operator-confirmed): (a) larger model chip + verbose token meter, (b) inline last-N-stdout-lines preview pinned in the hero header, (c) per-hero "primary action" button (e.g., "send prompt to hero session"), (d) different border/shadow to visually distinguish hero from squad. Operator chose (a) same chrome for ship-minimum; (b)-(d) are v3.1 candidates. Closure path: extend TileProps with `isHero?: boolean` flag (tile-grid.tsx passes `idx === heroIndex` per session); TileHeader/TileFooter conditionally render enhanced sub-elements when `isHero === true`. Tier 3 — v3.0 ships visually consistent across all tiles; enhanced chrome is differentiation polish. Discoverability: this row + MB-T19 decisions doc Q-MBT19-8 + findings doc §VIII. | MB-T19 WB5 2026-05-07 |
| `MB-F-DOCS-BUILD-INDEX-MISSING` | **Tier 3 methodology**. CLAUDE.md §6 "Reference docs" lists `docs/build-docs/00_BUILD_INDEX.md` as a reference for "Registry binary build sequencing", but the file does NOT exist (verified via MB-T19 Phase 1 diagnose §I-C grep). Surfaced as R-MBT19-7 during diagnose. Effect: any session reading CLAUDE.md §6 + attempting to consult 00_BUILD_INDEX.md hits a missing-file dead end. Closure path: (a) author the file with the canonical Registry build sequencing per docs/build-docs/REGISTRY.md (operator-arbitrated authoring — frozen contract per CLAUDE.md §1); (b) remove the §6 reference if the index is not load-bearing; (c) replace the §6 reference with a pointer to docs/build-docs/REGISTRY.md if the binary build sequencing lives there instead. Tier 3 — methodology / discoverability concern, NOT a logic bug. Operator-arbitrated which closure path. Discoverability: this row + MB-T19 Phase 1 diagnose §I-C "NOT FOUND" surface + R-MBT19-7 disposition (NOTE IN FINDINGS). | MB-T19 WB5 2026-05-07 |
| `MB-F-T21-SMART-PIN-SCROLL` | Q-MBT21-8=a shipped naive `tailAnchorRef.current?.scrollIntoView({block:'end'})` on `[history.length, inProgress]`. Yanks scroll to bottom even if user has manually scrolled up to read history — interrupts reading flow. Closes by adding scroll-position detection (e.g., `isAtBottom = log.scrollTop + log.clientHeight >= log.scrollHeight - 50`) + only auto-scroll if user is within ~50px of bottom OR if user just submitted. Tier 3 — cosmetic UX polish. Discoverability: `packages/dispatch-workstation/src/coarchitect/chat-panel.tsx` scroll-pin useEffect + `docs/coordination/mb-t21-findings-2026-05-07.md` §III. | MB-T21 WB5 2026-05-07 |
| `MB-F-T21-QUICK-PICK-SCHEMA-FIRST-CLASS` | Q-MBT21-2=b shipped sentinel-marker parse (`QUICK_PICK: ["a","b"]` line at end of assistant message body) as renderer-only — no schema/IPC change. Once orchestrator output spec stabilizes (post-v3.0), upgrade to first-class field on `OrchestratorMessageRow` / `MultiChoiceCardOutputSchema` integration so quick-pick options flow as structured data instead of in-band content sentinel. **Operator-arbitrated frozen-schema change required** per CLAUDE.md §2.10. Tier 2 — paired with `MB-F-T21-SPAWNED-LIST-SCHEMA-FIRST-CLASS`. Discoverability: `packages/dispatch-workstation/src/coarchitect/chat-content-markers.ts:parseQuickPickMarker` + `docs/coordination/mb-t21-findings-2026-05-07.md` §VII-C. | MB-T21 WB5 2026-05-07 |
| `MB-F-T21-SPAWNED-LIST-SCHEMA-FIRST-CLASS` | Q-MBT21-3=b shipped sentinel-marker parse (`spawned: ["sess-a", "sess-b"]` line at end of assistant message body) as renderer-only. Same shape as `MB-F-T21-QUICK-PICK-SCHEMA-FIRST-CLASS`: post-v3.0 schema upgrade to first-class field on `OrchestratorMessageRow` for spawned-session lists. Operator-arbitrated. Tier 2 — paired with quick-pick schema upgrade. Discoverability: `packages/dispatch-workstation/src/coarchitect/chat-content-markers.ts:parseSpawnedMarker`. | MB-T21 WB5 2026-05-07 |
| `MB-F-T21-LATEST-ASSISTANT-INTERACTIVE-ONLY` | When multiple assistant messages with QUICK_PICK markers exist in history (operator scrolls up after several turns), ALL render interactive option buttons. Clicking an old option fires a fresh `sendAndStream` against current orchestrator state — likely confusing UX. Closes by tracking the latest-assistant-message id in chat-panel.tsx + rendering buttons disabled (or omitted) on older messages. Tier 3 — UX polish; not load-bearing. Discoverability: `packages/dispatch-workstation/src/coarchitect/chat-panel.tsx` history.map render path + WB3 commit `fd6b9c5` body. | MB-T21 WB5 2026-05-07 |
| `MB-F-T21-COMPOSED-MARKER-ORDERING` | Current chat-panel.tsx render order for assistant messages with both markers: `ChatBubble (stripped body)` → `SpawnedList` → `QuickPickButtons` (informational context first, action last). Operator may prefer alternate orderings (e.g., quick-pick above spawned-list, or spawned-list inline within bubble body). Closes by either confirming current order matches design intent OR refactoring chat-panel.tsx history.map JSX. Tier 3 — cosmetic; current order is reasonable default. Discoverability: `packages/dispatch-workstation/src/coarchitect/chat-panel.tsx` Fragment composition + `docs/coordination/mb-t21-findings-2026-05-07.md` §I. | MB-T21 WB5 2026-05-07 |
| `MB-F-T26-RATE-TABLE-PROFILE-MIGRATION` | MB-T26 WB2 ships `MODEL_RATES` as a hardcoded Record in `packages/dispatch-workstation/src/main/cost-calc.ts` per Q-MBT26-2=a operator-confirmed 2026-05-07 (single entry: `claude-sonnet-4-6` at $3/$15 per Mtok, [MODELED] from operator's knowledge cutoff Jan 2026). Acceptance criterion *"per-model rate table operator-editable in profile"* is structurally deferred to MB-T33 profile system. Closes by migrating MODEL_RATES const → profile YAML field readable by cost-calc.ts; cost-calc loads from profile at call-time with the hardcoded const as fallback (operator-experience continuity if profile YAML missing). Drift risk: if Anthropic changes published rates between Jan 2026 cutoff and MB-T33 ship, displayed cost diverges from actual billed cost (operator-observable via Anthropic console). Tier 2 — closes at MB-T33. Discoverability: `cost-calc.ts:18-22` MODEL_RATES const + `docs/coordination/mb-t26-findings-2026-05-07.md` §III + §VI Q-MBT26-2 row. | MB-T26 WB4 2026-05-07 |
| `MB-F-T26-COST-METER-INITIAL-MOUNT-EMPTY-STATE` | CostMeter's first paint shows '—' (em-dash placeholder) until the first /v1/messages call after page-load (push-based path) OR the initial-fetch via coarchitectBridge.onCostUpdate's internal `coarchitect:getDailyCost` invoke completes (~ms latency). For operators who restart Conductor mid-day after prior API calls today, the meter briefly shows '—' before snapping to the persisted today's-total. Acceptable for v3.0 per Q-MBT26-5=d push-based design (no idle polling); v3.1 polish if the brief flash bothers operators. Closes by either (a) rendering CostMeter's initial state as `$0.0000` instead of '—' (lies-down for new days, matches new-day true total), OR (b) blocking initial render until first onCostUpdate callback fires (introduces splash). Tier 3 — UX polish. Discoverability: `packages/dispatch-workstation/src/chat-shell/cost-meter.tsx` `formatCost(null)` + `docs/coordination/mb-t26-findings-2026-05-07.md` §III empty-state row. | MB-T26 WB4 2026-05-07 |
| `MB-F-T26-METHO-1-T21-T26-CROSS-SESSION-SWEEP` | Methodology incident #1 of the 4-session parallel-cairn run: Terminal A's `e02aa52 spike(MB-T21):` commit at 2026-05-07 captured Terminal C's untracked `docs/coordination/mb-t26-diagnose-2026-05-07.md` (Phase 1 diagnose authored independently in same shared working tree) under Terminal A's authorship. Per-path `git add` discipline (CLAUDE.md §2.7) failed at Terminal A's stage step (likely `git add -A` or `git add docs/`). Terminal C's atomic-chain caught the contamination at MY end (diff-verify aborted my Phase 1 commit before push); damage was at Terminal A's end. Disposition Q-MBT26-METHO-1=c (operator-arbitrated 2026-05-07): accept-as-is + file finding; no force-push, no revert. Phase 1 content is correct, attribution-only off. Tier 2 — methodology audit-trail finding. Discoverability: `git --no-pager show e02aa52 --stat` shows both `mb-t21-diagnose-2026-05-07.md` AND `mb-t26-diagnose-2026-05-07.md` in same commit + `docs/coordination/mb-t26-findings-2026-05-07.md` §V incident #1 row + §VIII.1 (validates `MB-F-PARALLEL-CAIRN-INDEX-RACE-ATOMIC-COMMIT` Tier 1 defense at later-committing session). | MB-T26 WB4 2026-05-07 |
| `MB-F-T26-Q-DISPOSITION-INFEASIBILITY-ROUND-2` | Methodology evidence: pre-arbitrated Q-dispositions can lock in technically-infeasible designs when the disposing parties (operator + arbitrating session) lack full surface knowledge at arbitration time. **Round 2 evidence at MB-T26 WB3:** operator confirmed Q-MBT26-5=c (polling via existing `coarchitect:` IPC, "preserves preload.mts UNCHANGED invariant") at HALT 0; mid-WB3 authoring revealed that preload.mts has NO generic `invoke(channel, ...)` method — every IPC channel is an explicit named method on `coarchitectBridge`. With Electron `contextIsolation: true`, the renderer cannot reach `ipcRenderer.invoke` directly. So a new `coarchitect:getDailyCost` handler is unreachable WITHOUT adding a new bridge method. Q-MBT26-5=c was infeasible-as-arbitrated. Halt + surface (CLAUDE.md §2.5 + §2.10 honored) led to operator re-disposition Q-MBT26-5=d (push-based via `onCostUpdate` method, +1 line preload.mts addition). Operator recognition: "Q-MBT26-5 re-disposition mid-arbitration was the right call." Methodology gap: §3.10 Q-disposition arbitration is brittle to surface-knowledge gaps — NOT a failure of §3.10, a documented failure mode of the primitive. Closes by adding a §3.10 amendment: arbitrating sessions MUST surface mid-WB if a Q-disposition discovered to be infeasible-as-arbitrated; re-arbitration is operator-arbitrated, not session-pivoted. Tier 2 — methodology hardening. Discoverability: `docs/coordination/mb-t26-findings-2026-05-07.md` §V row + §VIII.4 + WB3 commit `09b38ce` body. | MB-T26 WB4 2026-05-07 |
| `MB-F-PARALLEL-CAIRN-WORKING-TREE-BLOCKING` | **Tier 1 methodology — distinct shared-tree failure mode from index-race.** Index-race (`MB-F-PARALLEL-CAIRN-INDEX-RACE-ATOMIC-COMMIT` Tier 1) protects against sweeps from another session's pre-staged work between this session's diff-verify and commit. **Working-tree-blocking is the inverse:** another session's *uncommitted authored work* in a file this session needs to author against forces this session to either (a) sweep foreign content into own commit (same shape as e02aa52 in reverse), (b) halt until other session commits, or (c) pivot to git-worktree for structural isolation. Atomic-chain does NOT prevent this — diff-verify confirms paths, not content provenance, and the file is legitimately this session's territory to edit. **Validated at MB-T26 mid-WB3:** Terminal B's uncommitted MB-T22 WB2 (chat-shell.tsx + mount.ts) blocked Terminal C's WB3 design; Terminal C halted + surfaced; operator confirmed B's a08b406 had landed; resumed authoring against the committed shape. Workaround in shared-tree contexts: halt-and-coordinate. **Forward fix: git worktree-per-session** per CLAUDE.md §4.3 — only structural defense. Closes by either (i) operator pivots all parallel-cairn sessions to worktree isolation as next-dispatch-wave precondition (recommended; Round 3 trigger threshold met per operator note), OR (ii) protocol amendment requiring all sessions to commit-on-pause (no uncommitted dirty trees crossing session boundaries). Tier 1 — structural methodology. Discoverability: `docs/coordination/mb-t26-findings-2026-05-07.md` §V incident #2 + §VIII.2. | MB-T26 WB4 2026-05-07 |
| `MB-F-PARALLEL-CAIRN-SHARED-TREE-CONTENT-SWEEP` | **Tier 1 methodology — third distinct shared-tree failure mode, NEW at this WB4.** Both sessions have legitimate uncommitted edits to the SAME file. Each session reads, edits its own zone, runs atomic-chain. Diff-verify confirms the path IS expected (each session legitimately edits the file). But `git add <file>` captures the FULL working-tree state of the file — including the OTHER session's uncommitted edits. **Atomic-chain diff-verify protects path-set contention, NOT content provenance** when both sessions have uncommitted edits to the same file. **Incident SHA: 09b38ce** (Terminal C MB-T26 WB3) — preload.mts captured Terminal B's MB-T22 commits-bridge zone (lines 158-179) along with Terminal C's MB-T26 cost-meter bridge zone (lines 32-55). Terminal B's WB3 commit body (`427c6a3`) explicitly cites the `09b38ce content-sweep`. **Preconditions:** two sessions authoring sentinel zones in same file in same window. **Failure surface:** commit attribution captures content from both sessions; cairn audit trail anomalous. Terminal C's WB3 Q7 self-check claim *"Zero T22 territory bleed at MY end"* was [INACCURATE] in retrospect. **Forward fix: git worktree-per-session** per CLAUDE.md §4.3 — only structural defense; per-file edit isolation is workaround for shared-tree contexts (e.g., per-section read-back of file before stage to verify only own zones are dirty). Closes by operator pivoting parallel-cairn sessions to worktree isolation. Same shape as Terminal B's WB3 report named it. Tier 1 — structural methodology. Discoverability: `git --no-pager show 09b38ce -- packages/dispatch-workstation/src/main/preload.mts` shows both MB-T26 zone (32-55) AND MB-T22 commits-bridge zone (158-179) under MY commit + `docs/coordination/mb-t26-findings-2026-05-07.md` §V incident #3 + §VIII.3 + Terminal B's `427c6a3` commit subject `(preload.mts cross-session-swept at 09b38ce — incident cited)`. | MB-T26 WB4 2026-05-07 |
| `MB-F-§3.11-COURTESY-DELAY-HARNESS-INCOMPATIBILITY-ROUND-2` | **Tier 3 methodology — Round 2 evidence of harness-conditional §3.11 executability.** Operator-prescribed §3.11 courtesy delay (~30s halt before atomic-chain to reduce cross-session race probability) is harness-blocked across all forms in **Claude Code 2.1.133** for Terminal C's session: standalone `sleep 30`, chained `sleep 30 && ...`, chained `sleep 30 && echo`, all return *"Blocked: ... use Monitor with an until-loop"*. Terminal A in same Claude Code version executed `sleep` chains successfully — same Claude Code version, divergent harness behavior. **§3.11 is per-session-conditional, not universally executable.** Atomic-chain diff-verify (CLAUDE.md §3.9) is the load-bearing protection; §3.11 is probabilistic mitigation layered on top. Closes by §3.11 amendment in operator-side methodology doc (operator-only territory; Terminal C's WB4 just files this evidence row). Tier 3 — methodology hygiene. Discoverability: Terminal C's WB1+WB2+WB3 commit bodies cite the harness-block + the Round 2 evidence + `docs/coordination/mb-t26-findings-2026-05-07.md` §V row. | MB-T26 WB4 2026-05-07 |
