# Followup index

Consolidated index of post-MVP work filed across Phase Y + Phase Z by both
Session A (daemon + CLI) and Session B (web UI + menubar). Each entry
references its origin cluster/ticket; commit bodies have richer context.

Three priority bands: **MVP-required** (must ship for v2.0 — should be
empty if Phase Z closed cleanly), **Post-MVP / v2.1** (planned next
iteration), **Deferred / nice-to-have** (no current commitment).

Z-7 harvest snapshot: 18 distinct followups across both sessions. MVP-
required band empty as expected (Z-1 + Z-4 closed real bugs as part of
their own tickets; no MVP-blocker carried into Z-7).

---

## MVP-required

_(empty)_

If anything surfaces here at re-harvest, it represents work that should
have resolved before Phase Z cluster close.

---

## Post-MVP / v2.1

### Session A — daemon + CLI

| ID | Scope | Origin |
|---|---|---|
| `DAEMON-F-installer-cross-platform` | Linux / Windows installer paths (S04 documented darwin-only for v2.0) | D-6 / T18 |
| `DAEMON-F-installer-prebuilt-binary` | Bundle daemon as single-file executable for end users without Node.js installed | D-6 / T18 |
| `DAEMON-F-installer-code-signing` | Apple notarization for distribution outside this repo (S04 followup #2) | D-6 / T18 |
| `DAEMON-F-runtime-deps-hygiene` | `node-notifier`, `@fastify/websocket`, `ws` currently in devDependencies; move to dependencies pre-distribution | D-5 / T16 |
| `DAEMON-F-version-source` | `/v2/health` version is hardcoded `'0.0.0'`; read package.json dynamically at startup | D-5 / T16 |
| `DAEMON-F-watcher-registry-mutex` | Composite: `last_commit_sha` (T14) + `last_status_json_at` (T15) + possibly `last_handoff_pulled_at` written by manager via single mutex coordinated with route writes | D-4+D-5 |
| `DAEMON-F-last-commit-sha-update` | Subsumed by the composite mutex above | D-4 / T14 |
| `CLI-F-ci-setup` | CI matrix: daemon-up + daemon-down configs; runs the existing test suite under both per X2 line 361 | CLI-T / T05 |
| `CLI-F-probe-timeout-env` | `/v2/health` probe timeout (currently 500ms hardcoded) configurable via env var | CLI-T / T04 |

### Session B — web + menubar

| ID | Scope | Origin |
|---|---|---|
| `WEB-F-focused-card-highlight` | Visual refinement on focused-via-keyboard SessionCard | W-2 / WEB-T07 |
| `WEB-F-send-modal-error-differentiation` | Modal currently surfaces a single error region; differentiate validation vs network vs daemon-422 paths | W-3 / WEB-T11 |
| `UI-F-banner-copy` | Banner text refinement (Session B authoring decision) | W-1 |
| `UI-F-real-browser-modal` | Real-browser smoke test for SendPromptModal interactions (deferred per Session B's no-Playwright scope) | W-3 |
| `MB-F-pnpm-electron-packaging` | When COARCH-T03 reinstalls `@anthropic-ai/sdk`, re-spike packager against actual workspace dep tree. Decide between `node-linker=hoisted` (workspace-wide), `public-hoist-pattern` (local), or `electron-builder`. MB-S04 ADR's K6 claim (263 MB / 8.4 s) was sterile-spike evidence, not workspace-dep evidence. | MB-T01 green |
| `MB-F-mb-s04-spike-amendment` | Amend MB-S04 ADR §K6 to reflect spike-vs-reality gap discovered at MB-T01 green: sterile package.json doesn't surface pnpm symlink + `@electron/packager` prune walker incompatibility. Future packaging spikes must use actual workspace dep tree. | MB-T01 green |
| `MB-F-MB-T03-DOCK-BADGE` | Dock badge implementation deferred per RESOLUTION-2 (operator 2026-04-30). V3_TICKETS.md L120 "if relevant" language stripped from MB-T03 frozen surface (contract §4.6, OPEN-Q-C-2). Implement in a future ticket: `app.setBadgeCount(n)` for unread-action-count badge on the macOS dock icon. Requires daemon polling or SSE event to drive count updates. Non-blocking for v3.0 ship. | MB-T03 green |
| `MB-F-MB-T02-PRODUCTION-LOADING` | v3.0 ships dev-only `loadURL('http://localhost:7878')` per contract §3.4 (IMPORTANT-5 operator resolution 2026-04-30). Production-mode loading (static file fallback or packaged-app URL resolution) is out of scope for MB-T02. Implement in MB-T08 or a dedicated follow-on ticket. | MB-T02 green |
| `MB-F-MB-T02-DOM-ASSERTION` | Round 2 webview-loader test (webview-loader-callable.test.ts) verifies callability + non-crash; full DOM assertion of kanban columns (AWAITING REVIEW / STALE / RUNNING / IDLE headers, session cards) deferred until dispatch-web server runnable in test env (MB-T07 territory). | MB-T02 green |
| `MB-F-COARCH-T02-REAL-DAEMON-WIRING` | **CLOSED by COARCH-T03 7f15c78.** HttpDaemonClient wired in coarchitect-ipc.ts; mount.ts bridge adapter uses `window.coarchitectBridge`; AnthropicChatClient streams via Sonnet 4.6. Daemon persistence gracefully fails when daemon unreachable (synthetic local-only message fallback per HttpDaemonClient design). OPEN-Q-D-2 remains open for typed import resolution in a future ticket. | COARCH-T02 green → CLOSED COARCH-T03 |
| `MB-F-ZIPPER-2-ESM-PRELOAD` | Electron 41 sandboxed preloads cannot load ESM modules — `import` statements in `.mjs` files cause `SyntaxError: Cannot use import statement outside a module` in the sandbox context, regardless of `package.json` `"type":"module"`. Fixed in Zipper-2 by compiling `src/main/preload.mts` to `dist/main/preload.cjs` via esbuild `format:'cjs'`. All future preload scripts in this package must use CJS output. If Electron ever ships first-class ESM preload support, revisit and remove the `build-preload.mjs` script. | Zipper-2 green 039721f |
| `MB-F-ZIPPER-2-COARCH-T03-IPC` | **CLOSED by COARCH-T03 7f15c78.** coarchitect-ipc.ts now uses HttpDaemonClient (real /v3/* calls, graceful failure); sendAndStream one-way IPC handler added; preload exposes streaming bridge; mount.ts uses bridge adapter from `window.coarchitectBridge`. Integration test for real message persistence (daemon must be running) deferred to COARCH-T04+ scope. | Zipper-2 green 039721f → CLOSED COARCH-T03 |
| `MB-F-MB-T04-PROJECT-LIST` | MB-T04 ships file-dialog fallback only — V3_TICKETS.md L130 names a "configured project list" picker that depends on the settings UI (V3_TICKETS.md MB-T11 / W-T19). When MB-T11 lands, the spawn modal should grow a dropdown above the path input populated from `electron-store` (or equivalent) with the operator-configured project paths; current text input + Browse… stay as the manual entry / fallback. | MB-T04 green |
| `MB-F-MB-T04-INITIAL-PROMPT` | WORKSTATION_CONTRACT.md §3.3 spawn-new-session row lists "optional initial prompt" in the payload. MB-T04 modal does NOT surface this field; IPC payload shape committed as `{repoPath, sessionName}`. If MB-T05 needs it, extend modal with a multi-line textarea + extend payload to `{repoPath, sessionName, initialPrompt?: string}` and add a v3-schema entry for the IPC channel under §2.1. Tier-2 because behavior under absence is well-defined (claude starts with no prompt; operator types one in the spawned pane). | MB-T04 green |
| `MB-F-MB-T04-PAYLOAD-VALIDATION` | spawn-ipc.ts main-process listener accepts payload as `unknown` and only validates shape implicitly via JSON.stringify (test-hook only). MB-T05 should add a Zod schema (likely `WorkstationSpawnRequestedSchema` in dispatch-core/src/v3/schema.ts §2.1 IPC section) and validate at the main-process boundary, surfacing `WorkstationError` on shape mismatch per WORKSTATION_CONTRACT.md §6.5. Until then, a malformed renderer could pass non-string repoPath/sessionName. | MB-T04 green |
| `MB-F-MB-T04-EMPTY-INPUT-UX` | Spawn confirm button is a silent no-op when either input is empty (after `.trim()`). Acceptable for MB-T04 ship per ticket scope, but a future UX pass should add inline validation (red border + helper text) or button-disable-when-empty similar to MB-T06's at-cap disable pattern. Tier 3 — non-blocking. | MB-T04 green |
| `MB-F-CONSOLE-T01-60S-SOAK` | CONDUCTOR_API_CONTRACT.md §4.7.7 explicitly defers a 60-second sustained backpressure soak test to CONSOLE-T01 (then continues to defer it). Vision §10.10 ship-gate names "60 s sustained streaming" as a criterion. MB-S06 §4 KNOWN backpressure at 2-second burst; converting MODELED→KNOWN at 60-second scale requires a real-tmux soak harness. Land before v3.0 ship-gate sign-off. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-STDIN-SEQ-PERSIST` | §4.7.1 says "Counters are session-scoped and persist across daemon restarts via the cc_console_buffer table". cc_console_buffer stores stdout rows only; CONSOLE-T01 ships stdin_seq as in-memory only (resets at daemon restart). Add a `cc_console_state` table (or extend cc_console_buffer with a direction column) to persist stdin_seq across restarts. Operator-impact is minor (stdin_seq is for write-ack correlation; clients don't typically rely on its monotonicity across restart) but the contract literal wording does require this. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-BUFFER-ENABLED-PERSIST` | vision §10.5 lists buffer_enabled as operator-configurable. CONSOLE-T01 hardcodes default = true, no toggle endpoint. Settings UI ships in MB-T11/W-T19; the daemon-side persistence (per-session buffer_enabled column in cc_console_state) lands alongside it. Tier 2. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-PIPE-PANE-HIGH-WATER-PAUSE` | §4.7.3 says PTY reader "pauses if no subscribers remain AND ring buffer hits its high-water mark". CONSOLE-T01 ships always-keep-reading even with zero subscribers (simpler upper bound; memory bounded by cap-and-prune loop). Optimization: pause pipe-pane when zero subscribers AND buffer ≥ 0.95 × cap, resume on subscribe. Saves CPU under always-on daemon scenarios. Tier 3. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-AUDIT-LOG` | vision §10.11 Q5 + §4.7.7 defer audit-log integration for operator-typed prompts to v3.x (separate `console_prompts` table). Each POST /v3/sessions/:name/console/stdin should log to the audit table per §7.8 audit discipline. Pending v3.x amendment to §4.7. Tier 3. | CONSOLE-T01 green |
| `MB-F-CONSOLE-T01-TMUX-HISTORY-LIMIT-CAP` | MB-S06 §4 finding: tmux's pane history-limit (default 2000 lines) duplicates retention with the daemon ring buffer. CONSOLE-T01 should call `tmux set-option -t <target> history-limit N` (small N, e.g. 100) per session at attach time so tmux does not duplicate-store the same lines the daemon has cached. Saves a few tens of MB per session in steady state. Tier 3. | CONSOLE-T01 green |
| `MB-F-MB-T05-PAYLOAD-VALIDATION` | spawn-ipc.ts `workstation:spawn-requested` listener still accepts `payload as SpawnSessionRequest` without runtime shape validation (cast trust). The MB-F-MB-T04-PAYLOAD-VALIDATION followup tracked this for MB-T05; MB-T05 GREEN preserved the cast pattern rather than introducing a Zod dependency. Add `WorkstationSpawnRequestedSchema` to `dispatch-core/src/v3/schema.ts` §2.1 IPC-section and validate at the main-process boundary, surfacing typed `WorkstationError` on shape mismatch per §6.5. Tier 2 — non-blocking but a malformed renderer could still pass a non-string repoPath/sessionName today. | MB-T05 green |
| `MB-F-MB-T05-INITIAL-PROMPT` | WORKSTATION_CONTRACT.md §3.3 lists "optional initial prompt" in the spawn-new-session payload. MB-T05 ships without it (matches MB-T04 modal which omits the field per MB-F-MB-T04-INITIAL-PROMPT followup). When operator wants initial-prompt support: extend SpawnSessionRequest with `initialPrompt?: string`, pass it to claude via tmux `new-session ... claude --prompt <text>` (or paste-buffer it after a brief delay if claude lacks a flag). Tier 2 — defer until ratified UX. | MB-T05 green |
| `MB-F-MB-T05-API-KEY-SETTINGS-UI` | **PARTIALLY CLOSED by MB-T08 d39f35d** (initial-entry path): MB-T08's onboarding flow now invokes `saveApiKey` from `src/onboarding/api-key-storage.ts` which calls `safeStorage.encryptString(plaintext)` and writes the buffer to `<userData>/anthropic-api-key.enc`; spawn-ipc.ts:defaultSpawnHandlerDeps reads it via `loadEncryptedApiKey`. Replacement-key UI for an already-onboarded operator (change key from settings panel without re-running onboarding) remains MB-T11/W-T19 scope. Tier 3 (downgraded from Tier 2 after MB-T08 closed the operator-blocker). | MB-T05 green → MB-T08 partial close |
| `MB-F-MB-T05-CLAUDE-FLAG-SUPPORT` | spawn-handler.ts assumes `tmux new-session ... claude` — i.e. claude with no flags. Real-world usage may want `claude --resume <id>` (continue conversation), `--model <id>`, or `--mcp-config <path>`. Extend SpawnSessionRequest with `claudeArgs?: string[]` and append to the argv. Tier 3 — defer until operator dogfood surfaces a need. | MB-T05 green |
| `MB-F-MB-T05-DAEMON-409-VERBATIM` | defaultRegisterSession surfaces 409 as SessionAlreadyRegistered with a generic message; the daemon's actual 409 body (per CONDUCTOR_API_CONTRACT.md §4.3 + Blocker 3 verbatim "Session name in use (killed record exists). Pick a new name.") is more useful for renderer UX. Forward the daemon body's `error` field through to the typed error envelope so the renderer can surface "Pick a new name" verbatim. Tier 3. | MB-T05 green |
| `MB-F-MB-T05-PRE-EXISTING-VALIDATOR-IMPORT` | Pre-existing failure observed during MB-T05 baseline: `test/unit/coarch-t04/build-doc-validator.spec.ts` fails to import `dispatch-core/src/v3/schema.js` in this worktree (build artifact missing — `dispatch-core` isn't compiled into `dist/` here). NOT MB-T05 territory; flagged for COARCH-T04 owner / build-system owner to investigate whether `dispatch-core` needs `pnpm -r build` before workstation tests, or whether the import path should resolve to `.ts` source. Tier 3. | MB-T05 baseline observation |
| `MB-F-MB-T08-ONBOARDING-RENDERER-MOUNT` | OnboardingModal React component lives at `src/onboarding/onboarding-modal.tsx` and is unit-tested in isolation (cluster 2 — 19 specs). The main-process IPC layer is wired (`workstation:onboarding-save-api-key` + `workstation:onboarding-complete` in main.ts d39f35d). What's missing: bundling the component into the production renderer + mounting it conditionally on first launch. Mirrors the `build-coarchitect.mjs` / `build-console-panel.mjs` esbuild pattern; needs a new `build-onboarding.mjs`, an `onboarding.html` page, and a `mount.tsx` entry. Without this, vision §8.1 ship-gate "First-launch flow walks operator through API key entry" is not satisfied for real operators (the persistence layer + smoke harness driver work, but the user-visible modal does not render in production). **Tier 1 — vision §8.1 ship-gate blocker.** | MB-T08 green |
| `MB-F-MB-T08-VISION-PROJECT-LIST-CONFIG` | Vision §8.1 (RATIFIED) onboarding requirement names two components: API key entry **AND** project list config. MB-T08 ships the API-key path; project-list config is not in MB-T08 scope per the ticket prompt's OOS line ("Project-list management UI" → MB-F-MB-T04-PROJECT-LIST + MB-T11/W-T19 settings UI). The acceptance gap: vision §8.1 ratified text expects both in the first-launch flow. Two operator paths: (a) accept the gap as a v3.0.x followup and ship MB-T08 satisfying API-key only; (b) extend MB-T08 onboarding with a project-picker step that captures the operator's first repo path before completion. Path (a) is the default unless operator arbitrates otherwise. **Tier 1 — vision §8.1 ship-gate decision.** | MB-T08 green |
| `MB-F-MB-T08-SPAWN-RESULT-SENTINEL` | The smoke harness's `spawnSession()` waits for `SPAWN_MODAL_OPENED` only — it cannot wait for spawn completion because no existing renderer-side subscription to `workstation:spawn-result` emits a console sentinel that the main.ts MB_TEST_HOOKS forwarder allowlist could surface. Wiring this requires (1) renderer-side subscription to `workstation:spawn-result` in `workstation-shell.html` (or a new preload bridge), (2) a `console.log('SPAWN_RESULT_OK')` on success / `SPAWN_RESULT_ERROR <type>` on failure, (3) extension of main.ts:54-72 forwarder allowlist. Crosses MB-T05 spawn-* territory which was OOS for MB-T08; defer to a small follow-on ticket so the smoke harness can validate the full spawn round-trip. Tier 2. | MB-T08 green → ENABLER RESOLVED Fix-B batch fix-batch-1 at `07fae77` (cairn finding #83). Items (1)–(3) now ship: `workstationBridge.onSpawnResult` subscribes via the new `spawn-result-listener.ts` helper; `workstation-shell.html` emits `SPAWN_RESULT_OK <sessionName>` / `SPAWN_RESULT_ERROR <error_type> <message>` console sentinels; main.ts SPAWN_RESULT_SUBSCRIPTION sentinel region forwards them under `MB_TEST_HOOKS=1`. The trivial follow-on (extending `smoke-harness.ts:spawnSession()` to `await this.ctrl.waitForSentinel('SPAWN_RESULT_OK …')`) is a single-line change against the now-shipped enabler — deferred per Fix-B scope-adjustment Option A (smoke-harness.ts is read-only in Fix-B file-ownership scaffold §1; not bundled in same patch). Live integration drive observed `SPAWN_RESULT_ERROR DaemonUnreachable …` end-to-end during Fix-B verification. Closing the smoke-harness consumer side is a sub-1-LOC next ticket. |
| `MB-F-MB-T08-SMOKE-CI` | Smoke harness ships at `src/main/run-smoke.ts` + `pnpm smoke` script. CI integration is operator territory and out of scope per the MB-T08 ticket prompt OOS line. Future v3.x work: GitHub Action that runs the smoke harness against a built `.app` on macOS runners with a placeholder API key + ephemeral daemon. Documented dependencies: a built `dist/`, a running daemon (or a daemon-less smoke variant that asserts onboarding-only). Tier 3. | MB-T08 green |
| `MB-F-MB-T08-MODAL-NOT-DISMISSIBLE-MID-FLOW` | OnboardingModal is intentionally not dismissible mid-flow (see test_onboarding_modal_dismissible_on_complete.spec.tsx rationale). Operators on a first-launch attempt who realize they don't have an API key cannot back out cleanly — they must close the entire app via Cmd+Q and relaunch later (which will re-trigger onboarding because config wasn't written). UX refinement: add a "Quit" / "Skip for now (limited functionality)" affordance on the welcome step. Tier 3 — defer until operator dogfood surfaces friction. | MB-T08 green |
| `MB-F-MB-T07-CARD-BRIDGE-PRELOAD-WIRING` | MB-T07 GREEN ships the dispatch-web side (OrchestratorCard component, card-ipc-bridge.ts emitters, useOrchestratorCards hook) and the workstation card-ipc.ts handler factory + pure audit-row builders. The Electron contextBridge wiring that exposes `window.cardBridge` to the dispatch-web webview is NOT yet landed: the webview needs its own preload script (the wrapper's preload.cjs runs in workstation-shell.html context, NOT in the nested `<webview>` for dispatch-web at WEB_UI_URL). Land a small `card-bridge.ts` factory (pattern after console-bridge.ts) plus a webview-preload script set via `<webview preload="...">` attribute in workstation-shell.html. Until then, dispatch-web's `window.cardBridge` is undefined and the bridge no-ops gracefully. Tier 1 — must ship before operator sees orchestrator-driven cards. | MB-T07 green → RESOLVED batch-6 Session B at 7217e66 (live integration dogfood-validated post-merge) |
| `MB-F-MB-T07-MAIN-IPC-WIRING` | `registerCardIpcHandlers` in src/main/card-ipc.ts takes an injected `ipcOn` for testability; production needs to register against `ipcMain.on` in main.ts alongside `registerIpcHandlers()` / `registerSpawnIpcHandlers()` / `registerConsoleIpcHandlers()`. Trivial wire-up. Tier 1 — bundles with the preload wiring above. | MB-T07 green → RESOLVED batch-6 Session B at 28f55c5 (extracted to card-wiring.ts; main.ts edit is the sentinel-region call) |
| `MB-F-MB-T07-DAEMON-AUDIT-CLIENT` | `card-ipc.ts` depends on a `DaemonAuditClient { postAudit(req) }` interface; production needs `HttpDaemonClient.postAudit(req: OrchestratorAuditWriteRequest)` that POSTs to `/v3/orchestrator/audit` (the daemon endpoint shipped by COARCH-T01). Mirror the existing `postMessage` pattern in HttpDaemonClient (graceful fallback on daemon-unreachable). Tier 1 — bundles with the wiring above. | MB-T07 green → RESOLVED batch-6 Session B at fc1d57c (pure-helper postAuditViaFetch + class wrapper; 5/5 unit tests cover success/401/422/ECONNREFUSED/no-token) |
| `MB-F-MB-T07-CARD-CONTEXT-CACHE` | `CardContextLookup` in card-ipc.ts is dependency-injected; production needs an actual cache populated when the shell emits `orchestrator-card-rendered` to the webview. The cache stores per-card context (trigger_event, build_doc_id, build_doc_commit_sha, output_payload, superseded_card_ids) so the audit-write path can reconstruct a complete `OrchestratorAuditWriteRequest` on Approve/Decline/multi-choice. Lands alongside MB-F-MB-T07-ORCHESTRATOR-CARD-EMITTER below. Tier 1. | MB-T07 green → RESOLVED batch-6 Session B at e933498 (CardContextCache class + cardContextCache singleton; get returns null per coord §4.4 not undefined as brief said) |
| `MB-F-MB-T07-ORCHESTRATOR-CARD-EMITTER` | The shell-side code that parses structured `CardOutput` / `MultiChoiceCardOutput` from the orchestrator's stream (`coarchitect-ipc.ts` `coarchitect:sendAndStream` path) and emits `orchestrator-card-rendered` IPC messages to the webview is NOT in MB-T07 scope. Currently the orchestrator's structured output surfaces as text in the chat panel only. A follow-on ticket (likely COARCH-T0x territory) parses `OrchestratorOutputSchema` discriminated union out of the model response, validates via Zod, and routes `card` / `multi-choice-card` outputs to the cardBridge while keeping `escape-block` in the chat panel and `action` (read-only `read-file`) firing-without-card per cairn-Sonnet §2.7. Tier 1 — without this MB-T07's webview surface has no input. | MB-T07 green → RESOLVED batch-6 Session B at f8c57f7 (orchestrator-output-router.ts pure variant routing + coarchitect-ipc.ts integration; emits orchestrator-card-rendered to webContents on card/multi-choice-card variants) |
| `MB-F-MB-T07-KANBAN-COLUMN-INTEGRATION` | `OrchestratorCardsLane` ships as the existing-column placement helper per WC §5.5 but is NOT yet wired into `KanbanColumn.tsx`. Wire it so `awaiting_review` column hosts the `awaiting` lane below its session cards and `stale` column hosts the `stale` lane below its session cards. Per WC §5.5: orchestrator cards plug into existing columns; per WC §5.2: subtle blue tint visual distinction (already in OrchestratorCard). Tier 2 — depends on the cardBridge being wired (above) so awaiting/stale buckets actually populate. | MB-T07 green |
| `MB-F-MB-T07-AUDIT-SUPERSESSION-SEMANTICS` | When a card is auto-superseded WITHOUT operator response (orchestrator-card-superseded arrives before any operator click), no audit row is written today. The `/v3/orchestrator/audit` endpoint is POST-only (no UPDATE) and `staleness_status` is set at insert time. Operator arbitration needed: should the shell write an audit row at supersession time with `operator_response='pending'` + `staleness_status='superseded'` so the audit log captures the supersession event? Or is no row by design (only operator-acknowledged supersessions are audited)? Surface for ratification — the v3.0 contract is silent on this case. Tier 2 — non-blocking for MB-T07 unit-test scope; needed before audit-log retention queries are operator-facing. | MB-T07 green |
| `MB-F-MB-T07-E2E-INTEGRATION` | V3_TICKETS.md MB-T07 acceptance lists "end-to-end test where operator uploads build doc, orchestrator generates action proposal as kanban card, operator approves, action fires, daemon-side state changes, audit row written". MB-T07 GREEN ships unit-test coverage of the seams (component, bridge, audit-row builders, IPC handlers — 35 web tests + 28 workstation tests, 226/226 web suite + 28/28 workstation MB-T07 suite). End-to-end with live orchestrator + live daemon belongs to Phase 3 dogfood validation per the redraft prompt's note. Land an integration-test harness in test/integration/ that spawns daemon + workstation + mocks Anthropic Sonnet 4.6 to drive the full flow. Tier 2 — Phase 3 ship-gate. | MB-T07 green |
| `MB-F-MB-T07-MULTI-CHOICE-OPTION-LABELS` | `OperatorResponseEnum` lists `multi-choice-A`..`multi-choice-D` but the v3 schema's `MultiChoiceCardOutputSchema.options` is `z.array(z.string()).min(2).max(4)` (just strings). Today `OrchestratorCardsLane` renders option text as button labels; the audit row records `multi-choice-A` etc. for routing. If the orchestrator wants to disambiguate "operator picked the option labeled X" vs "operator picked the option at index 0", a future schema amendment may add structured option records `{label: string, id: string}`. Out of scope for v3.0. Tier 3. | MB-T07 green |
| `MB-F-MB-T07-DISMISSED-CARD-RETENTION` | After Decline + audit-write confirmation, `is_dismissed` shows the card faded with a "Declined" badge. Parent state owns garbage-collection. Today there's no GC: dismissed cards live forever in the `dismissed` bucket of the card-state reducer. Add a TTL (e.g. clear after 30s) or operator-controlled clear ("Hide declined") so dismissed cards don't accumulate over a long session. Tier 3. | MB-T07 green |

| ID | Scope | Origin |
|---|---|---|
| `DAEMON-F-backpressure-integration` | Real slow-client browser smoke for T12's WS backpressure (force-disconnect at 1009); current coverage is unit-only | D-4 / T12 |
| `DAEMON-F-git-recursive-branches` | Support slash-bearing branch names (`feature/foo`); requires recursive `fs.watch` (S02 flagged macOS-fragile) | D-4 / T14 |
| `DAEMON-F-watcher-fsevents-smoke` | Real `fs.watch` integration smoke test (T13/T14/T15 currently mock-factory only) | D-4 |
| `DAEMON-F-watcher-real-git-smoke` | Real `git init` + commit roundtrip integration test for T14 git watcher | D-4 / T14 |
| `DAEMON-F-watcher-status-real-fs-smoke` | Real `STATUS.json` write + watch integration test for T15 | D-4 / T15 |
| `DAEMON-F-status-json-configurable-path` | Per-session configurable STATUS.json location (currently `<cwd>/STATUS.json`) | D-4 / T15 |
| `DAEMON-F-test-contention` | Investigated only if recurs: T14 P2 git-watcher debounce flaked once under cross-package pnpm recursive contention | D-4 / T14 |
| `DAEMON-F-health-response-schema-consumption` | Session B's UI consumption of `notifications_available` flag from `/v2/health` (Session B territory) | cross-session |
| `CLI-F-real-daemon-state-isolation` | Z-4 spawnRealDaemon writes to `~/.foxworks-dispatch/*` (operator state); post-MVP support test-isolated paths via env-var override or daemon CLI args | Z-4 |
| `CLI-F-send-pull-real-env-smoke` | Z-4 deferred fd send + fd pull from smoke coverage (need tmux pane + HANDOFF.md fixture) | Z-4 |
| `UI-F-color-audit` | Cross-component color usage audit (Session B authoring decision) | W-* |

---

## Numbering convention

Followups carry a domain prefix matching their package owner:

- `DAEMON-F-*` — `packages/dispatch-daemon/`
- `CLI-F-*` — `packages/dispatch-cli/`
- `WEB-F-*` — `packages/dispatch-web/`
- `UI-F-*` — Session B's UI surface broadly (web + menubar)
- `MB-F-*` — `packages/dispatch-workstation/` (v3.0 Workstation surface)

Origin column references the cluster ID (`D-1` through `D-6`, `W-1`
through `W-5`, `CLI-T`, `Z-*`) plus the originating ticket where
unambiguous. Commit bodies in the originating commits carry full
context; this index is a scan target, not a substitute.
| `MB-F-COARCH-T04-BUILD-DOC-SETTINGS-UI` | Build-doc config (repoRoot / relativePath / allowedScopes) is persisted via `setBuildDocConfig` IPC but there is no renderer UI to set it. Operator must call the bridge directly from DevTools or a future settings panel. Wire into MB-T11 (broader settings page) when that ticket lands — expose a "Build doc path" section with repo picker + relative path input + allowed-scopes textarea. | COARCH-T04 green |
| `MB-F-COARCH-T04-DAEMON-STATE-TIER3` | Context builder Tier 3 (daemon state — active RUNNING/IDLE sessions) is passed as `null` in coarchitect-ipc.ts pending a daemon sessions list endpoint. When the daemon exposes `/v3/sessions` or equivalent, fetch active sessions and populate `daemonState` in the `buildContext()` call. Contract: `DaemonStatePayload = { sessions: Array<{id, name?, status?}> }`. | COARCH-T04 green |
| `MB-F-COARCH-T04-CONTEXT-HALT` | **ARBITRATED 2026-05-02 by operator: full-doc injection per build-doc-schema-spec.md.** Original ambiguity: build-doc-schema-spec.md says full-doc; V3_TICKETS.md Tier C wording said "relevant ticket section only." Operator chose full-doc — context-builder.ts no longer has open question; comment in source should be updated by next coarchitect-touching ticket.  → CLOSED | COARCH-T04 green |
| `MB-F-COARCH-T02-STYLING` | Chat panel renders functional but unstyled — D shipped UI scaffold only per COARCH-T02 contract scope; needs production-quality CSS matching modern LLM chat aesthetic (Claude/GPT/Gemini-style message bubbles, role-based layout, input affordances). Operator-arbitrated new ticket required (visual reference + frozen exports + acceptance criteria with operator manual gate). | Round 2 Zipper-2 manual gate |
| `MB-F-DISPATCH-WEB-AUTH-PERSISTENCE` | Workstation Electron BrowserWindow session shows dispatch-web "Conductor authentication" screen on every fresh launch; Electron session has no relationship to user's regular browser auth state. Architectural decision needed: (a) IPC token-injection at workstation launch from `~/.foxworks-dispatch/token`, (b) auth-bypass for workstation context, (c) accept as workstation onboarding step. Link to MB-T08 (production-mode webview) territory. | Round 2 Zipper-2 manual gate |
| `MB-F-COARCH-T02-DEFAULT-LAYOUT` | Default `chat-region` height of 280px in workstation-shell.html may not be optimal default; revisit when styling lands and visual proportions become design-decision-able. | Round 2 Zipper-2 manual gate |
| `MB-F-COARCH-T04-COST-REVALIDATION` | Operator arbitration on MB-F-COARCH-T04-CONTEXT-HALT (2026-05-02) selected full-doc injection over ticket-section-only. MB-S01 ADR cost projections were measured against ticket-section-sized contexts; full-doc context is materially larger per call. Re-validate cost projections under full-doc conditions before v3.0 ship-gate. Block: take a representative build doc (e.g., V3_TICKETS.md), run through the actual context-builder pipeline, measure input token count per Sonnet call, re-project at 100/200/500 calls/day. Compare against MB-S01 ADR baseline. If material delta, surface to operator for v3.x cost mitigation decision. | COARCH-T04 a2424f8 |
| `MB-F-CONSOLE-T02-RECONNECT-BACKOFF` | Cluster 2 GREEN ships a fixed 1-second reconnect delay with infinite retries on non-terminal close codes. Production-grade behavior should be exponential backoff (e.g., 1s/2s/4s/8s capped at 30s) with a max-attempts ceiling that surfaces a terminal `console:error` after N failed reconnects. The fixed-1s loop is fine in dev; under daemon outages it would generate a steady connection storm. Tier 2 — land before v3.0 ship-gate so operator-experience under daemon-down scenarios is acceptable. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-PANEL-CAP-SETTINGS` | Vision §10.11 Q3 ratifies cap=4 in v3.0 and notes "configurable in settings". CONSOLE-T02 ships DEFAULT_PANEL_CAP=4 with `panelCap` constructor option as the test seam; settings UI integration lands with MB-T11/W-T19. When that ticket lands, expose a "Max simultaneous CC consoles" numeric input that persists to a shared settings store and threads through to ConsoleIpcController construction. Tier 2. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-V3-SCHEMA-INTEGRATION` | WORKSTATION_CONTRACT.md §7 Schema enforcement says IPC message types are part of the v3 Zod schema in `dispatch-core/src/v3/schema.ts`. CONSOLE-T02 defines the seven console:* payload shapes locally in console-bridge.ts (ConsoleBridge interface) and console-ipc.ts (controller methods). Promote these into dispatch-core/src/v3/schema.ts so message-bus boundaries can validate per WORKSTATION_CONTRACT §7.3. Tier 2 — land before v3.0 ship-gate so the IPC layer matches the schema-enforcement invariant. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T02-INTEGRATION-TEST` | All CONSOLE-T02 tests are unit-only: pure DI, no Electron spawn. Add a spawn-electron integration test pattern (per coarch-t03 streaming-renders.spec.ts) that exercises the full window.consoleBridge → ipcMain → mock-daemon round-trip. Mock daemon can be a vitest setup that runs a small ws + http server. Lands as part of CONSOLE-T03 (which adds the renderer-side panel mount that the integration test needs to drive) OR as a dedicated post-CONSOLE-T03 spec file. Tier 2 — needed before v3.0 ship-gate so the integration boundary is validated at runtime, not just at the unit-test mock boundary. | CONSOLE-T02 green |
| `MB-F-CONSOLE-T03-MENU-SUBSCRIPTION` | **PARTIALLY CLOSED by Fix-C (cairn finding #82 PARTIAL-RESOLVED at 320f707).** Subscribe wiring shipped: `subscribeConsoleMenuToDaemon` in `console-mount.ts` ships the operator-arbitrated hybrid pattern (bootstrap GET /v2/sessions + WS /v2/events/stream as "something changed" trigger + 150ms debounce). All 6 helper unit tests + live-daemon integration GREEN (`refreshMenu(["newTest1"])` correctly invoked). Visual closure NOT achieved — finding #89 (`MB-F-WORKSTATION-MENU-REBUILD-NO-OP`) discovered during Fix-C integration verification: `Menu.setApplicationMenu` post-ready does not propagate to the macOS menu bar regardless of subsequent `rebuildApplicationMenu` calls. Full closure of this followup gated on resolution of #89. Original triage retained for context: The CC Console native menu is rebuilt on demand via `refreshConsoleMenu(sessions)` in main.ts but the daemon-session-list subscription (so the menu auto-refreshes when /v2/sessions changes) is unwired. Initial app start hands `[]` so the menu shows the disabled "No sessions registered" placeholder until something calls `refreshConsoleMenu(['s1', 's2'])`. Wiring options: (a) periodic GET /v2/sessions poll (simple, latency-bounded), (b) /v2/events/stream subscription (push-based, lower-latency). Recommend (b) since the daemon already streams events; this matches the kanban refresh pattern. Tier 2 — land before v3.0 ship-gate so the operator-driven menu open path actually surfaces sessions. | CONSOLE-T03 green → PARTIALLY CLOSED Fix-C `320f707` (gated on cairn #89) |
| `MB-F-CONSOLE-T03-MULTI-PANEL` | CONSOLE-T02 ConsoleIpcController tracks up to `panelCap` panels (default 4 per vision §10.11 Q3) and forwards events independently per session. The CONSOLE-T03 React surface, however, is single-panel: state holds one `sessionName`, opening a second session rebinds (overwrites) the first in the visible UI. Multi-panel rendering needs either tabs, a stacked column layout, or a panel-list state. Vision §10.10 ship-gate: "Two simultaneous CC-console panels bound to different sessions both render correctly with no cross-talk." Land before v3.0 ship-gate. Approach: change PanelState from `{sessionName, gap, error}` to `Map<sessionName, PerPanelState>`, render a tab strip, use the controller's panelCount() as the source of truth. Tier 2. | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-SHELL-INTEGRATION` | dist/console-panel/ produces a standalone renderer (console-panel.html + renderer.js + xterm.css). The running Workstation shell (workstation-shell.html) does NOT yet load this renderer — clicking "CC Console > [session]" in the native menu calls `controller.openConsolePanel(name)` which fires the shell→webview `console:open` event, but the shell HTML has no `#console-root` and no `<script src=…/console-panel/renderer.js>`. The panel therefore renders nowhere visible in v3.0 ship without this last-mile wiring. UX placement is an operator decision: (a) embed alongside chat panel as a third region in the splitter, (b) replace chat panel via tab strip, (c) separate Electron BrowserWindow opened on demand. Recommend (a) for v3.0 simplicity. Tier 1 — blocks the v3.0 §10.10 ship-gate ("operator opens CC-console panel for a registered session"). | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-XTERM-BUNDLE-SIZE` | dist/console-panel/renderer.js is 1.5 mb because esbuild bundles @xterm/xterm + react + react-dom + console-bridge types inline. Acceptable for v3.0 (Workstation is desktop-installed, not over-the-network), but a future optimization could (a) externalize react/react-dom and serve from a shared chunk, (b) tree-shake xterm to drop unused addons, (c) use xterm's plain-text mode if ANSI fidelity is not required. Tier 3 — non-blocking; revisit during a packaging pass. | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-OPEN-TRIGGER-CARD-BUTTON` | Vision §10 implies the operator may want to open a console panel from a kanban session card too (not just the native menu). v3.0 ships menu-only. Adding a per-card "Open console" button in dispatch-web requires (a) dispatch-web UI changes, (b) a webview→shell IPC channel for "request console open" since dispatch-web runs in the embedded webview not the shell renderer. Tier 3 — operator can already open via the native menu; the per-card button is a UX nicety. | CONSOLE-T03 green |
| `MB-F-CONSOLE-T03-DAEMON-RESTART-RECONNECT` | Vision §10.10 ship-gate: "Daemon restart mid-stream cleanly reconnects WebSocket and backfills missed STDOUT from daemon ring buffer with no operator-visible gap beyond reconnection latency." CONSOLE-T02 cluster 2 ships fixed-1s reconnect on non-terminal close codes; the renderer panel does NOT explicitly visualize the reconnect-pending state. Add a transient "Reconnecting…" banner (similar to the gap-warning banner) that surfaces when the bridge fires a console:error and clears when the next console:stdout-chunk arrives. Tier 2 — needed for §10.10 ship-gate compliance. | CONSOLE-T03 green |
| `MB-F-MB-T06-CAP-DEFAULT-CONFIRM` | MB-T06 ships DEFAULT_SESSION_CAP=5 in `packages/dispatch-workstation/src/main/session-cap.ts` per ticket-prompt fallback. Vision §10.11 Q3 ratifies a 4-5 sustained-active range; MB-T06 selected 5 without operator arbitration. Confirm the 5 default (or override to 4) before v3.0 ship. Tier 3 — both 4 and 5 are within the ratified range; this is a default-policy confirmation, not a behavior bug. | MB-T06 green |
| `MB-F-MB-T06-CAP-SETTINGS` | Concurrent-session cap is hardcoded to DEFAULT_SESSION_CAP=5 with `sessionCap` constructor option as the test seam. Settings-UI integration lands with MB-T11/W-T19. When that ticket lands, expose a "Max concurrent sessions" numeric input that persists to a shared settings store (electron-store per WORKSTATION_CONTRACT.md §8.1) and threads through into `defaultSpawnHandlerDeps` construction in spawn-ipc.ts. Tier 2 — operator-configurability is part of the §8.1 settings surface. | MB-T06 green |
| `MB-F-MB-T06-CAP-STATE-FILTER-PRECISION` | `isActiveSession` in session-cap.ts filters out `state === 'archived'` and `state === 'killed'` only. V3_TICKETS.md MB-T06 specifies "RUNNING + IDLE count toward cap; AWAITING REVIEW, STALE, KILLED, archived do not" — strictly, AWAITING_REVIEW + STALE should also be filtered. The current first-pass filter therefore over-counts (counts AWAITING_REVIEW and STALE sessions). Refinement requires the daemon's `state` ↔ `computed_status` mapping to be exposed cleanly via `/v2/sessions`; current response carries both fields. Land before v3.0 ship-gate so the cap behavior matches V3_TICKETS.md spec. Tier 2. | MB-T06 green |
| `MB-F-MB-T06-OVERRIDE-FLOW` | V3_TICKETS.md MB-T06 also specifies "override flow with confirmation per vision §3.6" so the operator can spawn past cap. MB-T06 ships HARD-CAP only (SessionCapExceededError surfaces via IPC envelope to the renderer; no override path). Renderer-side modal that includes "[Spawn anyway]" button + confirmation dialog is workstation-UI work, scope-fenced from this ticket. Add an override path: either (a) operator clicks override in the modal → renderer emits a separate `workstation:spawn-requested-override` IPC with a flag; or (b) the cap-exceeded reply includes an "override token" the renderer surfaces back. Tier 2 — without override, at-cap operators cannot spawn even when intentional. | MB-T06 green |
| `MB-F-MB-T06-RENDERER-MODAL-UX` | MB-T06 wires the cap-error envelope (error_type=SessionCapExceeded, activeCount, cap, sessionName) but does NOT ship renderer-side display. Spawn modal (MB-T04 surface) currently treats all error replies generically. Add a SessionCapExceeded-specific branch that renders "Session cap (N) reached. M sessions active. Close one before spawning another." with a list of active session names + close-one affordance. Tier 2 — needed for V3_TICKETS.md MB-T06 acceptance ("at-cap state behaves per spec"). | MB-T06 green |
| `MB-F-MB-T06-HEADER-COUNT-DISPLAY` | V3_TICKETS.md MB-T06 also says "Header surfaces current session count, spawn button disables at cap with hover text." MB-T06 ships server-side cap enforcement; the dashboard header counter + spawn-button disable-at-cap state is workstation-shell UI work, not in this ticket's scope. Wire a periodic GET /v2/sessions poll (or /v2/events/stream subscription per CONSOLE-T03 menu pattern) to drive a header count display + button-enable predicate. Tier 2. | MB-T06 green |
| `MB-F-MB-T05-PATH-ALLOWLIST-CLAUDE-RESOLUTION` | **CLOSED by batch-6 Session A (cairn finding #72).** Dogfooded 2026-05-03: spawn-from-workstation appeared to succeed (modal closed, no error toast) but tmux session never started; daemon record orphaned. Root cause: `spawn-env.ts` `ALLOWLIST_PATH` excludes `~/.local/bin` (Anthropic official-installer location); tmux exited 0 then claude failed to exec. Permanent fix lands `packages/dispatch-workstation/src/main/binary-resolver.ts` with `resolveClaudeBin()`; `defaultSpawnHandlerDeps` resolves the absolute claude path at workstation startup; tmux argv now passes the absolute path verbatim, bypassing PATH lookup. Unresolved-bin guard in `spawnSession` surfaces SpawnFailed up-front when resolution fails. Tier 1 ship-gate blocker; KNOWN-validated for operator's specific environment, MODELED for other install paths (path-agnostic fix). | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-MB-T05-POST-SPAWN-LIVENESS-CHECK` | **CLOSED by batch-6 Session A (cairn finding #73).** Defense-in-depth companion to #72. Architectural assumption being repaired: tmux's `new-session -d` exit-0 means session-created, NOT program-running. Without a post-spawn check, any future failure mode where claude dies on exec (corrupt binary, license refused, immediate crash) would leave an orphaned daemon record. Permanent fix: between `runTmuxNewSession` and daemon `registerSession`, sleep `livenessCheckDelayMs` (default 500ms in production, 0ms in unit tests) then run `tmux has-session -t <name>`; on rejection throw SpawnFailed and skip daemon registration. New `runTmuxHasSession` dep + `livenessCheckDelayMs` knob threaded through `SpawnHandlerDeps`. Tier 1 defense-in-depth; 500ms heuristic flagged for measurement-driven tuning in a future ticket. | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-MB-T05-SPIKE-ENVIRONMENT-VALIDATION` | **CLOSED by batch-6 Session A (cairn finding #74).** Methodology amendment only; no code. MB-S02 spike (basis for `spawn-env.ts` ALLOWLIST_PATH) was run on a Homebrew-only system and baked the assumption "claude lives at /opt/homebrew/bin" into production code without enumerating the install-method variation space. Lesson: spike evidence about operator-installed binaries needs validation across documented install methods, not the spike-runner's environment alone. Binding decision (filed at `docs/adr/MB-S02-spike-environment-validation-amendment.md`): future spikes touching operator-environment-dependent assumptions MUST enumerate the install/configuration variation space and explicitly mark un-validated variations as SPECULATIVE in the ADR. Binds future spikes; does NOT retroactively re-open MB-S02. Tier 2 methodology. | MB-T05 green → CLOSED batch-6 Session A |
| `MB-F-PARALLEL-CAIRN-SCHEMA-FILE-MERGE-CONFLICT` | When two parallel-cairn branches both add schemas to the same file but cut from different parents (one branch cuts from the common ancestor before the other has merged), the second-merging branch hits a content conflict. The original parallel-cairn coordination prevented sess-mbt10 WB1 from committing schema before sess-mbt09 WB1 (RULE 1), but did not specify rebase-before-merge for the second-merging branch. Today's MB-T10 merge to main hit this conflict; resolved via rebase-then-merge with content-additive resolution. Add to parallel-cairn coordination prompt template: "Second-merging schema-file branch must rebase onto post-first-merge main BEFORE attempting its own merge to main." Discoverability: parallel-cairn coordination doc. Tier 1. | sess-mbt10 merge |
| `MB-F-CC-STREAM-IDLE-TIMEOUT-MID-COMMIT` | Long CC sessions can timeout mid-execution between file-write and commit. Per-commit-push discipline is the load-bearing safety net — because each WB ends with commit+push, a stream timeout interrupting between WB4 close and WB5 commit leaves zero ambiguity (file on disk, branch HEAD = WB4, no half-state). Today's MB-T10 WB5 was recovered cleanly via verify-existing-file path (read the orphan file, validate against probe expectations, integrate, commit). Recovery prompt template should default to verify-existing rather than regenerate when a recovered uncommitted artifact exists. Discoverability: recovery prompt template + chat-layer SOP. Tier 1. | sess-mbt10 WB5 recovery |
| `MB-F-INTEGRATION-TEST-ELECTRON-PROCESS-LEAK` | Workstation integration tests spawn Electron processes and don't clean up reliably. Multiple incidents this session (87 zombies, 30+ zombies, 10 zombies in separate clusters). Tests need afterEach/afterAll cleanup that explicitly kills spawned Electron processes. Currently mitigated via manual pkill -9 -f "[Ee]lectron" between phases. Discoverability: test runner ergonomics; future Workstation test infra ticket. Tier 1. | sess-mbt09+10 test-infra |
| `MB-F-WORKSTATION-KANBAN-VIEWPORT-BLANK` | Workstation main viewport renders only event-log strip + filter controls; kanban board panel below event strip is blank black area despite scrollbar thumb-to-track ratio (~1/4) suggesting content exists. Captured 2026-05-06 12:07pm during MB-T10 development. Possibly caused by stale dispatch-workstation dist (running May 5 build against post-MB-T07-T08 source) — should fix with rebuild after MB-T09 + MB-T10 ship. If kanban still blank after Phase 2 dist rebuild + Workstation relaunch, this becomes a real rendering regression worth triaging before v3.0 ship-gate. Affects v3.0 §8 ship-gate criterion. MB-T12 (auto-mount + tiling) supersedes the kanban panel rendering if it lands first. Discoverability: post-Tier-E ship-gate triage. Tier 1. → **RE-DIAGNOSED 2026-05-06 post-Phase-2 rebuild + Option-A fix attempt:** original "stale dist" premise was a partial truth (dist WAS stale, AND the worktree it was originally launched from had been removed from disk leaving the running Electron unable to lazy-load any resources), but a fresh rebuild of main HEAD revealed a more severe latent bug — Workstation could not launch at all due to MB-F-MB-T09-WORKSTATION-RUNTIME-IMPORT-BUG-SHIPPED (ERR_MODULE_NOT_FOUND on `dispatch-core/src/v3/schema.js`; helpers spawned then orphaned; 0 renderer processes; no window opened). Fix landed at 01dfd54; Workstation now launches cleanly (WINDOW_READY sentinel + 5-process tree with 2 renderer helpers). Status: **RESOLVED** — startup-crash path closed (01dfd54); kanban panel renders correctly (operator visual verification 2026-05-06 against post-fix dist). | sess-mbt10 dev observation 2026-05-06 → RESOLVED 01dfd54 (visual verified) |
| `MB-F-WORKTREE-FRESH-MISSING-DIST-CRASH` | Fresh worktrees created via git worktree add lack dist/ artifacts. Workstation tests that rely on dispatch-core/dist/v3/schema.js (resolved through pnpm workspace symlinks) fail at import-time on fresh worktrees until dispatch-core is built locally. Currently mitigated by spawning CC in worktrees that have dispatch-core dist/ pre-built. Future fix: pnpm pre-test hook that runs dispatch-core build if dist/ is absent or older than src/. Discoverability: worktree-spawn ergonomics; dev-onboarding doc. Tier 1. | sess-mbt09+10 worktree ergonomics |
| `MB-F-PRE-WB6-COST-ESTIMATE-VS-SCHEMA-MAXIMA-DIVERGENCE` | CONDUCTOR_V3_RESCOPE.md §3.5 line 136 quoted "~24KB max" before WB6 measurement. The 24KB number was a typical-content estimate that did NOT account for schema-permitted maxima (4KB recent_handoff per Q-MBT10-6=a, schema-uncapped recent_console_tail, populated pending_intents). At schema maxima the measured payload was 54.40 KB at N=8 sessions — over 2x the original estimate. Lesson: rescope-doc cost estimates that touch tiers capped at schema maxima must back-calculate from schema field caps + arithmetic, not from typical-content modeling. Future cost-validation sections should cite "≈ N × max-field-bytes" alongside typical estimates. Resolved in MB-T10 WB6 via HALT 2 Option A: rescope ceiling raised from 30KB to 60KB to match measured reality. Discoverability: rescope authoring SOP; build-doc review checklist. Tier 1. | sess-mbt10 WB6 |
| `MB-F-PNPM-INSTALL-FROZEN-LOCKFILE-DISCIPLINE` | CC needs to use pnpm install --frozen-lockfile in CI/build contexts to prevent silent lockfile mutation. Today's session had at least one incident where a CC ran pnpm install (without flag) which silently updated the lockfile. Future CC prompts should specify --frozen-lockfile when build-time install is required. Discoverability: CC prompt template + CI script ergonomics. Tier 2. | sess-mbt09+10 chat-layer |
| `MB-F-INTEGRATION-TEST-REGISTRY-LEAK` | Test fixtures (probe-mb-t05-card-*, probe-94-03-ask-*, probe-83-04-*, etc.) leave entries in the daemon registry after test runs complete. Visible 3+ hours after test execution as state_changed events in the Workstation event log. Same root-cause class as MB-F-INTEGRATION-TEST-ELECTRON-PROCESS-LEAK. Tests need afterEach/afterAll cleanup that removes the registry entries they create. Tier 2 — does not block ship but causes operator-facing UI noise. Discoverability: test runner ergonomics; test infra ticket. | sess-mbt09+10 test-infra |
| `MB-F-T10-COARCHITECT-IPC-WIRE-TIER4` | **CLOSED by sess-mbt11 WB7.** packages/dispatch-workstation/src/main/coarchitect-ipc.ts:137 originally passed spawnedSessions: null. Wiring closure shipped per Q-MBT11-7=a + Q-MBT11-8=a: (a) NEW packages/dispatch-workstation/src/main/tier4-fan-out.ts hosts the daemon HTTP fan-out helper buildTier4Payload that calls GET /v3/sessions/:name/context-snapshot per registered non-killed session AND merges workstation autopilot state (pending_intents + last_action_fired_at) into each SessionContextSnapshot per Q-MBT11-8=a (daemon endpoint stays returning hardcoded []/null per context-snapshot.ts:167; workstation overwrites authoritatively); (b) session-name source = HttpSessionListClient.listSessions filtered by !archived && !killed; (c) Promise.all fan-out via existing tier4-builder.assembleTier4Payload; (d) coarchitect-ipc.ts:141 replaced `spawnedSessions: null` with `await buildTier4Payload({ sessionListClient, autopilot, fetchImpl, daemonUrl, daemonToken }).catch(() => null)`. Originally Tier 2; closure validated via WB7 unit tests + runtime-relaunch smoke. | sess-mbt10 WB5 → CLOSED sess-mbt11 WB7 |
| `MB-F-MB-T09-WORKSTATION-RUNTIME-IMPORT-BUG-SHIPPED` | **CLOSED by 01dfd54.** MB-T09 (sess-mbt09) shipped 3 runtime-blocking value imports + 2 type-only same-pattern imports in workstation main process using `dispatch-core/src/...js` paths. TypeScript path-mapping resolved through workspace src-resolution; Node ESM at runtime requires actual on-disk `dispatch-core/dist/...js` artifact (only `.ts` source exists at `src/`). Bug passed pre-merge typecheck + unit tests + integration tests; surfaced only at workstation runtime relaunch from fresh main HEAD during 2026-05-06 post-cleanup verification. Crash mode: `ERR_MODULE_NOT_FOUND` on first lazy-resolved import; Electron spawned helper processes (GPU + network) then crashed before opening any renderer process; no window. Affected files: `session-send-prompt-ipc.ts` (3 value + 1 type-only), `card-ipc.ts` (1 type-only multi-import block), `http-daemon-client.ts` (1 type-only). Fix at 01dfd54: all 5 imports patched to `dispatch-core/dist/...js`; rebuild + relaunch confirmed `WINDOW_READY` sentinel + 5-process tree (main + GPU + network + 2 renderers). Was a v3.0 ship-gate blocker — workstation would not start at all from main HEAD between sess-mbt09 merge (7a9e564) and the fix (01dfd54). Tier 1. Discoverability: `git grep "dispatch-core/src/" packages/dispatch-workstation/src/`. | sess-mbt10 cleanup relaunch → CLOSED 01dfd54 |
| `MB-F-WORKSTATION-RUNTIME-RELAUNCH-AS-MERGE-GATE` | MB-T09 ran a full WB ladder (typecheck + unit tests + integration tests + spike validation + finding doc) and merged cleanly to main; the runtime ESM-import bug above was invisible to all of those gates because TypeScript accepts `dispatch-core/src/...js` via workspace path-mapping. Lesson: workstation merges that touch `src/main/*.ts` must include a runtime-launch smoke step (`pnpm --filter dispatch-workstation exec electron dist/main/main.js` → observe `WINDOW_READY` sentinel within N seconds) as part of the merge gate, not a post-merge dogfood. Future cairn-coord prompt template should add: "Workstation merges that touch main-process src files MUST include a runtime-launch smoke verification step before merge to main; ERR_MODULE_NOT_FOUND class bugs are invisible to typecheck + unit + integration suites." Tier 1 — methodology fix prevents recurrence of the MB-T09-class import-pattern bug + similar runtime-only failure modes. Discoverability: parallel-cairn coordination doc + chat-layer SOP. | sess-mbt10 cleanup relaunch |
| `MB-F-DISPATCH-CORE-DUAL-IMPORT-PATTERN-DRIFT` | Codebase had two import patterns coexisting against dispatch-core: (a) `dispatch-core/dist/v3/schema.js` (correct — points at the actual built artifact, runtime-resolvable via pnpm workspace symlink) used by sess-mbt10 territory + most pre-existing code; (b) `dispatch-core/src/v3/schema.js` (broken at runtime — TypeScript accepts via path-mapping, Node ESM rejects since no `.js` exists at `src/`) used by sess-mbt09 territory. After 01dfd54 all 5 known broken-pattern imports are patched and the codebase is consistent (all dispatch-core imports use `dist/`). Future hardening: add an ESLint rule (or a tsconfig path restriction) that bans `from 'dispatch-core/src/...'` patterns in workstation source to prevent future drift. Tier 2 — immediate bug closed by 01dfd54; this entry tracks the optional tooling-side hardening to prevent future regression. Discoverability: ESLint config + tsconfig.json review pass. | sess-mbt10 cleanup relaunch |
| `MB-F-T11-WB7-ORCHESTRATOR-SPAWN-DEFERRED` | MB-T11 WB7 wired the orchestrator-action-handler dispatcher's `fireSpawn` dep with a placeholder that throws "orchestrator-fired spawn-new-session not wired in v3.0". Operator-driven spawn (MB-T05) flows through the renderer → `workstation:spawn-requested` IPC path unchanged. v3.0 acceptance test (WB8) spawns sessions via the smoke harness, not via orchestrator-fired action, so this gap is non-blocking for v3.0 ship. Future closure: wire orchestrator-fired spawn through the existing SpawnIpcController.handleSpawnRequest, threading `defaultSpawnHandlerDeps()` (which is async — needs the same controllerPromise pattern as `registerSpawnIpcHandlers` in spawn-ipc.ts:266). Tier 2 — orchestrator-driven spawn becomes useful once the swarm-conductor flow regularly spawns sessions mid-plan; v3.0 dogfood will reveal whether this surfaces. Discoverability: coarchitect-ipc.ts inline comment in fireSpawn dep + this row. | sess-mbt11 WB7 |
| `MB-F-T11-T13-RESOLVER-STUB` | sess-mbt11 ships `packages/dispatch-workstation/src/main/approval-policy-resolver-stub.ts` per Q-MBT11-6=a + parallel-cairn coordination Rule 3. Stub always returns `{approvalRequired: true, reason: 'stub: ...'}` regardless of input — most-conservative default while sess-mbt13 territory (real resolver) is in flight. Closure path (post-cross-merge of sess-mbt11 + sess-mbt13): (a) sess-mbt13's real resolver lands at `packages/dispatch-workstation/src/main/approval-policy-resolver.ts`, exposing `resolveApproval(input: ResolverInput): ResolverResult` against the same contract surface this stub names; (b) `orchestrator-action-handler.ts` (MB-T11 WB5) swaps its import from `approval-policy-resolver-stub.js` to `approval-policy-resolver.js`; (c) the stub file + its 2-probe test directory are deleted; (d) probe-02-stub-shape-matches-interface.spec.ts has a `MB-F-T11-T13-RESOLVER-STUB`-substring assertion that intentionally fails on real-resolver swap to force test cleanup. Tier 1 — blocks correct §3.2 approval-policy semantics. Discoverability: src/main/approval-policy-resolver-stub.ts file-header docblock + this row. | sess-mbt11 WB4 (cross-merge with sess-mbt13) |
| `MB-F-T13-TILE-HEADER-PICKER-INTEGRATION` | Q-MBT13-10=b deferral: tile UI does NOT exist at sess-mbt13 ship time (MB-T12 unbuilt; coordination-doc-named territory `dispatch-workstation/src/renderer/tile-header/*` is non-existent). MB-T13 ships data layer + resolver + audit + IPC only; the per-session tile-header dropdown that lets operators flip approval policy via the tile UI lands as a follow-on after MB-T12 ships. Implementation sketch: tile-header component reads policy via `GET /v3/sessions/:name/approval-policy` (already shipped at WB5), renders dropdown with three options (tight/medium/loose), fires `PUT /v3/sessions/:name/approval-policy` (already shipped at WB5) on change. No daemon-side work; pure UI integration with already-shipped routes. Tier 2 — sess-mbt13 ship-gate satisfied without it (operator can still set policy via direct HTTP / future settings UI). Discoverability: coordination doc + this followup. | sess-mbt13 WB11 |
| `MB-F-T13-SETTINGS-DEFAULT-POLICY-INTEGRATION` | Q-MBT13-11=a deferral: MB-T11-old (settings UI from MB-F-MB-T05-API-KEY-SETTINGS-UI followup) is unbuilt; ticket body referenced "Default policy configured via workstation settings (existing MB-T11 settings UI from prior scope, now MB-T11-old/MB-T13-new — needs renumber, see §6)". sess-mbt13 ships SQL DEFAULT 'medium' as the structural default (per Q-MBT13-4=c defense-in-depth: SQL DEFAULT + workstation-side resolver fallback on no-row, both medium). When MB-T11-old / MB-T14 settings UI lands, surface a "Default approval policy" dropdown in workstation settings; the workstation reads the configured default at session-spawn time and PUTs the policy for the new session via the already-shipped `PUT /v3/sessions/:name/approval-policy` route. No daemon-side work needed; pure settings-UI integration. Tier 2 — sess-mbt13 ship-gate satisfied without it. Discoverability: this followup + CONDUCTOR_V3_RESCOPE.md §6 / §7 Q2. | sess-mbt13 WB11 |
| `MB-F-T11-T13-ACTION-TYPE-ENUM-DEDUP` | Q-MBT13-12=a parallel-definition: sess-mbt13's `ApprovalActionTypeEnum` (in dispatch-core/src/v3/schema.ts §13) and the resolver's local `ApprovalActionType` type (in dispatch-workstation/src/main/approval-policy-resolver.ts) are structurally parallel to sess-mbt11's §12 ActionType (which sess-mbt11 will ship as part of orchestrator action tools). Both branches define the same 5-value enum independently to avoid cross-territory writes pre-merge. Post-cross-merge dedup: choose ONE canonical source (likely sess-mbt11's §12 enum since it owns OrchestratorOutputSchema discriminated union extensions), re-export from §13 (or alias), update resolver to import the canonical type. No semantic change (the enums match by construction); pure schema-side cleanup. Tier 2 — minor schema duplication does NOT block ship. Discoverability: schema.ts §13 doc-comment on ApprovalActionTypeEnum. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-MODAL-FILTERS` | Q-MBT13-9=a v3.1 enrichment: WB8 audit-modal renders LIMIT 100 ORDER BY ts DESC (single page). Operator may want filter axes (session_name, action_type, date range, approval_status) for forensics on large audit logs. Implementation: extend `OrchestratorSwarmAuditQuerySchema` (§13) with optional filter fields; route handler builds dynamic WHERE clause; modal UI adds filter controls (dropdowns / date pickers / text inputs); paginated load-more button replaces single-page render. Tier 3 — single-page LIMIT 100 is sufficient for v3.0; only matters if audit logs grow beyond a few hundred rows. Discoverability: this followup + WB8 commit body + Q-MBT13-9=a arbitration record. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-WRITE-RETRY-QUEUE` | Q-MBT13-7=b risk R5: audit-write is async-after-fire, best-effort with single-shot retry. A workstation crash during the (action-fire → audit-write) window can lose audit rows. Acceptable per "forensics, not gating" framing (per CONDUCTOR_V3_RESCOPE.md §3.8: "useful for forensics when autopilot did something unexpected"). Future enhancement: persist failed audit-writes locally (e.g., to electron-store at ~/.foxworks-dispatch/pending-audit.json) and replay on next workstation startup. Tier 2 — defer to v3.1 unless operator dogfood surfaces lost-rows scenarios. Discoverability: Phase 1 diagnose risk R5 + this followup. | sess-mbt13 WB11 |
| `MB-F-T13-SESSION-POLICY-CLEANUP-ON-KILL` | Q-MBT13-1=a side-effect: sessions persist in `sessions.json` (RegistryV2, frozen v2 schema); `session_policies` is a separate SQLite table per coexistence-without-sync rule. When a session is killed (transitioned to `state=killed` in sessions.json), the corresponding `session_policies` row is NOT auto-cleaned. Stale rows accumulate over time — minor concern (rows are tiny, indexed on session_name, no GET-path cost), but a tidy session re-spawn under the same name could pick up the stale policy unexpectedly. Future enhancement: wire DELETE FROM session_policies WHERE session_name=? at the daemon's PATCH /v2/sessions/:name/state handler when the new state is 'killed'. Tier 2 — non-blocking; the stale-row scenario is operationally rare and the policy-pickup behavior is operator-acceptable (any policy mismatch is observable via the tile-header picker once MB-T12 + MB-F-T13-TILE-HEADER-PICKER-INTEGRATION ship). Discoverability: this followup + WB5 route comment + Q-MBT13-1=a doc. | sess-mbt13 WB11 |
| `MB-F-T13-AUDIT-SCHEMA-CONVERGENCE` | Risk R6 from Phase 1 diagnose: dispatch-core/src/v3/schema.ts has TWO audit schemas: §5 `OrchestratorAuditRowSchema` (operator-facing approve/decline of orchestrator chat output, freeze-anchor surface from MB-S03) and §13 `OrchestratorSwarmAuditRowSchema` (orchestrator swarm-action firings, sess-mbt13 surface). They are structurally similar but semantically orthogonal per §3.8 (different surfaces, separate forensics). v3.1 may want to converge them into a single discriminated-union audit table if convergence becomes desirable for unified forensics tooling. Tier 3 — orthogonal v3.0 surfaces are correct as-is; this followup tracks the optional convergence direction for v3.1+ if cross-surface forensics becomes a need. Discoverability: schema.ts §5 + §13 + this followup. | sess-mbt13 WB11 |
| `MB-F-DISPATCH-CORE-POST-PULL-REBUILD-DISCIPLINE` | After any merge to main that adds new exports to dispatch-core/src/v3/schema.ts (or any dispatch-core source), the workstation typecheck WILL fail until `pnpm --filter dispatch-core build` is run to refresh dist/ artifacts. Workstation imports use `dispatch-core/dist/v3/schema.js` paths (per MB-F-DISPATCH-CORE-DUAL-IMPORT-PATTERN-DRIFT lesson) and TypeScript reads exported member declarations from the dist .d.ts files, NOT from src. Manifested 2026-05-06 post-sess-mbt13-merge: workstation typecheck failed with TS2724 errors on the new §13 OrchestratorSwarmAudit* exports until dist was rebuilt. Recommend: (a) postinstall hook running `pnpm -r build` for dispatch-core, OR (b) merge-gate adding `pnpm --filter dispatch-core build` between merge-commit and post-merge typecheck verification, OR (c) explicit operator-discipline note in merge runbook. Tier 1 — every dispatch-core schema-additive merge will reproduce this until addressed. Discoverability: this followup + 2026-05-06 main-merge incident. | sess-mbt13 merge 2026-05-06 |
