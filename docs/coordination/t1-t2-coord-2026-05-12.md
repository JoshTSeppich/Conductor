# T1 ↔ T2 cross-session coordination — 2026-05-12

**Authored:** 2026-05-12 by T1 sub-session at WB1 entry
**Anchor commits:** T1 ticket body at `ec60622`; T2 ticket body authoring in parallel (HEAD evolves)
**Authority:** full-build-mode-dispatch.md §5.1 path-disjointness coordination + CLAUDE.md §4.3 cross-session coordination notes pattern

---

## §1 — File ownership (path-disjointness map)

### T1 primary territory (MB-T-WIREFRAME-T1-SESSION-DATA-FLOW)

Per ticket body `docs/build-docs/CONDUCTOR_MB-T-WIREFRAME-T1-SESSION-DATA-FLOW_BUILD.md` §2.4 + §5.5:

**NEW files (T1 exclusive):**
- `packages/dispatch-workstation/src/frame-c/sessions-context.tsx` (or equivalent per Sub-Q-A=(α) renderer-only useState)
- `packages/dispatch-workstation/src/frame-c/model-badge.ts`
- `packages/dispatch-workstation/src/frame-c/status-color.ts`
- `packages/dispatch-workstation/src/frame-c/uptime-format.ts`
- `packages/dispatch-workstation/src/frame-c/session-filter-bar.tsx`
- `packages/dispatch-workstation/test/unit/frame-c/probe-mbtwt1-*.spec.{ts,tsx}` (probes WB1-WB12)

**MOD files (T1 primary edits):**
- `packages/dispatch-workstation/src/frame-c/session-list.tsx` — extensions: status colors, model badge, uptime span, filter-aware rendering
- `packages/dispatch-workstation/src/tile-grid/mount.ts` lines 175-181 — `tryAutoMountFrameC` body (replace empty `sessions: []` stub)
- `packages/dispatch-workstation/src/tile-grid/tile-grid.tsx` lines 29-52 — additive `spawnedAtMs?` field to `TileGridSessionEntry`
- `packages/dispatch-workstation/src/tile-grid/types.ts` line 22 — additive `'error' | 'warning'` extension to `TileStatus` enum (Sub-Q-C=(i))
- `packages/dispatch-workstation/src/main/spawn-handler.ts` — additive `model?: string` field (Sub-Q-B=(i)) on `SpawnSessionResult`

**MOD files (T1 + T2 SHARED — coordination required):**
- `packages/dispatch-workstation/src/frame-c/frame-c-root.tsx` — see §2 below

### T2 primary territory (MB-T-WIREFRAME-T2-FOCUSED-TERMINAL-STREAM, ticket-body authoring in flight at coord-note time)

Per dispatch §2 T2 enumeration:

**NEW files (T2 exclusive):**
- `packages/dispatch-workstation/test/unit/frame-c/probe-mbtwft2-*.spec.tsx` (probes — `probe-mbtwft2-01-terminal-stream-mount.spec.tsx` + `probe-mbtwft2-02-header-bar-renders.spec.tsx` observed untracked at coord-note time; not yet committed)
- (likely) `packages/dispatch-workstation/src/frame-c/detail-pane-terminal-stream.tsx` OR inline detail-pane.tsx extensions

**MOD files (T2 primary edits):**
- `packages/dispatch-workstation/src/frame-c/detail-pane.tsx` — header bar (branch/ctx/uptime/plan), live PTY tail body, tool-invocation indicators, cooking timer

**MOD files (T1 + T2 SHARED — coordination required):**
- `packages/dispatch-workstation/src/frame-c/frame-c-root.tsx` — see §2 below

---

## §2 — `frame-c-root.tsx` shared edit window

`[KNOWN]` This file is the only T1 / T2 collision risk. Both workstreams need to modify it:

| T1 needs | T2 needs |
|---|---|
| Add `useState<TileGridSessionEntry[]>` + `useEffect(workstationBridge.onSpawnResult)` for sessions-stream consumption (WB4) | Pass `selectedEntry` (already wired Wave C #5 `bd31b94`) to extended DetailPane — likely no edit needed at frame-c-root level |
| Add `useState<FilterState>` + pass filtered sessions to SessionList (WB11) | (no edit) |
| Thread `workstationBridge` prop through from mount factory (WB4) | (likely no edit — DetailPane already receives `tokensUsed`/`tokenBudget` via Wave C #5) |

**Verdict:** T2's likely scope is **read-only** at frame-c-root.tsx (DetailPane prop threading already shipped at `bd31b94`). T1 owns the active edit window.

**Edit discipline:**
1. T1 freely modifies `frame-c-root.tsx` at WB4, WB11 (filter integration), and WB-final.
2. T2 reads `frame-c-root.tsx` for prop-threading reference but does NOT edit unless DetailPane prop shape must extend (e.g., new prop for header-bar uptime). If T2 needs an edit:
   - File an entry in §3 below before commit
   - Coordinate ordering: T1 WB4 lands → T2 reads new `frame-c-root.tsx` → T2 extends if needed
3. Per-path `git add -- src/frame-c/frame-c-root.tsx` mandatory in both sessions (CLAUDE.md §2.7; gap-closure per `MB-F-CROSS-SESSION-STAGING-AREA-COMMIT-CONTAMINATION` 8d178a4).

---

## §3 — Active edit window claims

| Session | File | Lines (rough) | Operation | Claimed at | Released at |
|---|---|---|---|---|---|
| T1 | `frame-c/frame-c-root.tsx` | full file | Add sessions-stream subscription + filter state | WB4 RED→GREEN window | WB4 GREEN commit |
| T1 | `frame-c/frame-c-root.tsx` | full file | Add filter state plumbing | WB11 RED→GREEN window | WB11 GREEN commit |
| (none) | (none) | (none) | (none) | (none) | (none) |

T2 sessions: append entries here BEFORE editing `frame-c-root.tsx`.

---

## §4 — Cross-session probe naming (path-disjoint)

T1 probes: `probe-mbtwt1-NN-<descriptor>.spec.{ts,tsx}` (11+ probes across WB1-WB12)
T2 probes: `probe-mbtwft2-NN-<descriptor>.spec.tsx` (existing convention)
T3 probes: `probe-mbtwft3-NN-<descriptor>.spec.tsx` (existing convention)

All under `packages/dispatch-workstation/test/unit/frame-c/` — flat directory, prefix-disjoint by ticket. No filename collision risk.

---

## §5 — Sub-Q resolution propagation (operator-arbitrated 2026-05-12)

T1 Sub-Q resolutions (operator ack 2026-05-12; commit `ec60622`):
- A=(α) renderer-only useState (independent subscription pattern mirroring `tile-grid-app.tsx:159-185`)
- B=(i) workstation spawn-handler model field extension
- C=(i) extend TileStatus enum + renderer-derived from PTY-tail sentinels
- D=(i) renderer-internal mount-time uptime (lazy useState snapshot per MB-T18 pattern)
- E=(α) renderer-only useState filter
- F=(α) keep selection persistence deferred (Wave B Sub-Q-MBTWBFCS-A=α unchanged)

**Impact on T2:** Sub-Q-A=(α) means T2 should NOT assume T1 publishes sessions via window CustomEvent or React Context. If T2 needs sessions data in DetailPane, threading via `selectedEntry` prop (already wired Wave C #5) is the channel. No new shared infrastructure.

---

## §6 — Hand-off cadence

- T1 announces WB4 GREEN landing (sessions-stream wired) in this doc §3 release column + dispatch coord
- T2 reads §3 to determine if edit window open before any `frame-c-root.tsx` modification
- Both sessions surface HALT-CROSS-SESSION immediately if conflicting edits detected at pre-commit `git status --short`
