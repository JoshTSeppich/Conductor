foxworks-dispatch v1 shipped. Tickets FD-T01 through FD-T11 landed in strict red-then-green pairs, the §C0 e2e loop test (`test/e2e/full-loop.test.ts`) exercises the §2.1 one-sentence workflow end-to-end against a real tmux pane, and the full suite is at 43 green tests with a clean typecheck; both freeze commits (schema after FD-T02, footer after FD-T04) landed as empty markers as the contract required; `SPIKES.md` consolidates the KNOWN facts from spikes 01/02/03 against tmux 3.6a on darwin arm64. One MODELED-vs-KNOWN deviation surfaced and was resolved in Phase 0: `tmux has-session` on 3.6a validates the full `<sess>:<w>.<p>` target (not just the session portion as the model assumed), so production `hasSession()` simply exec's `has-session -t <fq-target>` and trusts the exit code — Spike 03 was rewritten to assert the KNOWN behavior before FD-T05 went green, and the deviation is called out at the probe site and in `SPIKES.md`. Non-contract additions in FD-T11 that stayed inside §3.1's allow-list: `StatusView` uses ink's standard color codes per state (yellow/red/cyan/gray) and shows a `loading registry…` placeholder for the first tick — both visual, neither changes derivation or sort order. One UNKNOWN is on the record: only tmux 3.6a was exercised here; if the operator runs on a materially older tmux and sees `has-session -t <fq-target>` misbehave, `SPIKES.md` Spike 03 §KNOWN #4 names the `display-message` fallback gated behind a session-only `has-session` call. No other tmux behaviors surprised mid-build (load-buffer/paste-buffer preserved every hazard character — backticks, `$`-vars, triple-backticks, nested quotes, embedded newlines — in both the FD-T05 integration tests and the live smoke in this session). To register your first real session, discover your Claude Code pane's fully-qualified target via `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index}'`, then run `./dist/bin/fd.js init sherpa --cwd ~/code/foxworks-sherpa --target sherpa:0.0` (substituting your real name, cwd, and target); from there the daily loop is `fd send <name> <prompt-file>` / `fd pull <name>` / `fd status`, with `README.md` covering the hand-off footer convention and the four common failure modes operators will actually hit.
