{
  "name": "dispatch-cli",
  "version": "0.0.0",
  "private": true,
  "description": "fd CLI — thin client for the Conductor daemon with fd v1 fallback.",
  "type": "module",
  "bin": {
    "fd": "dist/bin/fd.js"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "build": "tsc && chmod +x dist/bin/fd.js"
  },
  "dependencies": {
    "dispatch-core": "workspace:*"
  }
}
