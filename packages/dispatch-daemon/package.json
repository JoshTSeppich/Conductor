{
  "name": "dispatch-daemon",
  "version": "0.0.0",
  "private": true,
  "description": "Conductor daemon — HTTP+WebSocket server coordinating CC sessions per CONDUCTOR_API_CONTRACT.md.",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "spike:S05": "tsx spikes/DAEMON-S05-http-server.ts",
    "spike:S01": "tsx spikes/DAEMON-S01-websocket.ts",
    "spike:S02": "tsx spikes/DAEMON-S02-fsevents.ts"
  },
  "dependencies": {
    "dispatch-core": "workspace:*"
  },
  "devDependencies": {
    "@fastify/websocket": "^11.0.0",
    "@types/ws": "^8.0.0",
    "fastify": "^5.0.0",
    "ws": "^8.0.0"
  }
}
